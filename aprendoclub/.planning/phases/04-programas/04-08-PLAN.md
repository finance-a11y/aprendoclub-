---
phase: 04-programas
plan: 08
type: execute
wave: 3
depends_on: [04-01, 04-03, 04-04, 04-05, 04-06]
files_modified:
  - components/programas-section.tsx
  - app/(site)/page.tsx
  - content/site.ts
  - app/sitemap.ts
autonomous: true
requirements: [PROG-06, PROG-01, PROG-02, PROG-03, PROG-04]
must_haves:
  truths:
    - "El home muestra una sección de programas con las 3 cards, visible sin scroll profundo"
    - "La sección enlaza al hub /programas y cada card a su página"
    - "El footer apunta Taller → /programas/taller-seo-con-ia y Reto → /reto (ya no al placeholder)"
    - "El sitemap incluye /programas/taller-seo-con-ia y /reto"
    - "npm run build pasa sin errores"
  artifacts:
    - path: "components/programas-section.tsx"
      provides: "Sección de programas del home"
      contains: "ProgramCard"
    - path: "app/sitemap.ts"
      provides: "Sitemap con las rutas nuevas"
      contains: "/reto"
  key_links:
    - from: "app/(site)/page.tsx"
      to: "components/programas-section.tsx"
      via: "render de la sección en el home"
      pattern: "ProgramasSection"
    - from: "content/site.ts"
      to: "/programas/taller-seo-con-ia, /reto"
      via: "footerColumns repuntadas"
      pattern: "taller-seo-con-ia"
---

<objective>
Cerrar la fase: sección de programas en el home (PROG-06), repunte de los links del footer a las páginas reales, alta de las rutas nuevas en el sitemap y verificación de build completa.

Purpose: Visibilidad de programas desde el home (Success Criteria 3) y limpieza de enlaces/sitemap para no dejar placeholders ni rutas huérfanas.
Output: sección home + footer repuntado + sitemap + build verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-programas/04-CONTENT-SOURCE.md
@.planning/phases/01-fundaciones-y-navegaci-n/01-UI-SPEC.md
@app/(site)/page.tsx
@content/site.ts
@app/sitemap.ts
@components/beneficios-section.tsx

<interfaces>
<!-- Consumir de Plan 04-01: content/programas.ts (programas, homeProgramas) y
     components/program-card.tsx. Insertar la sección en el home tras Beneficios o
     Precios (punto visible sin scroll profundo). Card en versión compacta
     (prop compact de ProgramCard). Patrón de sección: beneficios-section.tsx
     ("use client", useInView, eyebrow #b8f60d, useReducedMotion si anima). -->
Estado actual del home (app/(site)/page.tsx): HeroSection, ProblemaSection,
BeneficiosSection, PricingSection, TestimoniosSection, InstructorSection,
FaqSection, CtaSection, StickyCTAMobile. Insertar ProgramasSection tras BeneficiosSection.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sección de programas del home + inserción</name>
  <files>components/programas-section.tsx, app/(site)/page.tsx</files>
  <action>
Crear components/programas-section.tsx ("use client", useInView + useReducedMotion, patrón beneficios-section) que renderiza: eyebrow "NUESTROS PROGRAMAS", título "Formación para cada etapa de tu carrera", subtítulo "Elige cómo quieres aprender SEO e IA." (todo desde `homeProgramas` de content/programas.ts), grid de las 3 `programas` usando `<ProgramCard program={p} compact />`, y un botón "Ver todos los programas" → /programas (Link de next/link). Fondo alternado (bg-[var(--bg-secondary)] o var(--bg-primary)) para separar visualmente de las secciones vecinas. Accent solo en eyebrow y CTA.

Insertar `<ProgramasSection />` en app/(site)/page.tsx justo después de `<BeneficiosSection />` (punto visible sin scroll profundo). Añadir el import.
  </action>
  <verify>
    <automated>test -f components/programas-section.tsx && grep -q "ProgramasSection" "app/(site)/page.tsx" && grep -q "useReducedMotion" components/programas-section.tsx && echo OK</automated>
  </verify>
  <done>El home muestra la sección de programas (3 cards + CTA al hub) tras Beneficios; data-driven; useReducedMotion; accent limitado.</done>
</task>

<task type="auto">
  <name>Task 2: Repunte de footer + sitemap</name>
  <files>content/site.ts, app/sitemap.ts</files>
  <action>
En content/site.ts, `footerColumns` columna "Programas": cambiar el href de "Taller SEO con IA" de "/programas" a "/programas/taller-seo-con-ia" y el de "Reto 7 días" de "/programas" a "/reto". Actualizar los comentarios (quitar los "Fase 4: repuntar..."). Diplomado sigue → /diplomado. Econía sigue diferido (no agregar).

En app/sitemap.ts, agregar entradas para "https://aprendoclub.com/programas/taller-seo-con-ia" y "https://aprendoclub.com/reto" (changeFrequency "monthly", priority 0.8) y quitar el comentario TODO Fase 4. /diplomado ya está.
  </action>
  <verify>
    <automated>grep -q "programas/taller-seo-con-ia" content/site.ts && grep -q '"/reto"' content/site.ts && grep -q "/reto" app/sitemap.ts && grep -q "taller-seo-con-ia" app/sitemap.ts && echo OK</automated>
  </verify>
  <done>Footer apunta a las páginas reales del Taller y el Reto; sitemap incluye ambas rutas nuevas.</done>
</task>

<task type="auto">
  <name>Task 3: Verificación de build de toda la fase</name>
  <files>app/(site)/page.tsx</files>
  <action>
Ejecutar `npm run build` y resolver cualquier error introducido por la fase (imports, tipos, rutas). Verificar que las rutas /programas, /programas/taller-seo-con-ia, /reto, /diplomado compilan. Verificar que no queda ningún import a components/diplomado/navbar o footer. No cambiar contenido para "arreglar" el build salvo errores reales de código.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30 | grep -Eq "Compiled successfully|✓ Compiled|Generating static pages" && echo BUILD_OK</automated>
  </verify>
  <done>`npm run build` pasa; las 4 rutas de programas compilan; sin imports colgantes del diplomado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → enlaces internos/externos | footer y sección home enlazan rutas internas + WhatsApp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Tampering | enlaces externos del footer | mitigate | socials/WhatsApp ya con rel noopener (shell Fase 1) |
| T-04-17 | Information Disclosure | páginas estáticas | accept | sin input ni PII |
| T-04-SC | Tampering | instalación de paquetes | accept | no se instalan paquetes |
</threat_model>

<verification>
- Home muestra ProgramasSection tras Beneficios, visible sin scroll profundo.
- Footer: Taller → /programas/taller-seo-con-ia, Reto → /reto.
- sitemap.ts incluye ambas rutas nuevas.
- `npm run build` verde; sin rutas rotas ni imports colgantes del diplomado.
- Sin em/en dashes introducidos.
</verification>

<success_criteria>
Success Criteria 3 de la fase (home muestra/enlaza programas) + limpieza final de enlaces y sitemap. Con esto, los 4 Success Criteria de Phase 4 quedan cubiertos.
</success_criteria>

<output>
Crear `.planning/phases/04-programas/04-08-SUMMARY.md` al terminar.
</output>
