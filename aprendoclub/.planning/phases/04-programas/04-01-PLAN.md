---
phase: 04-programas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/program-card.tsx
  - content/programas.ts
  - content/taller-seo-con-ia.ts
autonomous: true
requirements: [PROG-01, PROG-03, PROG-06]
must_haves:
  truths:
    - "El hub y el home pueden importar datos tipados de los 3 programas desde content/programas.ts"
    - "La página del taller puede importar sus datos tipados desde content/taller-seo-con-ia.ts"
    - "Existe un ProgramCard reutilizable que renderiza badge, nombre, descripción, precio y CTA"
  artifacts:
    - path: "components/program-card.tsx"
      provides: "Card reutilizable de programa (hub + home)"
      min_lines: 30
    - path: "content/programas.ts"
      provides: "Datos de los 3 programas + hero hub + hero home + CTA final"
      contains: "export const programas"
    - path: "content/taller-seo-con-ia.ts"
      provides: "Datos del taller (módulos, incluye, precio, público, CTA)"
      contains: "export const"
  key_links:
    - from: "components/program-card.tsx"
      to: "content/programas.ts"
      via: "tipo Program importado"
      pattern: "import.*Program.*from.*programas"
---

<objective>
Crear la capa de contenido data-driven de los programas (hub, taller) y el componente de card reutilizable que consumirán el hub `/programas` (Plan 03) y la sección del home (Plan 08).

Purpose: Separar contenido de presentación (patrón Payload-ready ya establecido en content/quienes-somos.ts) para que las páginas solo rendericen. El copy va VERBATIM desde 04-CONTENT-SOURCE.md.
Output: components/program-card.tsx, content/programas.ts, content/taller-seo-con-ia.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-programas/04-CONTENT-SOURCE.md
@.planning/phases/04-programas/04-CONTEXT.md
@.planning/phases/01-fundaciones-y-navegaci-n/01-UI-SPEC.md
@content/quienes-somos.ts
@content/site.ts
@components/beneficios-section.tsx

<interfaces>
<!-- Patrón establecido: content/quienes-somos.ts exporta interfaces tipadas + const.
     El componente de sección usa "use client", useRef + useInView de framer-motion,
     eyebrow con text-[#b8f60d] uppercase tracking, cards rounded-xl bg-[#0d1117]
     border border-white/10, accent solo en eyebrow/CTA. Ver beneficios-section.tsx. -->
Tokens aprendoclub (globals.css):
- Fondo: var(--bg-primary) #0a0f14, superficie card: #0d1117, borde: white/10
- Accent (solo eyebrow + CTA): #b8f60d / var(--accent)
- Secondary brand: #012fd8 / var(--primary)
- Utilities: container-padding, section-spacing; fuente Montserrat (heredada)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capa de contenido content/programas.ts y content/taller-seo-con-ia.ts</name>
  <files>content/programas.ts, content/taller-seo-con-ia.ts</files>
  <action>
Crear content/programas.ts siguiendo el patrón de content/quienes-somos.ts (interfaces TS Payload-ready + consts exportadas, comentarios en español neutro).

Definir interface Program { id; badge; nombre; descripcion; precio; precioNota?; ctaLabel; ctaHref } y exportar `programas: Program[]` con los 3 programas VERBATIM de la sección "HUB /programas" de 04-CONTENT-SOURCE.md (Diplomado → /diplomado; Taller de SEO con IA → /programas/taller-seo-con-ia; Reto 7 días → /reto). Exportar también `hubHero` (eyebrow "PROGRAMAS", título, subtítulo), `hubCtaFinal` (título, texto, botonLabel "Habla con nosotros", botonHref WhatsApp `https://api.whatsapp.com/send?phone=13055728892`) y `homeProgramas` (eyebrow "NUESTROS PROGRAMAS", título, subtítulo, botonLabel "Ver todos los programas", botonHref "/programas") — todo VERBATIM de las secciones HUB y HOME de 04-CONTENT-SOURCE.md.

Crear content/taller-seo-con-ia.ts con los datos VERBATIM de la sección "TALLER SEO CON IA": hero (eyebrow, titulo H1, subtitulo, duracion), array `incluye` (4 items con texto y valor cuando aplique: 16 módulos $200, certificado, +10 recursos $100, acceso 15 días), `paraQuien`, `precio` ($49.99) con opciones de pago (tarjeta / 3 cuotas Cashea solo Venezuela), y `cta` (label "Quiero el taller", href checkout). Para el href del checkout: usar `https://aprendoseo.com/curso-seo-con-ia` como fallback y agregar comentario `// TODO Juan: confirmar URL real de checkout del taller`.

Regla dura: datos de programa (precios, módulos, valores) fieles; ningún em-dash ni en-dash (usar guion normal o reescribir); español neutro sin voceo.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "programas|taller-seo" || echo "no type errors in new content files"</automated>
  </verify>
  <done>Ambos archivos existen, tipados, exportan las consts esperadas, copy verbatim de 04-CONTENT-SOURCE.md, sin em/en dashes.</done>
</task>

<task type="auto">
  <name>Task 2: Componente ProgramCard reutilizable</name>
  <files>components/program-card.tsx</files>
  <action>
Crear components/program-card.tsx: componente presentacional (server component, sin "use client") que recibe `program: Program` (importar el tipo desde @/content/programas) y renderiza la card en look aprendoclub: contenedor `rounded-xl bg-[#0d1117] border border-white/10 p-8 flex flex-col gap-4` con hover `hover:-translate-y-1 hover:border-[#b8f60d]/30 transition-all duration-300`; badge como pill (`text-xs font-semibold uppercase tracking-[0.15em] text-[#b8f60d]`); nombre `text-lg font-semibold text-white`; descripción `text-sm leading-relaxed text-gray-400`; precio `text-white font-semibold` con precioNota opcional en `text-gray-500 text-xs`; CTA como `<Link>` de next/link con estilo botón accent (`bg-[#b8f60d] text-black rounded-lg px-5 py-2.5 text-sm font-semibold`) para el CTA primario o variante outline según prop opcional `variant`. Aceptar prop opcional `compact?: boolean` para la versión del home. Accent SOLO en badge y CTA (regla 01-UI-SPEC).
  </action>
  <verify>
    <automated>test -f components/program-card.tsx && grep -q "content/programas" components/program-card.tsx && echo OK</automated>
  </verify>
  <done>ProgramCard existe, importa el tipo Program, usa tokens aprendoclub, accent limitado a badge+CTA, sin dependencias nuevas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → enlaces externos (WhatsApp, checkout) | salida a dominios de terceros |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | enlaces externos en CTA | mitigate | enlaces externos con `target="_blank" rel="noopener noreferrer"` (aplicado por las páginas consumidoras) |
| T-04-02 | Information Disclosure | contenido estático | accept | sin PII, sin input de usuario, solo datos públicos de marketing |
| T-04-SC | Tampering | instalación de paquetes | accept | esta fase NO instala paquetes (solo deps existentes: framer-motion, lucide, next) |
</threat_model>

<verification>
- `npx tsc --noEmit` no introduce errores nuevos en los archivos creados.
- content/programas.ts exporta programas (3), hubHero, hubCtaFinal, homeProgramas.
- content/taller-seo-con-ia.ts exporta hero/incluye/precio/paraQuien/cta.
- Sin em-dashes ni en-dashes: `grep -nP "[\x{2013}\x{2014}]" content/programas.ts content/taller-seo-con-ia.ts components/program-card.tsx` no devuelve nada.
</verification>

<success_criteria>
Capa de contenido de programas y card lista para consumo por hub (Plan 03), taller (Plan 04) y home (Plan 08). Copy verbatim, tipado, tokens aprendoclub.
</success_criteria>

<output>
Crear `.planning/phases/04-programas/04-01-SUMMARY.md` al terminar.
</output>
