---
phase: 04-programas
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(site)/diplomado/page.tsx
  - app/diplomado/page.tsx
  - components/diplomado/navbar.tsx
  - components/diplomado/footer.tsx
  - components/diplomado/hero.tsx
  - components/diplomado/origin.tsx
  - components/diplomado/audience.tsx
  - components/diplomado/methodology.tsx
  - components/diplomado/curriculum.tsx
  - components/diplomado/how-it-works.tsx
autonomous: true
requirements: [PROG-02, PROG-08]
must_haves:
  truths:
    - "El Diplomado vive en /diplomado bajo el shell (site) y usa la navbar/footer compartidos"
    - "Las secciones hero, origin, audience, methodology, curriculum, how-it-works usan tokens aprendoclub"
    - "No queda el navbar/footer propios del diplomado"
    - "Se conserva todo el contenido y los ids de ancla (#metodologia, #programa, #equipo, #faq) y el checkout externo"
  artifacts:
    - path: "app/(site)/diplomado/page.tsx"
      provides: "Página del Diplomado bajo el shell (site), sin navbar/footer propios"
      contains: "Hero"
  key_links:
    - from: "app/(site)/diplomado/page.tsx"
      to: "components/diplomado/*"
      via: "imports de secciones (sin Navbar/Footer)"
      pattern: "components/diplomado"
---

<objective>
Mover el Diplomado bajo el shell (site) y reskin de la primera mitad de sus secciones (hero, origin, audience, methodology, curriculum, how-it-works) de tokens shadcn a tokens aprendoclub. Eliminar su navbar/footer propios. Cubre PROG-02 y la mitad de PROG-08.

Purpose: Que un visitante no note diferencia de diseño entre /diplomado y el resto del sitio (Success Criteria 4). Se conserva TODO el contenido.
Output: /diplomado bajo (site) + 6 secciones reskineadas.

NOTA: Esto supersede la nota de 01-UI-SPEC ("/diplomado no entra en (site)"); PROG-08 decide integrarlo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-programas/04-CONTEXT.md
@.planning/phases/01-fundaciones-y-navegaci-n/01-UI-SPEC.md
@app/diplomado/page.tsx
@components/diplomado/hero.tsx
@components/diplomado/navbar.tsx
@app/(site)/layout.tsx
@components/beneficios-section.tsx

<interfaces>
<!-- MAPEO DE TOKENS shadcn → aprendoclub (aplicar en TODAS las secciones): -->
<!--
  bg-background            → bg-[var(--bg-primary)]           (#0a0f14)
  bg-secondary / bg-card   → bg-[#0d1117] (superficie card)   ó bg-[var(--bg-secondary)] (#111827)
  bg-primary (botón CTA)   → bg-[#b8f60d] text-black          (CTA accent, convención del site)
  text-primary-foreground  → text-black                       (texto sobre accent)
  bg-accent / text-accent  → bg-[#b8f60d] / text-[#b8f60d]
  #b8ff2b                  → #b8f60d
  #4338f5                  → #012fd8                           (glow/secundario)
  text-foreground          → text-white
  text-muted-foreground    → text-gray-400
  border-border            → border-white/10
  font-mono                → (quitar; heredar Montserrat/font-sans) usar font-bold en headings
  max-w-5xl                → max-w-6xl + container-padding
  padding de sección (px-4 py-20/py-32) → container-padding + section-spacing (o py existentes si encaja mejor)
-->
<!-- REGLAS:
  - Accent #b8f60d SOLO en eyebrows/badges y botones CTA (regla 01-UI-SPEC).
  - Conservar TODO el texto de contenido; solo cambian clases/tokens.
  - Conservar el checkout externo https://diplomado.aprendoseo.com/offers/Z2hKbUch/checkout
    (es el checkout real de inscripción, un hostname funcional, NO texto de marca visible).
  - No queda texto de marca VISIBLE que diga "aprendoseo".
  - Si se añaden animaciones framer-motion, guardar con useReducedMotion(); si se
    mantienen transiciones CSS existentes, no hace falta motion guard.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mover /diplomado al shell (site) y eliminar navbar/footer propios</name>
  <files>app/(site)/diplomado/page.tsx, app/diplomado/page.tsx, components/diplomado/navbar.tsx, components/diplomado/footer.tsx</files>
  <action>
Mover la página a app/(site)/diplomado/page.tsx (la URL /diplomado no cambia; los route groups no afectan la URL). Eliminar app/diplomado/ (la carpeta vieja) para que no haya ruta duplicada.

En la nueva page.tsx: quitar los imports y el render de `Navbar` y `Footer` de components/diplomado (los provee el layout (site)). Quitar `className="min-h-screen bg-background text-foreground"` del `<main>` (el shell ya monta el `<main>`; usar un fragment o un div con `bg-[var(--bg-primary)] text-white`). Conservar los wrappers con ids `#metodologia`, `#programa`, `#equipo`, `#faq` y el orden de secciones. IMPORTANTE: la navbar del shell usa ancla `#faq` etc. — conservar esos ids.

Borrar components/diplomado/navbar.tsx y components/diplomado/footer.tsx (ya no se usan). Verificar que ningún otro archivo los importe.
  </action>
  <verify>
    <automated>test -f "app/(site)/diplomado/page.tsx" && test ! -e "app/diplomado/page.tsx" && test ! -e components/diplomado/navbar.tsx && test ! -e components/diplomado/footer.tsx && ! grep -rq "diplomado/navbar\|diplomado/footer" app components && echo OK</automated>
  </verify>
  <done>/diplomado sirve desde (site) con navbar/footer compartidos; navbar/footer propios eliminados y sin imports colgantes; ids de ancla conservados.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin secciones hero, origin, audience, methodology</name>
  <files>components/diplomado/hero.tsx, components/diplomado/origin.tsx, components/diplomado/audience.tsx, components/diplomado/methodology.tsx</files>
  <action>
Aplicar el MAPEO DE TOKENS (ver interfaces) a las 4 secciones. Sin tocar textos. En hero.tsx: los glows `bg-[#4338f5]/10` → `bg-[#012fd8]/10`, `bg-[#b8ff2b]/5` → `bg-[#b8f60d]/5`; badge `bg-secondary border-border` → `bg-[#0d1117] border-white/10`; `bg-accent` del punto → `bg-[#b8f60d]`; h1 `font-mono` → font-bold Montserrat; `text-accent` → `text-[#b8f60d]`; botón `bg-primary text-primary-foreground` → `bg-[#b8f60d] text-black`; `max-w-5xl` → `max-w-6xl` + container-padding. Aplicar el mismo criterio a origin, audience, methodology. Conservar checkout externo con rel="noopener noreferrer". Accent solo en eyebrow/badge/CTA.
  </action>
  <verify>
    <automated>cd components/diplomado && ! grep -lq "bg-background\|text-muted-foreground\|border-border\|#b8ff2b\|#4338f5\|font-mono\|max-w-5xl\|primary-foreground" hero.tsx origin.tsx audience.tsx methodology.tsx && echo OK</automated>
  </verify>
  <done>Las 4 secciones usan tokens aprendoclub; sin tokens shadcn residuales; textos intactos; accent limitado.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin secciones curriculum, how-it-works</name>
  <files>components/diplomado/curriculum.tsx, components/diplomado/how-it-works.tsx</files>
  <action>
Aplicar el MAPEO DE TOKENS a curriculum.tsx (es "use client") y how-it-works.tsx. Sin tocar textos ni la estructura de las 16 semanas del currículo. Conservar el checkout externo en how-it-works con rel="noopener noreferrer". Si curriculum usa alguna animación/estado que anime, y se introduce framer-motion, guardar con useReducedMotion(); si mantiene transiciones CSS, dejar como está. Accent #b8f60d solo en eyebrows/CTA.
  </action>
  <verify>
    <automated>cd components/diplomado && ! grep -lq "bg-background\|text-muted-foreground\|border-border\|#b8ff2b\|#4338f5\|font-mono\|max-w-5xl\|primary-foreground" curriculum.tsx how-it-works.tsx && echo OK</automated>
  </verify>
  <done>curriculum y how-it-works reskineados a tokens aprendoclub; 16 semanas intactas; checkout conservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → checkout externo del diplomado | CTAs de inscripción salen a diplomado.aprendoseo.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | CTA checkout inscripción | mitigate | conservar `rel="noopener noreferrer" target="_blank"` |
| T-04-13 | Information Disclosure | página estática | accept | sin input ni PII |
| T-04-SC | Tampering | instalación de paquetes | accept | no se instalan paquetes |
</threat_model>

<verification>
- /diplomado sirve desde (site) con el navbar/footer compartidos (no doble chrome).
- navbar.tsx y footer.tsx del diplomado eliminados, sin imports colgantes.
- hero, origin, audience, methodology, curriculum, how-it-works sin tokens shadcn residuales.
- ids de ancla #metodologia, #programa, #equipo, #faq conservados.
- Ningún texto de marca visible dice "aprendoseo" (solo el hostname del checkout).
- Sin em/en dashes introducidos.
</verification>

<success_criteria>
Mitad de PROG-08: /diplomado bajo el shell (site) con navbar/footer compartidos y 6 secciones en tokens aprendoclub. PROG-02: el Diplomado sigue accesible en /diplomado.
</success_criteria>

<output>
Crear `.planning/phases/04-programas/04-06-SUMMARY.md` al terminar.
</output>
