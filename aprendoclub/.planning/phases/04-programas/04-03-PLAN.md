---
phase: 04-programas
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/(site)/programas/page.tsx
autonomous: true
requirements: [PROG-01]
must_haves:
  truths:
    - "Un visitante puede abrir /programas y ver los 3 programas presentados con badge, precio y CTA"
    - "Cada card enlaza a su página (/diplomado, /programas/taller-seo-con-ia, /reto)"
    - "El hub cierra con un CTA a WhatsApp"
  artifacts:
    - path: "app/(site)/programas/page.tsx"
      provides: "Hub real de programas (reemplaza el placeholder)"
      contains: "ProgramCard"
  key_links:
    - from: "app/(site)/programas/page.tsx"
      to: "content/programas.ts"
      via: "import de programas + hubHero + hubCtaFinal"
      pattern: "from.*content/programas"
    - from: "app/(site)/programas/page.tsx"
      to: "components/program-card.tsx"
      via: "render de cada Program"
      pattern: "ProgramCard"
---

<objective>
Reemplazar el placeholder de app/(site)/programas/page.tsx por el hub real: hero + grid de 3 ProgramCard + CTA final a WhatsApp. Cubre PROG-01 y Success Criteria 1 de la fase.

Purpose: Página de entrada a la oferta educativa, data-driven, bajo el shell (site).
Output: Hub /programas funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-programas/04-CONTENT-SOURCE.md
@.planning/phases/01-fundaciones-y-navegaci-n/01-UI-SPEC.md
@components/beneficios-section.tsx
@app/(site)/programas/page.tsx

<interfaces>
<!-- Consumir de Plan 04-01: content/programas.ts (programas, hubHero, hubCtaFinal)
     y components/program-card.tsx (<ProgramCard program={...} />).
     La página está bajo (site) → NO montar navbar/footer (los da el layout).
     Patrón de sección: ver beneficios-section.tsx. -->
Tokens: fondo var(--bg-primary), eyebrow text-[#b8f60d] uppercase tracking-[0.2em],
container-padding + section-spacing, max-w-6xl mx-auto. Accent solo eyebrow+CTA.
La primera sección debe respetar la navbar fija: usar padding superior (pt-28 o scroll-mt) como el placeholder actual.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hub /programas (hero + grid + CTA final)</name>
  <files>app/(site)/programas/page.tsx</files>
  <action>
Reescribir app/(site)/programas/page.tsx. Mantener el export de `metadata` (title/description ya presentes, ajustar description si aplica). Estructura:
1. Sección hero: eyebrow `hubHero.eyebrow`, h1 `hubHero.titulo` (text-3xl sm:text-4xl md:text-5xl font-bold text-white text-center), subtítulo `hubHero.subtitulo` (text-lg text-gray-400). Contenedor `section-spacing container-padding max-w-6xl mx-auto pt-28`.
2. Grid de programas: `grid grid-cols-1 md:grid-cols-3 gap-6 max-w-6xl mx-auto`, mapeando `programas` a `<ProgramCard program={p} />`.
3. CTA final: bloque centrado con `hubCtaFinal.titulo`, `hubCtaFinal.texto` y botón accent enlazando `hubCtaFinal.botonHref` (WhatsApp) con `target="_blank" rel="noopener noreferrer"`.

Decidir si la página es server component (preferible; ProgramCard es server, no hay estado). Si se usa animación de entrada, extraer a un client component pequeño y respetar useReducedMotion; si no, mantener server sin animación. Accent SOLO en eyebrow y botones CTA.
  </action>
  <verify>
    <automated>grep -q "ProgramCard" app/(site)/programas/page.tsx && grep -q "content/programas" app/(site)/programas/page.tsx && npx next build 2>&1 | grep -E "/programas" || echo "check build in Plan 08"</automated>
  </verify>
  <done>/programas renderiza hero + 3 cards enlazadas + CTA WhatsApp; sin navbar/footer inline; accent limitado; sin em/en dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → WhatsApp externo | CTA final sale a api.whatsapp.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | CTA WhatsApp | mitigate | `rel="noopener noreferrer" target="_blank"` en el enlace externo |
| T-04-06 | Information Disclosure | página estática | accept | sin input ni PII |
| T-04-SC | Tampering | instalación de paquetes | accept | no se instalan paquetes |
</threat_model>

<verification>
- /programas usa ProgramCard y content/programas.ts.
- No monta Navbar/Footer inline.
- Enlaces externos con rel="noopener noreferrer".
- Sin em/en dashes en el archivo.
</verification>

<success_criteria>
Success Criteria 1 de la fase: un visitante abre /programas y ve los 3 programas presentados y enlazados.
</success_criteria>

<output>
Crear `.planning/phases/04-programas/04-03-SUMMARY.md` al terminar.
</output>
