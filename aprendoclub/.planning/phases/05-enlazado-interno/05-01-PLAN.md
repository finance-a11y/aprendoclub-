---
phase: 05-enlazado-interno
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/related-links.tsx
  - app/(site)/diplomado/page.tsx
  - app/(site)/programas/taller-seo-con-ia/page.tsx
  - app/(site)/reto/page.tsx
  - app/(site)/programas/page.tsx
  - components/quienes-somos/cta.tsx
  - components/testimonios/cta.tsx
autonomous: true
requirements: [LINK-01]

must_haves:
  truths:
    - "Cada página del sitio enlaza contextualmente (en el cuerpo, no solo navbar/footer) a al menos otras 2 páginas del sitio"
    - "Ningún anchor text usa 'clic aquí' ni 'aquí'; todos son descriptivos"
    - "Las páginas de programa (diplomado, taller, reto) enlazan de vuelta a /programas y a /testimonios"
    - "/quienes-somos enlaza a /testimonios y /testimonios enlaza a /quienes-somos"
    - "El accent (#b8f60d) sigue reservado a CTAs de conversión; los enlaces cruzados son inline subrayados"
    - "npm run build pasa sin errores"
  artifacts:
    - path: "components/related-links.tsx"
      provides: "Componente presentacional reutilizable para tiras de enlaces internos"
      contains: "export function RelatedLinks"
    - path: "app/(site)/diplomado/page.tsx"
      provides: "Enlaces de retorno a /programas y /testimonios"
      contains: "RelatedLinks"
    - path: "app/(site)/reto/page.tsx"
      provides: "Enlaces de retorno a /programas y /testimonios"
      contains: "RelatedLinks"
    - path: "app/(site)/programas/taller-seo-con-ia/page.tsx"
      provides: "Enlaces de retorno a /programas y /testimonios"
      contains: "RelatedLinks"
  key_links:
    - from: "app/(site)/diplomado/page.tsx"
      to: "/programas, /testimonios"
      via: "RelatedLinks"
      pattern: "RelatedLinks"
    - from: "components/quienes-somos/cta.tsx"
      to: "/testimonios"
      via: "Link inline"
      pattern: "href=\"/testimonios\""
    - from: "components/testimonios/cta.tsx"
      to: "/quienes-somos"
      via: "Link inline"
      pattern: "href=\"/quienes-somos\""
---

<objective>
Tejer enlaces internos cruzados y contextuales entre todas las páginas del sitio de aprendoclub, de modo que ninguna quede como callejón sin salida y todas tengan al menos 2 enlaces descriptivos a otras páginas del sitio dentro del cuerpo (además del navbar/footer globales). Cubre LINK-01.

Purpose: SEO interno (flujo de PageRank, contexto por anchor text descriptivo) y mejor navegación/conversión (prueba social y confianza accesibles desde las páginas de programa).
Output: Un componente reutilizable de enlaces relacionados + enlaces cruzados insertados en las páginas que hoy no cumplen el mínimo de 2 enlaces contextuales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-enlazado-interno/05-CONTEXT.md

<audit>
Auditoría de enlaces contextuales existentes (en el cuerpo, excluyendo navbar/footer que ya enlazan globalmente a todo). Verificado con grep sobre app/ + components/ + content/ el 2026-07-04:

| Página | Enlaces internos contextuales actuales | ¿Cumple 2+? | Acción |
|--------|----------------------------------------|-------------|--------|
| `/` (home) | instructor-section → /quienes-somos; testimonios-section → /testimonios; programas-section → /programas | Sí (3) | Ninguna |
| `/programas` (hub) | 3 program-cards → /diplomado, /programas/taller-seo-con-ia, /reto | Sí (3) | Agregar 1 enlace a /testimonios (refuerzo de conversión, por decisión de CONTEXT) |
| `/quienes-somos` | cta → /programas | No (1) | Agregar enlace a /testimonios |
| `/testimonios` | cta → /programas | No (1) | Agregar enlace a /quienes-somos |
| `/diplomado` | ninguno (CTA a checkout externo) | No (0) | Agregar → /programas y → /testimonios |
| `/programas/taller-seo-con-ia` | ninguno (CTA a aprendoseo.com) | No (0) | Agregar → /programas y → /testimonios |
| `/reto` | ninguno (CTAs a WhatsApp) | No (0) | Agregar → /programas y → /testimonios |

Nota: hubCtaFinal.botonHref = "/programas" es un self-link con target="_blank"; NO cuenta como enlace cruzado. No es objetivo de esta fase corregirlo.
</audit>

<interfaces>
Estilos y patrones a reutilizar (extraídos del código):

- Accent del design system: `#b8f60d` (= `var(--accent)`). RESERVADO a botones CTA de conversión. Los enlaces cruzados NO deben usar fondo accent ni verse como botón.
- Contenedores de sección estándar: `container-padding section-spacing max-w-6xl mx-auto w-full`.
- CTAs primarios existentes usan `bg-[var(--accent)] text-black px-6 py-3 rounded-lg font-semibold` (quienes-somos/cta, testimonios/cta) — NO tocar, son el CTA de conversión.
- Componente Link de Next: `import Link from "next/link"`.
- quienes-somos/cta.tsx y testimonios/cta.tsx son "use client" con framer-motion; los enlaces nuevos van como `<Link>` inline dentro del bloque `motion.div` existente, debajo del CTA primario.
- diplomado/page.tsx, reto/page.tsx y taller-seo-con-ia/page.tsx son Server Components (sin "use client"); pueden importar y renderizar un componente presentacional sin hooks.

Estilo de enlace inline cruzado a usar (subrayado, accent solo en hover):
`text-gray-300 underline underline-offset-4 decoration-white/30 transition-colors hover:text-[#b8f60d]`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear componente RelatedLinks e insertarlo en las páginas de programa y el hub</name>
  <files>components/related-links.tsx, app/(site)/diplomado/page.tsx, app/(site)/programas/taller-seo-con-ia/page.tsx, app/(site)/reto/page.tsx, app/(site)/programas/page.tsx</files>
  <action>
Crear `components/related-links.tsx` como Server Component presentacional (sin "use client", sin hooks). Exporta `RelatedLinks` con props `{ title?: string; links: { href: string; label: string }[] }`. Usa `Link` de "next/link". Render: una `<section>` con `container-padding section-spacing max-w-6xl mx-auto w-full`, dentro un contenedor centrado (`flex flex-col items-center gap-3 text-center`) con un eyebrow `<span>` en `text-xs font-semibold uppercase tracking-[0.2em] text-gray-500` mostrando `title` (default "Sigue explorando"), y un `<div className="flex flex-wrap items-center justify-center gap-x-6 gap-y-2">` que mapea `links` a `<Link>` con el estilo inline definido en <interfaces> (`text-gray-300 underline underline-offset-4 decoration-white/30 transition-colors hover:text-[#b8f60d]`). NO usar fondo accent (per decisión: accent reservado a CTAs).

Insertar `<RelatedLinks>` cerca del cierre de cada página, DESPUÉS de la sección CTA final existente:
- `app/(site)/diplomado/page.tsx`: importar y renderizar tras `<CTA />`, con links `[{ href: "/programas", label: "Ver todos los programas" }, { href: "/testimonios", label: "Lee las historias de nuestros estudiantes" }]`.
- `app/(site)/reto/page.tsx`: importar y renderizar tras `<RetoBottom />`, mismos dos links.
- `app/(site)/programas/taller-seo-con-ia/page.tsx`: importar y renderizar antes de cerrar el fragmento (`</>`), tras la sección de precio, mismos dos links.
- `app/(site)/programas/page.tsx` (hub): importar y renderizar tras la sección "CTA final", con links `[{ href: "/testimonios", label: "Lee las historias de nuestros estudiantes" }, { href: "/quienes-somos", label: "Conoce al equipo detrás de aprendoclub" }]` y `title="Antes de decidir"`.

Copy humanizada, español neutro, sin em-dashes, sin "clic aquí"/"aquí". Cumple LINK-01.
  </action>
  <verify>
    <automated>cd "/Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub" && grep -q "export function RelatedLinks" components/related-links.tsx && for f in "app/(site)/diplomado/page.tsx" "app/(site)/reto/page.tsx" "app/(site)/programas/taller-seo-con-ia/page.tsx" "app/(site)/programas/page.tsx"; do grep -q "RelatedLinks" "$f" || { echo "FALTA RelatedLinks en $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>RelatedLinks existe, no usa fondo accent, y las 4 páginas (diplomado, reto, taller, hub) lo renderizan con enlaces descriptivos a al menos 2 páginas del sitio.</done>
</task>

<task type="auto">
  <name>Task 2: Agregar enlaces cruzados inline en /quienes-somos y /testimonios</name>
  <files>components/quienes-somos/cta.tsx, components/testimonios/cta.tsx</files>
  <action>
En `components/quienes-somos/cta.tsx`: debajo del `<Link>` CTA primario (que apunta a `ctaFinal.botonHref` = /programas), dentro del mismo `motion.div`, agregar un `<Link href="/testimonios">` con texto descriptivo "Mira las historias de nuestros estudiantes", usando el estilo inline de <interfaces> (subrayado, hover accent). No modificar el CTA primario ni sus estilos.

En `components/testimonios/cta.tsx`: debajo del `<Link>` CTA primario (que apunta a `cta.botonHref` = /programas), dentro del mismo `motion.div`, agregar un `<Link href="/quienes-somos">` con texto "Conoce al equipo detrás de aprendoclub", con el mismo estilo inline. No modificar el CTA primario.

Ambos archivos son "use client" y ya importan `Link` de "next/link"; verificar el import antes de usar. Copy humanizada, español neutro, sin em-dashes, sin "clic aquí". Cumple LINK-01.
  </action>
  <verify>
    <automated>cd "/Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub" && grep -q 'href="/testimonios"' components/quienes-somos/cta.tsx && grep -q 'href="/quienes-somos"' components/testimonios/cta.tsx && echo OK</automated>
  </verify>
  <done>/quienes-somos tiene enlace inline a /testimonios y /testimonios tiene enlace inline a /quienes-somos, ambos con anchor descriptivo, sin tocar el CTA primario.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Esta fase solo agrega enlaces internos estáticos (`<Link href="/...">`) y un componente presentacional. No introduce entrada de usuario, endpoints, dependencias nuevas ni instalaciones de paquetes. No hay nuevas fronteras de confianza.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Enlaces internos con href hardcodeado a rutas del sitio | accept | Solo apuntan a rutas internas conocidas y verificadas por el build; sin datos externos ni untrusted input |
| T-05-02 | Information Disclosure | RelatedLinks (presentacional) | accept | No renderiza datos sensibles; solo labels y rutas estáticas |
</threat_model>

<verification>
Verificación de fase (ejecutar tras completar ambas tasks):

1. Build limpio:
   `cd "/Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub" && npm run build`
   Debe terminar sin errores.

2. Anchor text prohibido (no debe haber resultados):
   `grep -rin "clic aquí\|clic aqui\|haz clic\|\baquí\b\|\baqui\b" components/related-links.tsx components/quienes-somos/cta.tsx components/testimonios/cta.tsx`
   (Cero coincidencias en el texto de enlaces nuevos.)

3. Check anti-huérfanos — cada ruta del sitio debe recibir al menos un enlace contextual entrante (además de navbar/footer). Confirmar manualmente con el mapa: home→(qs,test,prog); qs→(prog,test); test→(prog,qs); prog→(diplo,taller,reto,test,qs); diplomado→(prog,test); taller→(prog,test); reto→(prog,test). Ninguna ruta con 0 enlaces contextuales salientes ni entrantes.
</verification>

<success_criteria>
- Cada una de las 7 páginas del sitio tiene ≥2 enlaces contextuales (en el cuerpo) a otras páginas del sitio.
- Ningún anchor text usa "clic aquí"/"aquí"; todos descriptivos y humanizados en español neutro sin em-dashes.
- El accent #b8f60d sigue reservado a CTAs de conversión; los enlaces cruzados son inline subrayados.
- `npm run build` pasa sin errores.
- Ninguna página queda como callejón sin salida.
</success_criteria>

<output>
Create `.planning/phases/05-enlazado-interno/05-01-SUMMARY.md` when done
</output>
