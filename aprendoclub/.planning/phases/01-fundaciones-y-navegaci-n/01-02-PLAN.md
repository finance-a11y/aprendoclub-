---
phase: 01-fundaciones-y-navegacion
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - components/navbar.tsx
  - components/footer.tsx
autonomous: true
requirements: [NAV-02, NAV-03, NAV-05]
must_haves:
  truths:
    - "El navbar navega a páginas reales (Inicio, Quiénes somos, Programas, Testimonios) en desktop y mobile"
    - "El navbar resalta la ruta activa vía usePathname y conserva glass + framer-motion + menú mobile"
    - "El navbar respeta prefers-reduced-motion"
    - "El footer solo enlaza a páginas/secciones que existen, sin links muertos ni columna Legal"
    - "Navbar y footer consumen content/site.ts (data-driven)"
  artifacts:
    - path: "components/navbar.tsx"
      provides: "Navbar data-driven con rutas reales, ruta activa y reduced-motion"
      contains: "usePathname"
    - path: "components/footer.tsx"
      provides: "Footer limpio consumiendo footerColumns/footerSocials"
      contains: "footerColumns"
  key_links:
    - from: "components/navbar.tsx"
      to: "content/site.ts"
      via: "import siteNav, siteCta"
      pattern: "from \"@/content/site\""
    - from: "components/footer.tsx"
      to: "content/site.ts"
      via: "import footerColumns, footerSocials, footerMeta"
      pattern: "from \"@/content/site\""
---

<objective>
Refactorizar `components/navbar.tsx` y `components/footer.tsx` para que consuman la capa `content/site.ts`, ofrezcan navegación real entre páginas, resalten la ruta activa y respeten prefers-reduced-motion, conservando el look-and-feel glass + framer-motion actual.

Purpose: NAV-02 (navegación real desktop+mobile con estilo glass), NAV-03 (footer limpio sin links muertos), NAV-05 (componentes data-driven).
Output: navbar y footer refactorizados, pixel-consistentes con el home, alimentados por datos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fundaciones-y-navegaci-n/01-UI-SPEC.md
@.planning/phases/01-fundaciones-y-navegaci-n/01-01-SUMMARY.md
@content/site.ts
@components/navbar.tsx
@components/footer.tsx
@app/links/page.tsx

<interfaces>
<!-- content/site.ts (creado en 01-01) — consumir estos exports, NO redefinir datos inline. -->
siteNav: NavItem[]; siteCta: NavItem; footerColumns: FooterColumn[]; footerSocials: SocialLink[]; footerMeta;
type NavItemType = "route" | "anchor";
interface NavItem { label: string; href: string; type: NavItemType; }
interface FooterColumn { title: string; links: { label: string; href: string; external?: boolean }[]; }
interface SocialLink { id: string; label: string; href: string; }

Tokens (globals.css): --bg-primary #0a0f14, --accent #b8f60d, --primary #012fd8. Utilities: container-padding, section-spacing.
SVGs de marca WhatsApp/TikTok: copiar los paths exactos desde app/links/page.tsx (socials[]).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactorizar Navbar a data-driven con ruta activa y reduced-motion</name>
  <files>components/navbar.tsx</files>
  <action>
Refactorizar `components/navbar.tsx` (sigue siendo `"use client"`) consumiendo `siteNav` y `siteCta` de `@/content/site`. Conservar 1:1 el look-and-feel del UI-SPEC (contenedor `motion.nav fixed top-0 h-[72px] z-50`, entrada slide-down `duration:0.5 ease:easeOut`, glass `scrollY>50` → `bg-black/60 backdrop-blur-xl border-b border-white/5`, inner `container-padding max-w-7xl mx-auto`, logo `/logo.svg h-8`, CTA verde, botón hamburguesa, panel mobile slide-in 280px, body scroll lock).

Cambios requeridos:
1. **Links por data:** renderizar los items desktop y mobile mapeando `siteNav`. El CTA (desktop y mobile) usa `siteCta.href` (`/programas`). Items `type: "route"` renderizan con `next/link` `<Link>`; items `type: "anchor"` con `<a href>`. (Hoy todos los items del shell son `route`; soportar ambos por el contrato.)
2. **Ruta activa vía `usePathname()`** (next/navigation): un item de `route` está activo cuando coincide con pathname. Regla de match: `/` exacto para Inicio; `pathname.startsWith("/programas")` para Programas; igualdad exacta para el resto (per tabla UI-SPEC). Renombrar el `layoutId` del underline de `activeSection` a `activeNav`. Punto activo mobile y underline desktop en `#b8f60d`.
3. **Comportamiento dual del IntersectionObserver:** el observer de secciones del home (`#problema`, `#beneficios`, `#precios`, `#faq`) SOLO se activa cuando `pathname === "/"`. Entre páginas el resaltado lo decide `pathname`. Guardar el efecto del observer tras `if (pathname !== "/") return;`.
4. **prefers-reduced-motion (obligatorio):** usar `useReducedMotion()` de framer-motion. Si es `true`: entrada del nav con `initial={false}` (sin slide `y`), items del panel mobile sin offset `x`/`y` ni stagger (`delay:0`), y sin animación de `layout` en el underline (render en estado final). El cambio de glass y el fade del backdrop pueden mantenerse cortos.
5. **Accesibilidad:** añadir `aria-current="page"` al item activo (desktop y mobile). Conservar `aria-label` dinámico del botón hamburguesa. El panel mobile usa `bg-[var(--bg-primary)]` (NO el hardcode `bg-[#0a0a0f]` actual — normalización UI-SPEC). Blurb del panel desde `footerMeta.mobilePanelBlurb`.
6. Cerrar el menú mobile al hacer click en cualquier item/CTA (`setIsMobileMenuOpen(false)`).

No introducir dependencias nuevas. No cambiar la altura, spacing ni la paleta.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub && grep -q "usePathname" components/navbar.tsx && grep -q "useReducedMotion" components/navbar.tsx && grep -q '@/content/site' components/navbar.tsx && grep -q 'aria-current' components/navbar.tsx && echo OK</automated>
  </verify>
  <done>Navbar consume siteNav/siteCta, resalta ruta activa vía usePathname con aria-current, respeta useReducedMotion, activa el observer solo en `/`, usa `bg-[var(--bg-primary)]` en el panel, y conserva glass + framer-motion + menú mobile. CTA → /programas.</done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar Footer limpio y data-driven</name>
  <files>components/footer.tsx</files>
  <action>
Refactorizar `components/footer.tsx` (permanece server component, SIN `"use client"`) consumiendo `footerColumns`, `footerSocials` y `footerMeta` de `@/content/site`. Conservar la estructura/clases del footer actual (contenedor `bg-[var(--bg-primary)] border-t border-white/[0.06]`, grid `container-padding py-16 → grid grid-cols-2 md:grid-cols-4 gap-10 lg:gap-16 max-w-6xl mx-auto`, brand `col-span-2 md:col-span-1`, copyright bar).

Cambios requeridos:
1. **Eliminar los links muertos:** borrar las columnas "Programa" (con Cursos/Precios/Comunidad/Testimonios rotos), "Recursos" (Blog/Guías SEO/Herramientas/FAQ) y "Legal" (Términos/Privacidad/Cookies) del código actual. Renderizar en su lugar las dos columnas de `footerColumns` (Programas, aprendoclub) mapeando sus links. Links de columna: `text-sm text-gray-500 hover:text-white transition-colors`; títulos `text-sm font-semibold text-white`. Links con `external: true` → `target="_blank" rel="noopener noreferrer"`.
2. **Brand blurb:** reemplazar el texto heredado por `footerMeta.blurb`.
3. **Iconos sociales reales:** mapear `footerSocials`. Para YouTube usar lucide `Youtube`; para WhatsApp y TikTok copiar los SVG inline `viewBox="0 0 24 24" fill="currentColor" className="h-5 w-5" aria-hidden` desde `app/links/page.tsx` (lucide no trae TikTok/WhatsApp). Seleccionar el icono por `social.id`. Contenedor `flex items-center gap-3 mt-2`; cada `<a href aria-label={social.label} target="_blank" rel="noopener noreferrer" className="text-gray-500 hover:text-white transition-colors">`, icono `h-5 w-5`. Eliminar los iconos LinkedIn/Instagram con `href="#"`.
4. **Copyright bar:** izquierda `© {new Date().getFullYear()} {footerMeta.copyrightLeft}` (`text-gray-600`), derecha `footerMeta.copyrightRight`.

Español neutro. No dependencias nuevas. No usar `#b8f60d` en links/iconos del footer (reservado, per UI-SPEC).
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub && grep -q '@/content/site' components/footer.tsx && ! grep -q 'use client' components/footer.tsx && ! grep -Eq 'href="#"' components/footer.tsx && echo OK</automated>
  </verify>
  <done>Footer es server component, consume footerColumns/footerSocials/footerMeta, no contiene `href="#"` ni columna Legal, muestra socials reales (YouTube/TikTok/WhatsApp) con rel=noopener, y usa el blurb nuevo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sitio → destinos externos | Enlaces sociales abren dominios externos (youtube/tiktok/whatsapp) en nueva pestaña. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | footer socials `target="_blank"` | mitigate | Todos los enlaces externos llevan `rel="noopener noreferrer"` (evita reverse tabnabbing). Verificado por grep de ausencia de `href="#"` y presencia de rel en externos. |
| T-01-03 | Information Disclosure | navbar/footer | accept | Solo datos públicos de marketing; sin PII ni secretos. |
| T-01-SC | Tampering | dependencias | accept | Sin instalación de paquetes (constraint: sin deps nuevas). |
</threat_model>

<verification>
- Navbar: `usePathname`, `useReducedMotion`, import de `@/content/site`, `aria-current`, panel con `bg-[var(--bg-primary)]`.
- Footer: server component, sin `href="#"`, socials con `rel="noopener noreferrer"`.
- Ambos importan de `@/content/site` (data-driven).
- `npm run build` se valida al final del plan 01-03.
</verification>

<success_criteria>
- NAV-02: navegación real entre páginas en desktop y mobile con estilo glass y ruta activa.
- NAV-03: footer sin links muertos, solo enlaces a rutas/secciones existentes (o declaradas para Fase 4).
- NAV-05: navbar y footer consumen la capa de contenido.
</success_criteria>

<output>
Create `.planning/phases/01-fundaciones-y-navegaci-n/01-02-SUMMARY.md` when done.
</output>
