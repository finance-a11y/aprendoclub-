---
phase: 01-fundaciones-y-navegacion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/site.ts
  - app/sitemap.ts
autonomous: true
requirements: [NAV-05, NAV-04]
must_haves:
  truths:
    - "El contenido de nav y footer vive en objetos tipados separados de la presentación"
    - "Las interfaces TypeScript espejan la forma de un futuro bloque Payload"
    - "El sitemap lista las rutas resolubles del sitio, incluyendo las páginas nuevas"
  artifacts:
    - path: "content/site.ts"
      provides: "Capa de contenido data-driven: siteNav, siteCta, footerColumns, footerSocials, footerMeta + interfaces"
      contains: "export const siteNav"
    - path: "app/sitemap.ts"
      provides: "Sitemap con rutas nuevas"
      contains: "quienes-somos"
  key_links:
    - from: "content/site.ts"
      to: "consumidores (navbar/footer en 01-02)"
      via: "named exports tipados"
      pattern: "export (const|interface|type)"
---

<objective>
Crear la capa de contenido data-driven Payload-ready (`content/site.ts`) que centraliza los datos de nav + footer, y actualizar el sitemap con las rutas del sitio. Esta es la base que consumen navbar y footer en el plan 01-02.

Purpose: NAV-05 exige separar contenido de presentación en objetos/consts tipados para que la migración a bloques Payload en v1.1 sea 1:1. NAV-04 exige un sitemap con las rutas nuevas.
Output: `content/site.ts` con interfaces + datos exportados; `app/sitemap.ts` extendido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-fundaciones-y-navegaci-n/01-CONTEXT.md
@.planning/phases/01-fundaciones-y-navegaci-n/01-UI-SPEC.md

<interfaces>
<!-- Forma prescrita por 01-UI-SPEC.md "Data-Driven Pattern". Nombres finales a discreción, respetar convención existente (alias @/, sin deps nuevas). -->
<!-- El executor DEFINE estas interfaces en content/site.ts; downstream (01-02) las consume tal cual. -->

type NavItemType = "route" | "anchor";
interface NavItem { label: string; href: string; type: NavItemType; }
interface FooterColumn { title: string; links: { label: string; href: string; external?: boolean }[]; }
interface SocialLink { id: string; label: string; href: string; }

Exports esperados:
- siteNav: NavItem[]           -> Inicio(/), Quiénes somos(/quienes-somos), Programas(/programas), Testimonios(/testimonios) — todos type "route"
- siteCta: NavItem             -> { label: "Únete ahora", href: "/programas", type: "route" }
- footerColumns: FooterColumn[] -> col "Programas" y col "aprendoclub"
- footerSocials: SocialLink[]   -> youtube, tiktok, whatsapp (URLs reales)
- footerMeta                    -> { blurb, copyrightLeft, copyrightRight, mobilePanelBlurb }

URLs sociales reales (de app/links/page.tsx):
- YouTube  https://www.youtube.com/@aprendoclub
- TikTok   https://tiktok.com/@aprendo.club
- WhatsApp https://api.whatsapp.com/send?phone=13055728892
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear content/site.ts (interfaces + datos nav/footer)</name>
  <files>content/site.ts</files>
  <action>
Crear el directorio `content/` y el módulo `content/site.ts`. Definir y exportar las interfaces `NavItem`, `FooterColumn`, `SocialLink` (y el type `NavItemType`) exactamente con la forma del bloque `<interfaces>` — están pensadas para mapear 1:1 a colecciones/bloques Payload en v1.1, así que documentar cada interface con un comentario breve indicándolo (NAV-05, decisión CONTEXT "Patrón data-driven").

Exportar los datos:
- `siteNav: NavItem[]` con: Inicio → `/`, Quiénes somos → `/quienes-somos`, Programas → `/programas`, Testimonios → `/testimonios`, todos `type: "route"`.
- `siteCta: NavItem` = `{ label: "Únete ahora", href: "/programas", type: "route" }` (CTA apunta al hub, no a `#precios`, per decisión CONTEXT).
- `footerColumns: FooterColumn[]` con dos columnas:
  - "Programas": Diplomado → `/diplomado`; Taller SEO con IA → `/programas/taller-seo-con-ia`; Reto 7 días → `/reto`; Econía / SEOconía → `/programas/econia`. (Los tres últimos targets se resuelven en Fase 4; en Fase 1 son links declarados — dejar comentario `// resuelve Fase 4` en esas entradas.)
  - "aprendoclub": Quiénes somos → `/quienes-somos`; Testimonios → `/testimonios`.
- `footerSocials: SocialLink[]` con `id` youtube/tiktok/whatsapp, `label` y `href` reales (ver `<interfaces>`). NO incluir Instagram ni LinkedIn (no existen en /links; per UI-SPEC se omiten). Los SVGs de marca (TikTok/WhatsApp) NO van en este módulo de datos: el footer los mapea por `id`. Sí se puede usar lucide `Youtube` para YouTube desde el componente.
- `footerMeta` = objeto con `blurb: "Academia de SEO + IA. Formación, comunidad y acompañamiento para especialistas."`, `copyrightLeft` (plantilla con año dinámico se arma en el componente — aquí guardar solo el texto base `"aprendoclub. Todos los derechos reservados."`), `copyrightRight: "Hecho con 💚 para la comunidad SEO"`, `mobilePanelBlurb: "Membresía profesional SEO + IA"`.

Español neutro, sin voceo. No instalar dependencias. Usar solo tipos TS (sin JSX en este archivo).
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "content/site" || echo "no type errors in content/site.ts"</automated>
  </verify>
  <done>`content/site.ts` existe, exporta las interfaces y los 5 objetos de datos, compila sin errores de tipo, y no contiene JSX ni URLs `#` muertas.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar app/sitemap.ts con las rutas nuevas</name>
  <files>app/sitemap.ts</files>
  <action>
Extender `sitemap()` para incluir todas las rutas resolubles del sitio además de home y `/links`:
- `https://aprendoclub.com/diplomado` (ruta existente, hoy faltante en el sitemap)
- `https://aprendoclub.com/quienes-somos`
- `https://aprendoclub.com/programas`
- `https://aprendoclub.com/testimonios`

Usar `changeFrequency: "monthly"` y `priority: 0.8` para las páginas de contenido; mantener home en `priority: 1`. NO agregar aún las sub-rutas que 404 en Fase 1 (`/programas/taller-seo-con-ia`, `/programas/econia`, `/reto`): se añaden en Fase 4 cuando resuelvan, para no publicar URLs rotas en el sitemap (decisión de higiene SEO — dejar comentario `// TODO Fase 4: taller-seo-con-ia, econia, reto`). NAV-04.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub && grep -c -E "quienes-somos|/programas|/testimonios|/diplomado" app/sitemap.ts</automated>
  </verify>
  <done>`app/sitemap.ts` lista home, /links, /diplomado, /quienes-somos, /programas, /testimonios; el grep devuelve ≥ 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguno relevante | Módulo de datos estático + sitemap generado en build. Sin entrada de usuario, sin fetch, sin backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | content/site.ts | accept | Solo datos públicos de marketing (nav labels, URLs públicas). Sin secretos ni PII. |
| T-01-SC | Tampering | dependencias | accept | Este plan NO instala paquetes (constraint: sin deps nuevas). Sin superficie de supply-chain. |
</threat_model>

<verification>
- `content/site.ts` compila (`npx tsc --noEmit`) y exporta interfaces + datos.
- `app/sitemap.ts` incluye las rutas nuevas.
- Ningún `href: "#"` muerto en los datos del footer.
</verification>

<success_criteria>
- NAV-05: existe una capa de contenido tipada, Payload-ready, con nav/footer separados de la presentación.
- NAV-04: el sitemap lista las rutas resolubles nuevas del sitio.
</success_criteria>

<output>
Create `.planning/phases/01-fundaciones-y-navegaci-n/01-01-SUMMARY.md` when done.
</output>
