---
phase: 01-fundaciones-y-navegacion
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - app/(site)/layout.tsx
  - app/(site)/page.tsx
  - app/page.tsx
  - app/(site)/quienes-somos/page.tsx
  - app/(site)/programas/page.tsx
  - app/(site)/testimonios/page.tsx
autonomous: true
requirements: [NAV-01, NAV-02]
must_haves:
  truths:
    - "Cualquier página nueva se monta sobre un shell compartido con el mismo navbar y footer del home"
    - "El home se renderiza bajo el shell sin montar Navbar/Footer inline (sin doble chrome)"
    - "/quienes-somos, /programas y /testimonios resuelven bajo el shell (placeholders)"
    - "/diplomado y public/reto conservan su chrome propio (fuera del route group)"
    - "npm run build pasa"
  artifacts:
    - path: "app/(site)/layout.tsx"
      provides: "Route-group layout con skip-link + Navbar + main#main + Footer"
      contains: "id=\"main\""
    - path: "app/(site)/page.tsx"
      provides: "Home movido bajo el shell, sin Navbar/Footer inline"
      contains: "HeroSection"
    - path: "app/(site)/quienes-somos/page.tsx"
      provides: "Placeholder page bajo el shell"
      contains: "Quiénes somos"
  key_links:
    - from: "app/(site)/layout.tsx"
      to: "components/navbar.tsx + components/footer.tsx"
      via: "import y montaje del shell"
      pattern: "Navbar|Footer"
    - from: "app/(site)/page.tsx"
      to: "app/(site)/layout.tsx"
      via: "route group children"
      pattern: "export default"
---

<objective>
Crear el route group `(site)` con su `layout.tsx` que monta el shell compartido (skip-link + Navbar + `<main id="main">` + Footer), mover el home bajo ese layout eliminando el chrome inline, y crear las páginas placeholder (`/quienes-somos`, `/programas`, `/testimonios`) para que la navegación resuelva. Verificar `npm run build`.

Purpose: NAV-01 (shell compartido para todas las páginas nuevas), NAV-02 (los links del navbar resuelven a rutas reales).
Output: layout de grupo, home migrado sin doble chrome, tres placeholders, build verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fundaciones-y-navegaci-n/01-UI-SPEC.md
@.planning/phases/01-fundaciones-y-navegaci-n/01-02-SUMMARY.md
@app/page.tsx
@app/layout.tsx
@components/navbar.tsx
@components/footer.tsx

<interfaces>
<!-- Estructura del layout de grupo (UI-SPEC "Route-group (site)/layout.tsx"): -->
<!-- <a skip-link href="#main" .../> → <Navbar/> → <main id="main" className="flex min-h-screen w-full flex-col">{children}</main> → <Footer/> -->
<!-- Navbar es client; Footer es server; el layout es server component (puede montar ambos). -->
<!-- StickyCTAMobile es específico del home → se queda en app/(site)/page.tsx, NO en el layout. -->
Skip-link (conservar clases): sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:rounded-md focus:bg-[var(--primary)] focus:px-4 focus:py-2 focus:text-white
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear (site)/layout.tsx y mover el home bajo el shell</name>
  <files>app/(site)/layout.tsx, app/(site)/page.tsx, app/page.tsx</files>
  <action>
1. **Crear `app/(site)/layout.tsx`** (server component, sin `"use client"`). Importa `Navbar` de `@/components/navbar` y `Footer` de `@/components/footer`. Renderiza, en orden: el skip-link (clases exactas del bloque `<interfaces>`, `href="#main"`), `<Navbar />`, `<main id="main" className="flex min-h-screen w-full flex-col">{children}</main>`, `<Footer />`. Es el shell que compartirán home + páginas nuevas (NAV-01). No definir un nuevo `metadata` global (el root `app/layout.tsx` ya lo maneja); opcionalmente exportar metadata parcial si hace falta, pero no duplicar html/body.

2. **Mover el home:** trasladar el contenido de `app/page.tsx` a `app/(site)/page.tsx` (usar `git mv app/page.tsx "app/(site)/page.tsx"` y luego editar). En el nuevo home:
   - ELIMINAR el `<Navbar />` y `<Footer />` inline y el skip-link inline (ahora los provee el layout del grupo) para evitar doble chrome.
   - ELIMINAR el `<main>` wrapper propio (el layout ya aporta `<main id="main">`); dejar las secciones como fragmento (`<>...</>`).
   - CONSERVAR `<StickyCTAMobile />` (específico del home) fuera/después de las secciones.
   - Los `id` de sección (`#problema`, `#beneficios`, `#precios`, `#faq`) se conservan en sus componentes de sección (no se tocan).
   Confirmar que `app/page.tsx` ya no existe tras el move (evita conflicto de ruta `/`).

3. `/diplomado` (en `app/diplomado`) y los exports de `public/` (incl. `reto`) NO se tocan y NO entran en `(site)`: conservan su chrome propio (decisión CONTEXT). No crear ningún root layout de navegación global.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub && test -f "app/(site)/layout.tsx" && test -f "app/(site)/page.tsx" && test ! -f "app/page.tsx" && grep -q 'id="main"' "app/(site)/layout.tsx" && ! grep -q '<Navbar' "app/(site)/page.tsx" && echo OK</automated>
  </verify>
  <done>Existe `(site)/layout.tsx` con skip-link + Navbar + main#main + Footer; el home vive en `(site)/page.tsx` sin Navbar/Footer/main inline pero con StickyCTAMobile; `app/page.tsx` ya no existe.</done>
</task>

<task type="auto">
  <name>Task 2: Crear páginas placeholder y verificar build</name>
  <files>app/(site)/quienes-somos/page.tsx, app/(site)/programas/page.tsx, app/(site)/testimonios/page.tsx</files>
  <action>
Crear tres páginas placeholder mínimas bajo `(site)` para que la navegación del navbar y del footer resuelva (NAV-02). El contenido real llega en Fases 2-4 — estos son andamios, NO simplificaciones de un requisito de esta fase.

Para cada una (`quienes-somos`, `programas`, `testimonios`):
- Server component (sin `"use client"`). Heredan automáticamente el shell del route group.
- Export `metadata` con `title` propio (p. ej. "Quiénes somos | aprendoclub", "Programas | aprendoclub", "Testimonios | aprendoclub") y una `description` breve en español neutro.
- Contenido: una sección centrada con `section-spacing container-padding max-w-6xl mx-auto`, un `<h1>` con el nombre de la página (`text-white`, peso ≤ 600, Montserrat heredada) y un párrafo `text-gray-400` tipo "Contenido próximamente." o equivalente neutro. Debe respirar bajo el navbar fijo `h-[72px]` (dar padding-top suficiente, p. ej. `pt-28` o vía `section-spacing`). Usar tokens existentes (`bg-[var(--bg-primary)]` heredado), sin introducir un nuevo lenguaje visual ni `#b8f60d` fuera de lo reservado.

Tras crear las páginas, ejecutar `npm run build` y confirmar que compila sin errores (rutas `/`, `/quienes-somos`, `/programas`, `/testimonios`, `/diplomado`, `/links` presentes en el output). Corregir cualquier error de tipos/import que surja de los planes 01-01/01-02/01-03 hasta que el build pase.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>Las tres páginas resuelven bajo el shell con metadata propia; `npm run build` termina sin errores y lista las rutas `/`, `/quienes-somos`, `/programas`, `/testimonios`, `/diplomado`, `/links`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguno relevante | Páginas estáticas server-rendered sin entrada de usuario ni data-fetching. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of Service | route group / build | mitigate | Verificación `npm run build` asegura que no hay rutas rotas ni conflicto `/` (app/page vs (site)/page) antes de shippear. |
| T-01-05 | Information Disclosure | placeholder pages | accept | Contenido público de marketing; sin PII ni secretos. |
| T-01-SC | Tampering | dependencias | accept | Sin instalación de paquetes (constraint: sin deps nuevas). |
</threat_model>

<verification>
- `(site)/layout.tsx` monta skip-link + Navbar + main#main + Footer.
- Home en `(site)/page.tsx` sin chrome inline; `app/page.tsx` eliminado (sin conflicto de ruta `/`).
- `/quienes-somos`, `/programas`, `/testimonios` resuelven con el shell y metadata propia.
- `/diplomado` y `public/reto` intactos, fuera del grupo.
- `npm run build` verde.
</verification>

<success_criteria>
- NAV-01: shell compartido activo; toda página nueva hereda navbar + footer del home.
- NAV-02: los links del navbar/footer resuelven a rutas reales en desktop y mobile.
- Build de producción pasa sin errores.
</success_criteria>

<output>
Create `.planning/phases/01-fundaciones-y-navegaci-n/01-03-SUMMARY.md` when done.
</output>
