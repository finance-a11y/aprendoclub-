---
phase: 02-qui-nes-somos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/quienes-somos.ts
  - components/quienes-somos/hero.tsx
  - components/quienes-somos/historia.tsx
  - components/quienes-somos/fundadora.tsx
  - components/quienes-somos/equipo.tsx
autonomous: true
requirements: [ABOUT-01]

must_haves:
  truths:
    - "content/quienes-somos.ts exporta la copia VERBATIM de 02-CONTENT-SOURCE.md en objetos tipados"
    - "El visitante ve el Hero, la Historia (con la quote de Arianna), la sección Fundadora y el grid de Equipo (5) con el design system del home"
  artifacts:
    - path: "content/quienes-somos.ts"
      provides: "Capa de contenido data-driven (Payload-ready) con toda la copia verbatim + interfaces TS por bloque"
      contains: "La primera academia de SEO pensada para el mundo hispano"
    - path: "components/quienes-somos/hero.tsx"
      provides: "Sección Hero"
      min_lines: 15
    - path: "components/quienes-somos/historia.tsx"
      provides: "Sección Historia con quote destacada"
      min_lines: 20
    - path: "components/quienes-somos/fundadora.tsx"
      provides: "Sección Fundadora (estilo InstructorSection)"
      min_lines: 20
    - path: "components/quienes-somos/equipo.tsx"
      provides: "Grid de 5 miembros con foto/iniciales"
      min_lines: 20
  key_links:
    - from: "components/quienes-somos/*.tsx"
      to: "content/quienes-somos.ts"
      via: "import de los objetos de contenido"
      pattern: "from \"@/content/quienes-somos\""
---

<objective>
Crear la capa de contenido data-driven de la página Quiénes somos y construir las primeras cuatro secciones (Hero, Historia, Fundadora, Equipo) con el design system del home.

Purpose: Establecer la fuente única de copia (VERBATIM desde 02-CONTENT-SOURCE.md, ya humanizada) que consumirán todas las secciones, y entregar las secciones narrativas centrales que dan credibilidad al sitio (ABOUT-01).
Output: `content/quienes-somos.ts` + cuatro componentes de sección bajo `components/quienes-somos/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-qui-nes-somos/02-CONTEXT.md
@.planning/phases/02-qui-nes-somos/02-CONTENT-SOURCE.md
@.planning/phases/01-fundaciones-y-navegaci-n/01-UI-SPEC.md
@content/site.ts
@components/instructor-section.tsx
@components/testimonios-section.tsx
@app/globals.css

<interfaces>
<!-- Patrón data-driven a seguir (de content/site.ts): interfaces TS por bloque, comentario Payload-ready, objetos const exportados. -->
<!-- Tokens de design system disponibles en globals.css: -->
<!--   var(--bg-primary)=#0a0f14, var(--bg-secondary)=#111827, var(--accent)=#b8f60d, var(--primary)=#012fd8 -->
<!--   utilidades: container-padding, section-spacing -->
<!-- Patrón de sección (de instructor-section.tsx / testimonios-section.tsx): -->
<!--   "use client"; useRef + useInView(ref,{once:true,margin:"-100px"}); motion.div initial/animate -->
<!--   Eyebrow: <span className="text-sm font-bold uppercase tracking-wider text-[#b8f60d]">EYEBROW</span> -->
<!--   Heading: text-3xl md:text-4xl lg:text-5xl font-bold text-white -->
<!--   Cuerpo: text-gray-400 leading-relaxed -->
<!--   Card grid (testimonios): rounded-xl bg-[#0d1117] border border-white/10 p-6 hover:-translate-y-1 hover:border-white/20 -->
<!-- reduced-motion: framer-motion expone useReducedMotion(); navbar.tsx ya lo usa (const reduceMotion = useReducedMotion()) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear la capa de contenido content/quienes-somos.ts (contrato + copia verbatim)</name>
  <files>content/quienes-somos.ts</files>
  <action>
Crear `content/quienes-somos.ts` siguiendo el patrón data-driven de `content/site.ts` (interfaces TS por bloque, comentario de cabecera Payload-ready, objetos const exportados). Este archivo es el CONTRATO que consumen todas las secciones de esta fase y de la fase 2 completa.

Definir interfaces por bloque y exportar objetos con la copia tomada LITERALMENTE de `02-CONTENT-SOURCE.md` (ABOUT-01). NO reescribir, NO parafrasear, NO agregar frases. Reproducir cada texto carácter por carácter, incluyendo mayúsculas de eyebrows y signos. Los textos ya están humanizados (español neutro, sin em/en dashes, sin AI tells).

Bloques a modelar y exportar:
- `hero`: { eyebrow, titulo, subtitulo } — sección "## Hero".
- `historia`: { eyebrow, titulo, parrafos: string[] (los 3 párrafos en orden), quote: { texto, autor } } — sección "## Historia / Origen". La quote destacada es "Nuestro norte no es que entiendas SEO. Es que lo apliques, lo vendas y construyas una carrera real con él." atribuida a Arianna Lupi.
- `fundadora`: { eyebrow, nombre, rol, bio: string[] (los 2 párrafos), foto: "/arianna-lupi.webp" } — sección "## Fundadora".
- `equipo`: TeamMember[] de 5 elementos { nombre, rol, bio, foto?, iniciales } — sección "## Equipo". Mapear en orden a `/avatar-1.webp` .. `/avatar-5.webp` en el campo `foto`; `iniciales` = iniciales del nombre (ej. "AL", "DR") como fallback. Reproducir nombre/rol/bio verbatim.
- `metodologia`: { eyebrow, titulo, pilares: { nombre, descripcion }[] (los 4 pilares EPAM en orden) } — sección "## Metodología / valores (EPAM)".
- `stats`: { value, label }[] de 3 — sección "## Stats": `2.000+`/`estudiantes formados`, `30+`/`empresas ayudadas`, `$2M+`/`en ventas generadas`.
- `ctaFinal`: { titulo, texto, botonLabel: "Ver programas", botonHref: "/programas" } — sección "## CTA final".

Definir e interface-exportar los tipos: `HeroContent`, `HistoriaContent`, `Quote`, `FundadoraContent`, `TeamMember`, `MetodologiaContent`, `Pilar`, `StatItem`, `CtaContent`. Incluir el comentario de cabecera explicando que espeja bloques Payload (v1.1), igual que site.ts.

Este task modela TODA la copia de la fase (incluidos metodologia/stats/ctaFinal que consume el Plan 02) para que el contrato quede completo desde la Wave 1.
  </action>
  <verify>
    <automated>grep -q 'La primera academia de SEO pensada para el mundo hispano' content/quienes-somos.ts && grep -q 'Nuestro norte no es que entiendas SEO. Es que lo apliques' content/quienes-somos.ts && grep -q 'El método EPAM' content/quienes-somos.ts && grep -q '/arianna-lupi.webp' content/quienes-somos.ts && node -e "const f=require('fs').readFileSync('content/quienes-somos.ts','utf8'); if((f.match(/avatar-[1-5]\.webp/g)||[]).length<5) process.exit(1)"</automated>
  </verify>
  <done>content/quienes-somos.ts existe, exporta hero/historia/fundadora/equipo(5)/metodologia/stats/ctaFinal con interfaces TS, y toda la copia coincide VERBATIM con 02-CONTENT-SOURCE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Secciones Hero + Historia</name>
  <files>components/quienes-somos/hero.tsx, components/quienes-somos/historia.tsx</files>
  <action>
Crear dos client components (`"use client"`) que consumen `content/quienes-somos.ts`. Sin texto hardcodeado: todo el copy sale de los objetos importados.

`components/quienes-somos/hero.tsx` (export `QuienesSomosHero`): sección centrada con fondo `var(--bg-primary)`, `container-padding` + padding-top que despeje el navbar fijo `h-[72px]` (usar `pt-28` o `section-spacing` con margen superior, coherente con el placeholder actual). Eyebrow (`text-sm font-bold uppercase tracking-wider text-[#b8f60d]`), título (`text-3xl md:text-5xl lg:text-6xl font-bold text-white`), subtítulo (`text-lg text-gray-400 leading-relaxed max-w-2xl`). Entrada con `motion.div` initial `{opacity:0, y:20}` → animate.

`components/quienes-somos/historia.tsx` (export `HistoriaSection`): fondo `var(--bg-secondary)`, `container-padding section-spacing`, `useRef`+`useInView(ref,{once:true,margin:"-100px"})`. Header con eyebrow + título. Renderizar `historia.parrafos` con `.map` como `<p className="text-gray-400 leading-relaxed">` dentro de un contenedor `max-w-3xl`. Debajo, la quote destacada: bloque tipo blockquote con borde-izquierdo accent (`border-l-2 border-[#b8f60d] pl-6`), texto en `text-xl md:text-2xl text-white font-medium italic` y atribución `text-sm text-gray-500` con `historia.quote.autor`. Envolver quote con comillas tipográficas (`&ldquo;`/`&rdquo;`) sin alterar el texto interno.

Ambos: importar `useReducedMotion` de framer-motion; si `reduceMotion` es true, usar `initial={false}` y omitir offsets x/y (render en estado final), igual que el patrón del navbar. Respetar el uso reservado del accent (solo eyebrows y el borde de la quote como detalle, no como texto de cuerpo).
  </action>
  <verify>
    <automated>grep -q 'from "@/content/quienes-somos"' components/quienes-somos/hero.tsx && grep -q 'from "@/content/quienes-somos"' components/quienes-somos/historia.tsx && grep -q 'useReducedMotion' components/quienes-somos/historia.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Hero e Historia renderizan copy desde content/quienes-somos.ts (sin texto inline), respetan reduced-motion y tipan sin errores.</done>
</task>

<task type="auto">
  <name>Task 3: Secciones Fundadora + Equipo</name>
  <files>components/quienes-somos/fundadora.tsx, components/quienes-somos/equipo.tsx</files>
  <action>
Crear dos client components que consumen `content/quienes-somos.ts`.

`components/quienes-somos/fundadora.tsx` (export `FundadoraSection`): replicar la estructura de dos columnas de `components/instructor-section.tsx` (foto con marco decorativo desplazado + bio) pero alimentada por `fundadora` del content. Fondo `var(--bg-primary)`. Eyebrow = `fundadora.eyebrow`, `<h2>` = `fundadora.nombre`, rol en `text-[#012fd8] font-medium` = `fundadora.rol`, párrafos = `fundadora.bio.map(...)` como `text-gray-400 leading-relaxed`. `<img src={fundadora.foto} alt={...}>` con las mismas clases de marco/objeto que instructor-section (rounded-2xl, object-cover object-top). Entrada motion con offsets x (foto -30, bio +30) como el original.

`components/quienes-somos/equipo.tsx` (export `EquipoSection`): fondo `var(--bg-secondary)`, `container-padding section-spacing`, `useRef`+`useInView`. Header con eyebrow "NUESTRO EQUIPO" (usar un eyebrow apropiado; si no hay uno en el content para el equipo, usar el texto "NUESTRO EQUIPO" — esto es rótulo estructural, no copia de marca, pero preferir mantenerlo mínimo) y un título breve. Grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl` de 5 cards siguiendo el patrón de testimonios-section (`rounded-xl bg-[#0d1117] border border-white/10 p-6 hover:-translate-y-1 hover:border-white/20`). Cada card: avatar circular (`w-16 h-16 rounded-full object-cover`) con `member.foto`; si la imagen falla o no representa a la persona real, fallback a un círculo con `member.iniciales` (`bg-[var(--bg-tertiary)] text-white flex items-center justify-center`) — implementar el fallback con `onError` que oculta el img y muestra el círculo de iniciales, o renderizar iniciales cuando no haya foto. Nombre (`font-bold text-white`), rol (`text-sm text-[#012fd8]`), bio (`text-sm text-gray-400 leading-relaxed`). Stagger `delay: index * 0.1`. Respetar reduced-motion (`useReducedMotion`).

NO inventar caras ni datos: usar solo los avatares provistos y la copia del content.
  </action>
  <verify>
    <automated>grep -q 'from "@/content/quienes-somos"' components/quienes-somos/fundadora.tsx && grep -q 'from "@/content/quienes-somos"' components/quienes-somos/equipo.tsx && grep -q 'useReducedMotion' components/quienes-somos/equipo.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Fundadora (dos columnas, foto arianna-lupi.webp) y Equipo (grid de 5 con foto/iniciales) renderizan desde el content y tipan sin errores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguno | Página estática (SSG) de solo lectura. Sin input de usuario, sin data-fetching, sin nuevas dependencias npm. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | copia verbatim | accept | Contenido público de marketing; ya publicado en aprendoseo.com. Sin datos sensibles. |
| T-02-SC | Tampering | npm installs | accept | Este plan no instala paquetes (solo usa framer-motion/lucide ya presentes). Sin gate de legitimidad requerido. |
</threat_model>

<verification>
- `npx tsc --noEmit` sin errores.
- Los cuatro componentes importan de `@/content/quienes-somos` y no contienen copy hardcodeado de marca.
- Las cadenas distintivas de 02-CONTENT-SOURCE.md aparecen VERBATIM en content/quienes-somos.ts.
</verification>

<success_criteria>
- content/quienes-somos.ts existe con interfaces TS por bloque y toda la copia verbatim de la fase.
- Hero, Historia (con quote), Fundadora y Equipo (5) existen bajo components/quienes-somos/ con el design system del home y reduced-motion respetado.
- No hay texto de marca inventado ni parafraseado.
</success_criteria>

<output>
Create `.planning/phases/02-qui-nes-somos/02-01-SUMMARY.md` when done
</output>
