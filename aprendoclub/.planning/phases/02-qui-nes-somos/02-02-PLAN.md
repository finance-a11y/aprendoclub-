---
phase: 02-qui-nes-somos
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/quienes-somos/metodologia.tsx
  - components/quienes-somos/stats.tsx
  - components/quienes-somos/cta.tsx
  - app/(site)/quienes-somos/page.tsx
  - components/instructor-section.tsx
autonomous: false
requirements: [ABOUT-01, ABOUT-02]

must_haves:
  truths:
    - "El visitante puede abrir /quienes-somos y leer historia, misión, bio de la fundadora, equipo, método EPAM, stats y CTA a /programas"
    - "El home muestra un teaser 'Conoce más sobre nosotros' que enlaza a /quienes-somos"
    - "npm run build pasa sin errores"
  artifacts:
    - path: "app/(site)/quienes-somos/page.tsx"
      provides: "Página ensamblada con las 7 secciones + metadata SEO"
      contains: "export const metadata"
    - path: "components/quienes-somos/metodologia.tsx"
      provides: "Sección Metodología EPAM (4 pilares)"
      min_lines: 20
    - path: "components/quienes-somos/stats.tsx"
      provides: "Sección de stats reutilizados"
      min_lines: 15
    - path: "components/quienes-somos/cta.tsx"
      provides: "CTA final hacia /programas"
      min_lines: 15
  key_links:
    - from: "app/(site)/quienes-somos/page.tsx"
      to: "components/quienes-somos/*"
      via: "import y render de las 7 secciones"
      pattern: "components/quienes-somos"
    - from: "components/instructor-section.tsx"
      to: "/quienes-somos"
      via: "next/link teaser"
      pattern: "/quienes-somos"
    - from: "components/quienes-somos/cta.tsx"
      to: "/programas"
      via: "next/link"
      pattern: "/programas"
---

<objective>
Completar la página Quiénes somos: construir las secciones restantes (Metodología EPAM, Stats, CTA), ensamblar `app/(site)/quienes-somos/page.tsx` con metadata SEO, y añadir el teaser del home que enlaza a la página (ABOUT-01 + ABOUT-02).

Purpose: Entregar la página completa navegable bajo el shell compartido y conectar el home con ella, cumpliendo los tres criterios de éxito de la fase 2 del ROADMAP.
Output: 3 componentes de sección + página ensamblada + link en el home + build verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-qui-nes-somos/02-CONTENT-SOURCE.md
@.planning/phases/01-fundaciones-y-navegaci-n/01-UI-SPEC.md
@content/quienes-somos.ts
@components/instructor-section.tsx
@app/(site)/quienes-somos/page.tsx
@app/(site)/page.tsx

<interfaces>
<!-- content/quienes-somos.ts (creado en Plan 02-01) exporta: hero, historia, fundadora, equipo, -->
<!--   metodologia { eyebrow, titulo, pilares: {nombre, descripcion}[] }, -->
<!--   stats: {value,label}[], ctaFinal { titulo, texto, botonLabel, botonHref } -->
<!-- Componentes ya creados en 02-01 (Wave 1): -->
<!--   components/quienes-somos/hero.tsx      -> QuienesSomosHero -->
<!--   components/quienes-somos/historia.tsx  -> HistoriaSection -->
<!--   components/quienes-somos/fundadora.tsx -> FundadoraSection -->
<!--   components/quienes-somos/equipo.tsx    -> EquipoSection -->
<!-- instructor-section.tsx es "use client"; usa next/link? NO actualmente. Añadir import Link de next/link. -->
<!-- page.tsx actual es server component con export const metadata (mantener ese patrón para SEO). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Secciones Metodología (EPAM) + Stats + CTA</name>
  <files>components/quienes-somos/metodologia.tsx, components/quienes-somos/stats.tsx, components/quienes-somos/cta.tsx</files>
  <action>
Crear tres client components (`"use client"`) que consumen `content/quienes-somos.ts`. Sin copy hardcodeado.

`components/quienes-somos/metodologia.tsx` (export `MetodologiaSection`): fondo `var(--bg-primary)`, `container-padding section-spacing`, `useRef`+`useInView`. Header con `metodologia.eyebrow` + `metodologia.titulo`. Grid de 4 pilares `grid-cols-1 sm:grid-cols-2 gap-6 max-w-5xl`: cada pilar como card (`rounded-xl bg-[#0d1117] border border-white/10 p-6`) con `pilar.nombre` (`font-bold text-white`) y `pilar.descripcion` (`text-gray-400 leading-relaxed`). Opcional: numeral 01-04 en accent como detalle. Stagger `delay: index * 0.1`.

`components/quienes-somos/stats.tsx` (export `StatsSection`): fondo `var(--bg-secondary)`, `container-padding section-spacing`. Fila `flex flex-wrap justify-center gap-8 md:gap-16` con `stats.map`: value (`text-4xl md:text-5xl font-extrabold text-white`) + label (`text-sm text-gray-500`). Reutilizar el look de stats de instructor-section.

`components/quienes-somos/cta.tsx` (export `CtaFinalSection`): fondo `var(--bg-primary)`, sección centrada. `ctaFinal.titulo` (`text-3xl md:text-4xl font-bold text-white`), `ctaFinal.texto` (`text-lg text-gray-400`), y botón con `next/link` `<Link href={ctaFinal.botonHref}>` mostrando `ctaFinal.botonLabel`. El botón SÍ es un CTA real → puede usar el accent: `bg-[#b8f60d] text-black px-6 py-3 rounded-lg font-semibold hover:shadow-[0_0_20px_rgba(184,246,13,0.3)] transition-all`. Enlaza a `/programas`.

Los tres: `useReducedMotion` respetado (initial={false} + sin offsets cuando reduce).
  </action>
  <verify>
    <automated>grep -q 'from "@/content/quienes-somos"' components/quienes-somos/metodologia.tsx && grep -q 'from "@/content/quienes-somos"' components/quienes-somos/stats.tsx && grep -q '/programas' components/quienes-somos/cta.tsx && grep -q 'useReducedMotion' components/quienes-somos/metodologia.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Metodología (4 pilares), Stats (3) y CTA (→/programas) renderizan desde el content, respetan reduced-motion y tipan sin errores.</done>
</task>

<task type="auto">
  <name>Task 2: Ensamblar la página /quienes-somos + metadata SEO</name>
  <files>app/(site)/quienes-somos/page.tsx</files>
  <action>
Reemplazar el placeholder de `app/(site)/quienes-somos/page.tsx`. Mantener el archivo como SERVER component (sin `"use client"`) para conservar `export const metadata`.

Importar y renderizar en orden las 7 secciones (client components, se pueden importar y renderizar desde un server component): `QuienesSomosHero` → `HistoriaSection` → `FundadoraSection` → `EquipoSection` → `MetodologiaSection` → `StatsSection` → `CtaFinalSection`. Envolver en un fragment `<>...</>` (el `<main>` y el chrome los aporta el layout `(site)`). No añadir navbar/footer.

Actualizar `metadata`: `title: "Quiénes somos | aprendoclub"`, `description` de 1-2 frases coherente con el subtítulo del hero (basada en la copia real, español neutro, sin AI tells, sin em-dashes). Ejemplo aceptable: "Conoce la historia de aprendoclub, la primera academia de SEO e IA para el mundo hispano, y al equipo que forma especialistas con proyectos reales." No inventar cifras nuevas.

La página se monta bajo el shell `(site)` de la Fase 1 (navbar + footer heredados) y usa el patrón data-driven vía los componentes que consumen content/quienes-somos.ts.
  </action>
  <verify>
    <automated>grep -q 'export const metadata' "app/(site)/quienes-somos/page.tsx" && grep -q 'QuienesSomosHero' "app/(site)/quienes-somos/page.tsx" && grep -q 'CtaFinalSection' "app/(site)/quienes-somos/page.tsx" && node -e "const f=require('fs').readFileSync('app/(site)/quienes-somos/page.tsx','utf8'); if(f.includes('Contenido próximamente')) process.exit(1); if(f.includes('use client')) process.exit(1)"</automated>
  </verify>
  <done>La página importa y renderiza las 7 secciones en orden, exporta metadata SEO actualizada, no es client component y ya no contiene el placeholder.</done>
</task>

<task type="auto">
  <name>Task 3: Teaser del home (ABOUT-02) + build</name>
  <files>components/instructor-section.tsx</files>
  <action>
ABOUT-02: en `components/instructor-section.tsx`, añadir DEBAJO del bloque de bio (dentro de la columna de la derecha, tras las stats, o bajo el grid) un enlace/botón "Conoce más sobre nosotros" que apunta a `/quienes-somos`. Importar `Link` de `next/link` y `ArrowRight` de `lucide-react`.

Estilo: botón secundario (NO usar el accent como relleno — el accent está reservado a CTAs primarios por el UI-SPEC). Usar un botón con borde: `inline-flex items-center gap-2 rounded-lg border border-white/20 px-5 py-2.5 text-sm font-semibold text-white hover:border-white/40 hover:bg-white/5 transition-all` con el icono `ArrowRight className="h-4 w-4"`. Texto VERBATIM: `Conoce más sobre nosotros`.

NO duplicar contenido: solo sumar el enlace, sin crear una sección about paralela ni mover texto. No tocar la copia existente de la InstructorSection.

Tras el cambio, ejecutar el build completo del proyecto para validar toda la fase.
  </action>
  <verify>
    <automated>grep -q 'Conoce más sobre nosotros' components/instructor-section.tsx && grep -q '/quienes-somos' components/instructor-section.tsx && npm run build</automated>
  </verify>
  <done>El home muestra el botón "Conoce más sobre nosotros" → /quienes-somos en la InstructorSection, sin duplicar contenido, y `npm run build` pasa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Página /quienes-somos completa (Hero, Historia+quote, Fundadora, Equipo de 5, Método EPAM, Stats, CTA) bajo el shell compartido, más el teaser "Conoce más sobre nosotros" en el home.</what-built>
  <how-to-verify>
1. `npm run dev` y abrir http://localhost:3000/quienes-somos
2. Confirmar visualmente que las 7 secciones aparecen en orden, con navbar/footer del shell, tipografía Montserrat, fondos var(--bg-primary)/var(--bg-secondary) alternados y accent #b8f60d solo en eyebrows/CTA.
3. Verificar que TODOS los textos coinciden con 02-CONTENT-SOURCE.md (hero, quote de Arianna, bios del equipo, pilares EPAM, stats 2.000+/30+/$2M+). Ninguna frase inventada ni parafraseada.
4. Click en "Ver programas" → debe ir a /programas.
5. Abrir http://localhost:3000/ , bajar a la sección del instructor y confirmar el botón "Conoce más sobre nosotros" → click lleva a /quienes-somos.
6. Activar "reducir movimiento" en el sistema y recargar: la página no debe animar entradas (render en estado final).
  </how-to-verify>
  <resume-signal>Escribe "approved" o describe los ajustes visuales/copy necesarios</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguno | Página estática (SSG) y modificación de un componente del home. Sin input de usuario, sin data-fetching, sin nuevas dependencias npm. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | enlaces href (/quienes-somos, /programas) | mitigate | Usar next/link con rutas internas literales; sin hrefs dinámicos ni externos. |
| T-02-SC | Tampering | npm installs | accept | No se instalan paquetes (framer-motion, lucide-react y next/link ya presentes). Gate de legitimidad no aplica. |
</threat_model>

<verification>
- `npm run build` pasa sin errores ni warnings de tipos.
- /quienes-somos renderiza las 7 secciones bajo el shell (site).
- El home enlaza a /quienes-somos con el teaser (ABOUT-02).
- Toda la copia coincide VERBATIM con 02-CONTENT-SOURCE.md.
</verification>

<success_criteria>
Criterios de éxito de la Fase 2 (ROADMAP):
1. El visitante abre /quienes-somos y lee historia, misión, propuesta y bio de Arianna Lupi (contenido migrado, verbatim).
2. El home muestra un teaser de quiénes somos que enlaza a la página completa.
3. /quienes-somos usa el shell compartido y el patrón data-driven de la Fase 1 (content/quienes-somos.ts).
Más: build verde y checkpoint humano de verificación visual aprobado.
</success_criteria>

<output>
Create `.planning/phases/02-qui-nes-somos/02-02-SUMMARY.md` when done
</output>
