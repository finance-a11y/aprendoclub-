---
phase: 03-testimonios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/testimonios.ts
  - components/testimonios/testimonial-avatar.tsx
  - components/testimonios/hero.tsx
  - components/testimonios/grid.tsx
  - components/testimonios/reto-galeria.tsx
  - components/testimonios/cta.tsx
  - app/(site)/testimonios/page.tsx
  - components/testimonios-section.tsx
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "Un visitante puede abrir /testimonios y ver todos los testimonios de 03-CONTENT-SOURCE.md (3 con foto + los nombrados con avatar de iniciales)"
    - "Los testimonios sin foto muestran un avatar de iniciales (no imágenes inventadas)"
    - "La galería 'Historias del Reto' muestra las 9 capturas t1..t9"
    - "La banda de logos 'empresas como' aparece en la página"
    - "El CTA final de /testimonios enlaza a /programas"
    - "La sección de testimonios del home muestra un botón 'Ver todos los testimonios' que enlaza a /testimonios, conservando los 3 testimonios existentes"
    - "El copy propio de la página (hero, títulos, CTA) es verbatim de 03-CONTENT-SOURCE.md; los quotes son verbatim"
  artifacts:
    - path: "content/testimonios.ts"
      provides: "Capa de contenido data-driven (copy de página + testimonios + lista de imágenes del Reto)"
      contains: "export const testimonios"
    - path: "app/(site)/testimonios/page.tsx"
      provides: "Página /testimonios ensamblada con metadata SEO"
      contains: "export const metadata"
    - path: "components/testimonios/grid.tsx"
      provides: "Grid de cards de testimonios con avatar foto/iniciales"
    - path: "components/testimonios/reto-galeria.tsx"
      provides: "Galería de imágenes t1..t9 del Reto"
    - path: "components/testimonios-section.tsx"
      provides: "Sección del home con botón 'Ver todos' (TEST-02)"
      contains: "/testimonios"
  key_links:
    - from: "app/(site)/testimonios/page.tsx"
      to: "content/testimonios.ts"
      via: "import de los objetos de contenido"
      pattern: "from \"@/content/testimonios\""
    - from: "components/testimonios-section.tsx"
      to: "/testimonios"
      via: "Link next/link"
      pattern: "/testimonios"
    - from: "components/testimonios/cta.tsx"
      to: "/programas"
      via: "Link next/link"
      pattern: "/programas"
---

<objective>
Construir la página `/testimonios` (TEST-01) migrando los testimonios y las imágenes del Reto de aprendoseo.com, y enlazar la sección de testimonios del home a la página completa (TEST-02).

Purpose: Reunir la prueba social del proyecto en una página navegable y visible desde el home para reforzar la conversión de tráfico de Ads.
Output: `content/testimonios.ts` (data-driven, Payload-ready), la página ensamblada con sus secciones, el helper de avatar de iniciales, y el botón "Ver todos" en el home.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Fuente de contenido VERBATIM (regla dura del plan)
@.planning/phases/03-testimonios/03-CONTENT-SOURCE.md
@.planning/phases/03-testimonios/03-CONTEXT.md

# Contrato visual del shell (tokens, accent reservado, motion, reduced-motion)
@.planning/phases/01-fundaciones-y-navegaci-n/01-UI-SPEC.md

# Patrón data-driven a espejar (interfaces tipadas + copy verbatim)
@content/quienes-somos.ts

# Card de testimonio + banda de logos a reusar; aquí se añade el botón TEST-02
@components/testimonios-section.tsx

# Placeholder a reemplazar
@app/(site)/testimonios/page.tsx

<interfaces>
<!-- Patrón de avatar con fallback ya establecido en Fase 2 (components/quienes-somos/equipo.tsx). -->
<!-- El componente TeamAvatar usa useState + onError para caer a iniciales. Espejar esta forma. -->

Tokens CSS disponibles (app/globals.css):
  --bg-primary: #0a0f14   (fondo dominante)
  --bg-secondary: #111827 (alternancia de secciones)
  --bg-tertiary: #1f2937  (fondo del avatar de iniciales)
  --accent: #b8f60d       (SOLO eyebrows del sistema + fondo del CTA; nunca texto de cards)

Utilidades: container-padding, section-spacing (ya definidas).

Assets confirmados en public/:
  /testimonio-1.webp, /testimonio-2.webp, /testimonio-3.webp
  /reto/testimonios/t1.png ... t9.png
  /logo-holafly.svg, /logo-hubspot.svg, /logo-unilever.svg, /logo-dslx.svg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capa de contenido content/testimonios.ts (verbatim, data-driven)</name>
  <files>content/testimonios.ts</files>
  <action>
Crear la capa de contenido tipada, espejando el estilo de `content/quienes-somos.ts` (interfaces con doc-comments "Payload-ready", copy separado de la presentación). Definir e implementar:

- `interface Testimonio { nombre: string; rol?: string; ubicacion?: string; quote: string; foto?: string; }`
  - Los 3 con foto llevan `rol` + `foto`; los nombrados llevan `ubicacion` (que puede ser cadena vacía). Nunca ambos.
- `interface RetoImagen { src: string; alt: string; }`
- Objetos de copy de página: `hero` (eyebrow, titulo, subtitulo), `gridTitulo`, `logosBanda` (texto de la banda), `reto` (eyebrow, titulo, texto), `cta` (titulo, texto, botonLabel, botonHref).
- `export const testimonios: Testimonio[]` con TODAS las entradas de 03-CONTENT-SOURCE.md: primero los 3 con foto (versión rica del home), luego cada entrada nombrada (Wilson Moros … Randy Barrera). Copiar nombre, rol/ubicación y quote EXACTAMENTE del source, incluyendo mayúsculas, acentos y la marca ya rebrandeada a "aprendoclub" donde el quote la nombra (no re-aplicar el rebrand: el source ya lo trae resuelto).
- `export const retoImagenes: RetoImagen[]` con t1..t9: `src: "/reto/testimonios/tN.png"`, `alt: "Testimonio de participante N del Reto 7 días"` (numerar 1-9 para unicidad de alt).
- `export const trustedCompanies` reusando las 4 de components/testimonios-section.tsx (Holafly, HubSpot, Unilever, DSLX) para no duplicar inline en la página.

REGLA DURA: los quotes y el copy de página van VERBATIM de 03-CONTENT-SOURCE.md. No parafrasear, no inventar nombres/quotes, no añadir entradas que no estén en el source, no usar em-dashes. NO fabricar fotos para las entradas sin foto.
  </action>
  <verify>
  <automated>node -e "const t=require('./content/testimonios.ts')" 2>/dev/null || npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "content/testimonios" | grep -q '^0$' && echo "tipos OK"</automated>
  Además, contar entradas: `grep -c 'quote:' content/testimonios.ts` debe igualar el número de entradas de testimonio en 03-CONTENT-SOURCE.md (3 con foto + las nombradas). `grep -c 'foto:' content/testimonios.ts` == 3.
  </verify>
  <done>content/testimonios.ts exporta `testimonios`, `retoImagenes`, y el copy de página; el conteo de quotes coincide con el source; exactamente 3 entradas con `foto`; compila sin errores de tipos.</done>
</task>

<task type="auto">
  <name>Task 2: Secciones + ensamblaje de app/(site)/testimonios/page.tsx</name>
  <files>components/testimonios/testimonial-avatar.tsx, components/testimonios/hero.tsx, components/testimonios/grid.tsx, components/testimonios/reto-galeria.tsx, components/testimonios/cta.tsx, app/(site)/testimonios/page.tsx</files>
  <action>
Reemplazar el placeholder de la página componiendo secciones nuevas, todas siguiendo el patrón establecido de Fase 2 (`"use client"` + `useRef` + `useInView(ref,{once:true,margin:"-100px"})` + `useReducedMotion()` guard con `initial={reduceMotion ? false : {...}}` y `transition={{duration: reduceMotion ? 0 : 0.6}}`).

1. `testimonial-avatar.tsx`: componente `TestimonialAvatar` que espeja `TeamAvatar` de components/quienes-somos/equipo.tsx. Si hay `foto`, renderiza `<img>` con `onError` que cae a iniciales; si no, muestra iniciales sobre `bg-[var(--bg-tertiary)]`. Incluir helper local `getIniciales(nombre)` que toma las iniciales de las dos primeras palabras (p.ej. "Wilson Moros" → "WM"), en mayúsculas.
2. `hero.tsx`: eyebrow (`text-[var(--accent)]` uppercase) + titulo (`text-white`, h1) + subtitulo (`text-gray-400`), padding superior para no chocar con el navbar fijo `h-[72px]` (usar `pt-28` o equivalente como en el placeholder). Fondo `var(--bg-primary)`.
3. `grid.tsx`: reusa el estilo EXACTO de la card del home (rounded-xl, `bg-[#0d1117]`, `border-white/10`, 5 estrellas amarillas de lucide, quote en `&ldquo;…&rdquo;`), pero usando `TestimonialAvatar` y renderizando `rol ?? ubicacion` como subtítulo (omitir el subtítulo cuando ambos faltan o `ubicacion` es cadena vacía). Grid responsivo `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl`. Título de sección desde `gridTitulo`. Stagger de entrada por índice. Debajo del grid, incluir la banda de logos "empresas como" reusando `trustedCompanies` (mismo markup opacity/grayscale del home). Fondo `var(--bg-secondary)`.
4. `reto-galeria.tsx`: eyebrow + titulo + texto desde `reto`; grid de imágenes `grid-cols-2 md:grid-cols-3 gap-4 max-w-5xl` mapeando `retoImagenes` con `alt` descriptivo. Tratar como capturas (object-cover, rounded-lg, aspect-ratio consistente). Fondo `var(--bg-primary)`.
5. `cta.tsx`: espejar components/quienes-somos/cta.tsx (titulo, texto, `Link` a `cta.botonHref` con fondo `var(--accent)` + glow hover). Botón "Ver programas" → `/programas`.
6. `page.tsx`: server component. `export const metadata: Metadata` (title "Testimonios | aprendoclub" + description humanizada en español neutro sobre testimonios reales). Componer `<TestimoniosHero/> <TestimoniosGrid/> <RetoGaleria/> <TestimoniosCta/>`.

Accent `#b8f60d`/`var(--accent)` limitado a eyebrows y CTA (regla del UI-SPEC). Ningún em-dash en copy nuevo. No introducir dependencias nuevas.
  </action>
  <verify>
  <automated>npx tsc --noEmit 2>&1 | grep -E "testimonios/(page|hero|grid|reto|cta|testimonial-avatar)" | grep -vc "" | grep -q '^0$' && echo "tipos OK"</automated>
  Además: `grep -q "from \"@/content/testimonios\"" app/(site)/testimonios/page.tsx` (o dentro de las secciones) y `grep -q "export const metadata" app/(site)/testimonios/page.tsx`.
  </verify>
  <done>La página renderiza hero, grid de cards (avatar foto/iniciales), banda de logos, galería t1..t9 y CTA a /programas; usa var(--accent) solo en eyebrows+CTA; metadata SEO presente; compila sin errores.</done>
</task>

<task type="auto">
  <name>Task 3: Botón "Ver todos los testimonios" en el home (TEST-02) + build</name>
  <files>components/testimonios-section.tsx</files>
  <action>
En components/testimonios-section.tsx (sección del home), añadir un `Link` de next/link "Ver todos los testimonios" → `/testimonios`, colocado tras el grid de las 3 cards (antes o junto a la banda de logos), con animación de entrada coherente (respetando `useReducedMotion` si se agrega el guard; el componente actual usa `useInView` sin guard, no romperlo). Estilo del botón: coherente con el design system; puede ser secundario/outline (borde `border-white/20`, texto blanco, hover) para NO competir con el CTA verde reservado del navbar, o el botón verde de CTA si se prefiere. Conservar los 3 testimonios existentes en el home; NO duplicar la lista completa aquí. Importar `Link` de "next/link".

Tras el cambio, ejecutar el build completo del proyecto.
  </action>
  <verify>
  <automated>npm run build</automated>
  Además: `grep -q "/testimonios" components/testimonios-section.tsx` y confirmar que siguen los 3 objetos en el array `testimonials` (no se agregaron 24).
  </verify>
  <done>El home muestra un enlace "Ver todos los testimonios" → /testimonios conservando sus 3 cards; `npm run build` pasa sin errores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguno nuevo | Página de contenido estático server-rendered. Sin input de usuario, sin data-fetching, sin formularios, sin nuevas dependencias/paquetes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Contenido de testimonios (nombres/países reales) | accept | Datos ya públicos en aprendoseo.com; se migran verbatim, sin PII adicional. |
| T-03-SC | Tampering | Instalación de paquetes | accept | El plan no instala paquetes (reusa framer-motion/lucide ya presentes). Sin superficie de supply-chain. |
</threat_model>

<verification>
- `npm run build` pasa.
- `/testimonios` carga y muestra: hero, grid con todas las entradas del source (3 con foto real + resto con iniciales), banda de logos, galería t1..t9, CTA a /programas.
- Los quotes y el copy de página coinciden VERBATIM con 03-CONTENT-SOURCE.md (revisión manual de spot-check en el checkpoint de verificación humana de fase, si aplica).
- El home enlaza a /testimonios y conserva sus 3 testimonios.
- Accent `#b8f60d` solo en eyebrows + CTA.
</verification>

<success_criteria>
1. Un visitante abre `/testimonios` y ve los testimonios migrados de aprendoseo.com y las capturas de `public/reto/testimonios` (Criterio 1 de ROADMAP Fase 3).
2. La sección de testimonios del home es visible y enlaza a `/testimonios` (Criterio 2).
3. El contenido vive en `content/testimonios.ts` (objetos/consts data-driven), consumido por la página y sus secciones (Criterio 3).
4. Quotes y copy de página verbatim del source; entradas sin foto usan avatar de iniciales; ningún em-dash en copy nuevo.
</success_criteria>

<output>
Create `.planning/phases/03-testimonios/03-01-SUMMARY.md` when done
</output>
