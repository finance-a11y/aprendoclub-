---
phase: 26-copy-general-home
plan: 04
type: execute
wave: 2
depends_on: [26-01, 26-02, 26-03]
files_modified: []
autonomous: true
requirements: [COPY-01, COPY-02, HOME-01, HOME-02]

must_haves:
  truths:
    - "Ningun archivo de seed-data tiene 'real'/'reales' en copy visible tras el barrido"
    - "La base de datos (Postgres/Neon) refleja el nuevo copy tras correr el seed"
  artifacts:
    - path: "aprendoclub/scripts/seed/seed-data"
      provides: "Gate de verificacion global previo al seed"
  key_links:
    - from: "aprendoclub/scripts/seed.ts"
      to: "Postgres/Neon (site-settings + pages)"
      via: "npm run seed"
      pattern: "payload run scripts/seed.ts"
---

<objective>
Cerrar la fase: (1) verificar de forma global que el barrido de "real"/"reales" y el de-enfasis de SEO quedaron completos en todos los archivos editados por los planes 01-03, y (2) correr el seed para que la base de datos refleje los cambios.

Purpose: Los planes 01-03 editan objetos TypeScript fuente, no la DB. Sin re-seed, el sitio en vivo no cambia. Este plan materializa el copy y confirma que no quedaron residuos.
Output: Gate de verificacion pasado + seed corrido contra la DB local/configurada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-copy-general-home/26-CONTEXT.md

<nota_entorno>
El seed corre desde el subdirectorio `aprendoclub/` con `npm run seed` (alias de `payload run scripts/seed.ts`). Requiere las env vars de conexion a Postgres/Neon YA configuradas localmente (.env). NO inventar credenciales. Si faltan env vars o el comando falla por conexion, detener y reportar — no simular exito.
El seed es idempotente (upsert por clave natural / updateGlobal), re-correrlo no duplica datos.
</nota_entorno>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate de verificacion global del barrido</name>
  <files>aprendoclub/scripts/seed/seed-data (solo lectura), aprendoclub/scripts/seed/globals.ts (solo lectura)</files>
  <read_first>
    - aprendoclub/scripts/seed/seed-data/home.ts
    - aprendoclub/scripts/seed/seed-data/diplomado.ts
    - aprendoclub/scripts/seed/seed-data/quienes-somos.ts
    - aprendoclub/scripts/seed/seed-data/faqs.ts
    - aprendoclub/scripts/seed/seed-data/testimonios.ts
    - aprendoclub/scripts/seed/seed-data/reto.ts
    - aprendoclub/scripts/seed/globals.ts
  </read_first>
  <action>
    Correr el gate global. Contar ocurrencias de "real"/"reales" en copy visible (excluyendo comentarios `//`/`*` y rutas `/diplomado/real/`) sobre todos los archivos de seed-data. Debe ser 0.
    Verificar que no quedan frases de posicionamiento SEO objetivo: "academia de SEO" (quienes-somos), "Academia de SEO" (globals), "aprenden SEO" (home badge), "Especializate en SEO" (home subtitulo).
    Verificar la cifra de home: "10.000" aparece >= 2 veces en home.ts y no queda "+500 estudiantes".
    Si algun conteo falla, reportar el archivo/linea residual y detener (el plano correspondiente debe corregirse antes de sembrar).
  </action>
  <verify>
    <automated>cd aprendoclub && test $(grep -rniE "reales?\b" scripts/seed/seed-data/ scripts/seed/globals.ts | grep -vE ':\s*(//|\*)' | grep -v "/diplomado/real/" | wc -l) -eq 0 && test $(grep -rc "academia de SEO\|Academia de SEO\|aprenden SEO\|Especializate en SEO\|+500 estudiantes" scripts/seed/ | grep -v ':0$' | wc -l) -eq 0 && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - 0 ocurrencias de "real"/"reales" en copy visible en todo seed-data/ + globals.ts.
    - 0 ocurrencias de las frases de posicionamiento SEO objetivo listadas.
    - 0 ocurrencias de "+500 estudiantes" en el arbol de seed.
    - home.ts contiene "10.000" al menos 2 veces.
  </acceptance_criteria>
  <done>El gate global pasa: barrido de "real" completo y de-enfasis SEO aplicado en todas las ubicaciones objetivo.</done>
</task>

<task type="auto">
  <name>Task 2: Correr el seed para propagar a la base de datos</name>
  <files>aprendoclub (ejecucion de npm run seed; no modifica archivos fuente)</files>
  <read_first>
    - aprendoclub/package.json (confirmar script "seed": "payload run scripts/seed.ts")
    - aprendoclub/scripts/seed.ts (confirmar orden media -> collections -> globals -> pages)
  </read_first>
  <action>
    Desde el subdirectorio `aprendoclub/`, correr `npm run seed`. Requiere env de Postgres/Neon ya configurada localmente. El seed es idempotente. Confirmar en la salida que los pasos "Globals (site-settings)" y "Pages" terminan sin error.
    Si el comando falla por credenciales/conexion faltante, detener y reportar a Juan que el seed debe correrse en un entorno con .env de DB configurado (no inventar credenciales).
  </action>
  <verify>
    <automated>cd aprendoclub && npm run seed 2>&1 | tee /tmp/seed26.log | grep -qE "Pages|Globals|seed.*(done|complet)" && echo SEED_RAN</automated>
  </verify>
  <acceptance_criteria>
    - `npm run seed` termina con codigo 0 y la salida muestra los pasos Globals y Pages completados.
    - Ningun error de escritura/validacion en la salida del seed.
    - (Si falla por env faltante) se reporta claramente el bloqueo sin marcar la tarea como completada.
  </acceptance_criteria>
  <done>El seed corrio exitosamente y la DB refleja el nuevo copy del home, diplomado, quienes-somos, faqs, testimonios, reto y la meta global.</done>
</task>

</tasks>

<verification>
- Gate: `grep -rniE "reales?\b"` sobre seed-data/ + globals.ts (sin comentarios ni rutas /diplomado/real/) = 0.
- Seed: `npm run seed` finaliza sin error con pasos Globals y Pages OK.
</verification>

<success_criteria>
El barrido de la fase queda verificado globalmente (cero residuos de "real" y de-enfasis SEO aplicado) y la base de datos refleja los cambios tras un seed exitoso. Si el entorno no tiene credenciales de DB, el bloqueo se reporta a Juan sin simular exito.
</success_criteria>

<output>
Crear `.planning/phases/26-copy-general-home/26-04-SUMMARY.md` al terminar.
</output>
