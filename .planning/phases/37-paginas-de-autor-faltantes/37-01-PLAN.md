---
phase: 37-paginas-de-autor-faltantes
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - aprendoclub/scripts/seed-autores-faltantes.ts
  - .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv
  - .planning/deliverables/v1.8/redirects-phase33-cloudflare-noheader.csv
  - .planning/deliverables/v1.8/redirects-phase33-cloudflare-both-domains.csv
autonomous: true
requirements: [AUTHOR-01, AUTHOR-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Los autores Ibraim Zayed y Verónica Romero existen en la colección authors de Payload CMS con su nombre, slug, rol, bio y foto avatar."
    - "Las rutas /autor/ibraim-zayed y /autor/veronica-romero resuelven con HTTP 200 a través del catch-all [...slug] mostrando AuthorView."
    - "Los archivos CSV de Cloudflare en .planning/deliverables/v1.8/ contienen las reglas 301 de aprendoseo.com hacia aprendoclub.com para ambos autores."
  artifacts:
    - "aprendoclub/scripts/seed-autores-faltantes.ts"
    - ".planning/deliverables/v1.8/redirects-phase33-cloudflare.csv"
  key_links:
    - "Payload authors collection ↔ AUTHOR-01"
    - "Cloudflare CSVs ↔ AUTHOR-02"
---

<objective>
Crear los perfiles de autor para Ibraim Zayed y Verónica Romero en Payload CMS (Neon DB), de modo que sus páginas /autor/ibraim-zayed y /autor/veronica-romero se sirvan fielmente con el mismo formato que los autores existentes, y configurar sus redirecciones 301 en Cloudflare.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-paginas-de-autor-faltantes/37-CONTEXT.md
@aprendoclub/collections/Author.ts
@aprendoclub/components/blog/author-view.tsx
@.planning/deliverables/v1.8/redirects-phase33-cloudflare.csv
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Seed / Upsert de autores faltantes en Payload CMS (Neon DB)</name>
  <files>aprendoclub/scripts/seed-autores-faltantes.ts</files>
  <action>
    1. Crear `aprendoclub/scripts/seed-autores-faltantes.ts`:
       - Importar `getPayload` de `payload` y `config` de `../payload.config`.
       - Buscar o crear por `slug` en la colección `authors`:
         - `slug`: 'ibraim-zayed', `name`: 'Ibraim Zayed', `role`: 'SEO Coach y Community Builder', `bio`: 'Coach de SEO y creador de comunidad. Ha trabajado con 6 clientes en Estados Unidos y en dos agencias. Diseña estrategias para posicionar marcas en buscadores y redes.', `avatar`: 9 (ibraim.avif).
         - `slug`: 'veronica-romero', `name`: 'Verónica Romero', `role`: 'SEO Manager', `bio`: 'Content Manager con amplia experiencia en crecimiento orgánico. Ha trabajado con clientes como AMBL, Storybook y Papora con resultados destacados.', `avatar`: 11 (veronica.avif).
       - Asegurar `context: { disableRevalidate: true }` en create / update.
    2. Ejecutar `seed-autores-faltantes.ts` contra Neon DB.
  </action>
  <verify>
    <automated>bash -c '
node --env-file=aprendoclub/.env.local -e '\''
const { Client } = require("pg");
const c = new Client({ connectionString: process.env.DATABASE_URI });
async function check() {
  await c.connect();
  const res = await c.query("SELECT id, name, slug, role FROM authors WHERE slug IN (\x27ibraim-zayed\x27, \x27veronica-romero\x27);");
  console.log("Authors found:", res.rows);
  if (res.rows.length !== 2) process.exit(1);
  await c.end();
}
check();
'\''
'</automated>
  </verify>
  <done>Los autores Ibraim Zayed y Verónica Romero están persistidos en Neon DB.</done>
</task>

<task type="auto">
  <name>Task 2: Agregar redirecciones 301 de autores a Cloudflare CSVs</name>
  <files>.planning/deliverables/v1.8/redirects-phase33-cloudflare.csv, .planning/deliverables/v1.8/redirects-phase33-cloudflare-noheader.csv, .planning/deliverables/v1.8/redirects-phase33-cloudflare-both-domains.csv</files>
  <action>
    1. En `.planning/deliverables/v1.8/redirects-phase33-cloudflare.csv` y sus variantes:
       - Agregar las redirecciones de `https://www.aprendoseo.com/autor/ibraim-zayed` a `https://www.aprendoclub.com/autor/ibraim-zayed`.
       - Agregar las redirecciones de `https://www.aprendoseo.com/autor/veronica-romero` a `https://www.aprendoclub.com/autor/veronica-romero`.
  </action>
  <verify>
    <automated>bash -c '
grep -q "autor/ibraim-zayed" .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv || { echo "FAIL_IBRAIM"; exit 1; }
grep -q "autor/veronica-romero" .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv || { echo "FAIL_VERONICA"; exit 1; }
echo AUTHORS_REDIRECTS_OK
'</automated>
  </verify>
  <done>Redirecciones 301 de autores configuradas en todos los archivos CSV de Cloudflare.</done>
</task>

<task type="auto">
  <name>Task 3: Verificación de compilación TypeScript y resolución de rutas de autor</name>
  <files>aprendoclub/scripts/seed-autores-faltantes.ts</files>
  <action>
    1. Ejecutar `npx tsc --noEmit` en `aprendoclub/`.
    2. Probar mediante script de prueba que `findAuthorBySlug` retorna correctamente los autores recién insertados.
  </action>
  <verify>
    <automated>bash -c '
cd aprendoclub && npx tsc --noEmit || { echo "FAIL_TSC"; exit 1; }
echo "TSC_OK"
'</automated>
  </verify>
  <done>TypeScript compila limpiamente y la resolución de autores está garantizada.</done>
</task>

</tasks>
