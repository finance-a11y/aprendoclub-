---
phase: 34-reestructura-de-urls-de-programas
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - aprendoclub/next.config.ts
  - aprendoclub/components/blog/post-cta.tsx
  - aprendoclub/lib/schema-mappers.ts
  - aprendoclub/lib/llms/seed.ts
  - aprendoclub/scripts/seed/collections.ts
  - aprendoclub/scripts/seed/globals.ts
  - aprendoclub/scripts/seed/pages.ts
  - aprendoclub/scripts/seed/rewrite-links.ts
  - aprendoclub/scripts/migrate-program-slugs.ts
autonomous: true
requirements: [RESTRUCT-01, RESTRUCT-02, RESTRUCT-03]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Visitar /reto en aprendoclub redirige vía 301 a /programas/reto."
    - "Visitar /diplomado en aprendoclub redirige vía 301 a /programas/diplomado."
    - "Las páginas /programas/reto y /programas/diplomado resuelven en Next.js con HTTP 200 y renderizan el contenido de Payload."
    - "Todos los enlaces internos en el código y en Payload (navbar, footer, cards de home, CTAs) apuntan a /programas/reto y /programas/diplomado."
    - "Los datos estructurados JSON-LD de schema-mappers reconocen los nuevos slugs programas/reto y programas/diplomado."
  artifacts:
    - "aprendoclub/next.config.ts"
    - "aprendoclub/scripts/migrate-program-slugs.ts"
    - "aprendoclub/components/blog/post-cta.tsx"
    - "aprendoclub/lib/schema-mappers.ts"
  key_links:
    - "next.config.ts redirects ↔ RESTRUCT-02"
    - "Payload pages slugs ↔ RESTRUCT-01"
    - "Internal links and globals ↔ RESTRUCT-03"
---

<objective>
Reestructurar las URLs de los programas Reto y Diplomado moviéndolas de sus rutas históricas (`/reto` y `/diplomado`) a sus rutas canónicas anidadas (`/programas/reto` y `/programas/diplomado`), consistentes con `/programas/taller-seo-con-ia`.
Configurar redirecciones 301 en `next.config.ts`, migrar los slugs en la base de datos de Payload (Neon Postgres), y actualizar todos los enlaces internos del código y de las colecciones globales.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-reestructura-de-urls-de-programas/34-CONTEXT.md
@aprendoclub/next.config.ts
@aprendoclub/lib/schema-mappers.ts
@aprendoclub/components/blog/post-cta.tsx
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Redirecciones 301 en next.config.ts y actualización de referencias en código</name>
  <files>aprendoclub/next.config.ts, aprendoclub/components/blog/post-cta.tsx, aprendoclub/lib/schema-mappers.ts, aprendoclub/lib/llms/seed.ts, aprendoclub/scripts/seed/collections.ts, aprendoclub/scripts/seed/globals.ts, aprendoclub/scripts/seed/pages.ts, aprendoclub/scripts/seed/rewrite-links.ts</files>
  <action>
    1. En `aprendoclub/next.config.ts`, agregar dentro del array retornado por `redirects()`:
       `{ source: "/reto", destination: "/programas/reto", permanent: true }`
       `{ source: "/diplomado", destination: "/programas/diplomado", permanent: true }`
    2. En `aprendoclub/components/blog/post-cta.tsx`:
       Cambiar `<Button href="/diplomado">` por `<Button href="/programas/diplomado">`.
    3. En `aprendoclub/lib/schema-mappers.ts`:
       Actualizar `COURSES` para que las claves sean `"programas/diplomado"` y `"programas/reto"`, con sus `path` apuntando a `"/programas/diplomado"` y `"/programas/reto"`.
       En `getGraphsForSlug`, actualizar el bloque switch para evaluar `case "programas/diplomado":` y `case "programas/reto":` (conservando `"diplomado"` y `"reto"` como fallback).
    4. En `aprendoclub/lib/llms/seed.ts`:
       Actualizar las URLs de diplomado y reto a `https://www.aprendoclub.com/programas/diplomado` y `https://www.aprendoclub.com/programas/reto`.
    5. En `aprendoclub/scripts/seed/collections.ts`, `globals.ts`, `pages.ts` y `rewrite-links.ts`:
       Actualizar las rutas `/reto` y `/diplomado` a `/programas/reto` y `/programas/diplomado`.
  </action>
  <verify>
    <automated>bash -c '
grep -q "destination: \"/programas/reto\"" aprendoclub/next.config.ts || { echo "FAIL_RETO_REDIRECT"; exit 1; }
grep -q "destination: \"/programas/diplomado\"" aprendoclub/next.config.ts || { echo "FAIL_DIPLOMADO_REDIRECT"; exit 1; }
grep -q "href=\"/programas/diplomado\"" aprendoclub/components/blog/post-cta.tsx || { echo "FAIL_POST_CTA"; exit 1; }
grep -q "\"programas/diplomado\":" aprendoclub/lib/schema-mappers.ts || { echo "FAIL_SCHEMA_DIPLOMADO"; exit 1; }
grep -q "\"programas/reto\":" aprendoclub/lib/schema-mappers.ts || { echo "FAIL_SCHEMA_RETO"; exit 1; }
echo TRACER_CODE_OK
'</automated>
  </verify>
  <done>next.config.ts redirige 301 /reto y /diplomado a /programas/*, y las referencias estáticas en componentes y schemas están actualizadas.</done>
</task>

<task type="auto">
  <name>Task 2: Script de migración y actualización en Payload CMS (Neon DB)</name>
  <files>aprendoclub/scripts/migrate-program-slugs.ts</files>
  <action>
    1. Crear `aprendoclub/scripts/migrate-program-slugs.ts` para ejecutar mediante `payload run` o tsx con variables de entorno cargadas:
       - Importar `getPayload` de `payload` y `config` de `../payload.config`.
       - Buscar en la colección `pages` los documentos con `slug: 'diplomado'` y `slug: 'reto'`.
       - Actualizar cada uno a `slug: 'programas/diplomado'` y `slug: 'programas/reto'`.
       - En la colección `programas`, actualizar los documentos cuyo `ctaHref` sea `'/diplomado'` o `'/reto'` hacia `'/programas/diplomado'` y `'/programas/reto'`.
       - En el global `site-settings`, actualizar los enlaces de `programMenu` y `footerColumns` para que apunten a `/programas/diplomado` y `/programas/reto`.
       - Invocar `payload.destroy()` y finalizar con código 0.
    2. Ejecutar el script contra la base de datos de Neon:
       `node --env-file=aprendoclub/.env.local -e 'const { spawnSync } = require("child_process"); spawnSync("npx", ["tsx", "scripts/migrate-program-slugs.ts"], { cwd: "aprendoclub", stdio: "inherit", env: process.env });'`
    3. Re-consultar los slugs de `pages` usando `aprendoclub/scripts/query-payload-slugs.ts` para verificar que `programas/diplomado` y `programas/reto` están presentes en Neon.
  </action>
  <verify>
    <automated>bash -c '
node --env-file=aprendoclub/.env.local -e '\''
const { spawnSync } = require("child_process");
const res = spawnSync("npx", ["tsx", "scripts/query-payload-slugs.ts"], { cwd: "aprendoclub", encoding: "utf8", env: process.env });
const out = res.stdout || "";
if (!out.includes("programas/diplomado") || !out.includes("programas/reto")) {
  console.error("FAIL: missing updated slugs in Payload", out);
  process.exit(1);
}
console.log("PAYLOAD_SLUGS_OK");
'\''
'</automated>
  </verify>
  <done>Las páginas en Payload tienen los slugs programas/diplomado y programas/reto, y los globals y colecciones de programas apuntan a las nuevas URLs.</done>
</task>

<task type="auto">
  <name>Task 3: Verificación de compilación, TypeScript y comprobación de rutas</name>
  <files>aprendoclub/next.config.ts</files>
  <action>
    1. Ejecutar verificación de TypeScript (`npx tsc --noEmit`) en `aprendoclub/` para confirmar que no se introdujo ningún error de tipos en `schema-mappers.ts`, `post-cta.tsx` ni en los scripts.
    2. Validar que no existan enlaces residuales indeseados apuntando a `/diplomado` o `/reto` en componentes de la aplicación.
    3. Documentar los cambios en el resumen de la fase.
  </action>
  <verify>
    <automated>bash -c '
cd aprendoclub && npx tsc --noEmit || { echo "FAIL_TSC"; exit 1; }
echo TSC_OK
'</automated>
  </verify>
  <done>TypeScript compila limpiamente sin errores y la estructura de URLs queda validada.</done>
</task>

</tasks>
