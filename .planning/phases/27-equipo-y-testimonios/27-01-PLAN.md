---
phase: 27-equipo-y-testimonios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aprendoclub/scripts/seed/seed-data/quienes-somos.ts
  - aprendoclub/scripts/seed/pages.ts
  - aprendoclub/scripts/seed/collections.ts
  - aprendoclub/scripts/seed/seed-data/testimonios.ts
autonomous: true
requirements: [TEAM-01, TESTIM-01]

must_haves:
  truths:
    - "El grid de equipo en /quienes-somos muestra 'Dana Aliaga' con avatar de iniciales 'DA' (sin foto rota) en el mismo slot que ocupaba Diana"
    - "'Diana Rodríguez' y 'diana.avif' no aparecen en ninguna página renderizada del sitio"
    - "La sección de logos de empresas confiadas ya no se renderiza en /testimonios"
    - "La colección Payload ClientesTrabajados sigue intacta y poblada (trustedCompanies conservado como fuente de seed)"
    - "El seed corre exitosamente contra Neon y el proyecto compila sin imports rotos"
  artifacts:
    - path: "aprendoclub/scripts/seed/seed-data/quienes-somos.ts"
      provides: "Entrada de equipo de Dana Aliaga sin foto"
      contains: "Dana Aliaga"
    - path: "aprendoclub/scripts/seed/pages.ts"
      provides: "QUIENES_SOMOS_TEAM_ORDER con Dana; buildTestimonios sin bloque logosRef"
      contains: "Dana Aliaga"
    - path: "aprendoclub/scripts/seed/collections.ts"
      provides: "Registro único y coherente de Dana Aliaga; cleanup del registro huérfano de Diana"
      contains: "Dana Aliaga"
    - path: "aprendoclub/scripts/seed/seed-data/testimonios.ts"
      provides: "logosBanda eliminado; trustedCompanies + interface TrustedCompany conservados"
      contains: "trustedCompanies"
  key_links:
    - from: "quienes-somos.ts equipo[].nombre 'Dana Aliaga'"
      to: "pages.ts QUIENES_SOMOS_TEAM_ORDER"
      via: "idsFor(maps.teamMembers, QUIENES_SOMOS_TEAM_ORDER)"
      pattern: "Dana Aliaga"
    - from: "testimonios.ts trustedCompanies"
      to: "collections.ts clientes-trabajados + media.ts manifest"
      via: "import { trustedCompanies }"
      pattern: "trustedCompanies"
---

<objective>
Sincronizar equipo y testimonios con el estado actual del negocio: reemplazar a Diana Rodríguez por Dana Aliaga en el grid de /quienes-somos (avatar de iniciales, sin foto) y eliminar la sección de logos de empresas confiadas de /testimonios, sin tocar la colección Payload ClientesTrabajados.

Purpose: Cerrar TEAM-01 y TESTIM-01 del milestone v1.6 (rediseño de copy). El equipo mostrado y la sección de testimonios deben reflejar la realidad vigente.
Output: Ediciones en 4 archivos de seed + re-seed a Neon + gate de verificación.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-equipo-y-testimonios/27-CONTEXT.md

<discovered_facts>
El equipo de planificación leyó el código real. Hechos verificados que el executor
debe respetar (NO re-descubrir ni contradecir):

1. **El render de equipo es por lista de nombres, no por flag.**
   `pages.ts` renderiza el grid con `idsFor(maps.teamMembers, QUIENES_SOMOS_TEAM_ORDER)`
   (línea ~460) y el de /diplomado con `idsFor(maps.teamMembers, DIPLOMADO_TEAM_ORDER)`
   (línea ~283). Cambiar solo el array `equipo` en quienes-somos.ts NO basta:
   `QUIENES_SOMOS_TEAM_ORDER` (pages.ts ~línea 43) lista "Diana Rodríguez" literalmente
   y DEBE cambiarse a "Dana Aliaga", o el slot desaparece del grid.

2. **Ya existe un registro "Dana Aliaga".** `collections.ts` tiene un array
   `DIPLOMADO_TEAM_ONLY` (~línea 105) que crea a "Dana Aliaga" para el grid del
   diplomado (mostrarEnDiplomado:true, mostrarEnQuienesSomos:false). El upsert es por
   `nombre`. Si el array `equipo` ahora también contiene "Dana Aliaga", el loop de
   `equipo` (que corre primero) y el loop de `DIPLOMADO_TEAM_ONLY` (que corre después)
   upsertan el MISMO doc por nombre; el segundo sobrescribe `mostrarEnQuienesSomos` a
   false. Hay que reconciliar (ver Task 1) para que Dana sea UN registro coherente
   mostrado en ambas páginas.

3. **Contrato del fallback de foto (TeamGridRef.tsx, verificado):**
   El componente hace `const foto = resolveMedia(member.foto)` y luego
   `foto ? <Image> : <div>{member.iniciales}</div>`. En el seed,
   `collections.ts` (~línea 224) hace `foto: m.foto ? (mediaMap.get(m.foto) ?? undefined) : undefined`.
   Para Dana sin foto: OMITIR la clave `foto` del objeto (interface `foto?: string`).
   `media.ts` línea ~57 hace `for (const m of equipo) add(m.foto, ...)` y `add` tiene
   guard `if (!p) return`, así que un `foto` undefined es un no-op seguro. NO reusar
   diana.avif, NO apuntar a un archivo inexistente, NO usar string vacío.

4. **trustedCompanies NO es "código no usado" — NO borrarlo.**
   Aunque el phase brief pedía "borrar el array trustedCompanies", el planner verificó
   que lo importan DOS consumidores vivos:
   - `collections.ts` (líneas 6 y ~183): siembra la colección `clientes-trabajados`.
   - `media.ts` (líneas 12 y ~54): agrega los logos al manifest de Media.
   La decisión LOCKED de CONTEXT.md es "NO tocar la colección Payload ClientesTrabajados
   — queda intacta". Borrar `trustedCompanies` rompería la compilación y vaciaría el
   seed de esa colección. Resolución: CONSERVAR `trustedCompanies` y la interface
   `TrustedCompany`. Solo `logosBanda` (export ~línea 90) queda genuinamente huérfano
   tras quitar el bloque `logosRef`, y ese sí se elimina.

5. **Ámbito real de TESTIM-01 en pages.ts:** el bloque `logosRef` está en
   `buildTestimonios()` (~líneas 504-512). Sus imports desde ./seed-data/testimonios
   son `logosBanda` (~línea 28) y `trustedCompanies` (~línea 31); ambos quedan sin uso
   en pages.ts tras quitar el bloque y deben removerse del import. `retoImagenes`
   (mismo import) SIGUE usándose (~línea 518): conservarlo.
</discovered_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TEAM-01 — Diana Rodríguez → Dana Aliaga (datos, orden y reconciliación)</name>
  <files>aprendoclub/scripts/seed/seed-data/quienes-somos.ts, aprendoclub/scripts/seed/pages.ts, aprendoclub/scripts/seed/collections.ts</files>
  <read_first>
    - aprendoclub/scripts/seed/seed-data/quienes-somos.ts (array `equipo`, entrada de Diana ~líneas 122-128; interface TeamMember ~líneas 38-47)
    - aprendoclub/scripts/seed/pages.ts (QUIENES_SOMOS_TEAM_ORDER ~líneas 40-47; DIPLOMADO_TEAM_ORDER ~líneas 49-56)
    - aprendoclub/scripts/seed/collections.ts (DIPLOMADO_TEAM_ONLY ~líneas 99-112; SHARED_WITH_DIPLOMADO ~líneas 114-120; loop equipo ~líneas 218-232; loop DIPLOMADO_TEAM_ONLY ~líneas 233-244; console.log ~línea 245)
    - aprendoclub/components/blocks/render/TeamGridRef.tsx (contrato de fallback foto→iniciales, ya verificado en discovered_facts #3)
  </read_first>
  <action>
    Implementa TEAM-01 respetando la decisión LOCKED de CONTEXT.md (Dana sin foto, avatar de iniciales "DA", mantener rol y bio).

    A) En `quienes-somos.ts`, en el array `equipo`, editar la entrada de "Diana Rodríguez":
       - `nombre`: "Diana Rodríguez" → "Dana Aliaga"
       - `iniciales`: "DR" → "DA"
       - ELIMINAR por completo la clave `foto` (estaba "/coaches/diana.avif"). No dejar
         string vacío ni undefined explícito: simplemente quitar la propiedad.
       - `rol`: mantener "SEO Specialist" verbatim.
       - `bio`: mantener "Especialista en SEO, enfocada en ejecución y resultados." verbatim.
       - Conservar la posición del objeto en el array (índice 1, mismo slot que Diana).

    B) En `pages.ts`, en `QUIENES_SOMOS_TEAM_ORDER`, cambiar "Diana Rodríguez" → "Dana Aliaga".
       `DIPLOMADO_TEAM_ORDER` ya lista "Dana Aliaga": dejarlo intacto.

    C) En `collections.ts`, reconciliar Dana como UN registro coherente mostrado en
       quienes-somos Y diplomado, y limpiar el residuo de Diana:
       - Agregar 'Dana Aliaga' al set `SHARED_WITH_DIPLOMADO` (para que el loop de `equipo`
         setee `mostrarEnDiplomado: true` en Dana).
       - Eliminar la entrada "Dana Aliaga" de `DIPLOMADO_TEAM_ONLY` (evita el segundo upsert
         que sobrescribiría `mostrarEnQuienesSomos` a false). Si `DIPLOMADO_TEAM_ONLY` queda
         vacío, eliminar el const y su loop (~líneas 233-244) por ser código muerto, o
         dejarlo como `[]`; no dejar comentarios contradictorios.
       - Actualizar el comentario obsoleto (~líneas 99-104) que afirma "Dana Aliaga es una
         persona real DISTINTA de Diana Rodríguez — no deduplicar": ahora Diana queda
         reemplazada por Dana. Reescribir en neutral, breve.
       - Actualizar el mensaje del `console.log` (~línea 245) que dice
         "(incl. Diana Rodríguez y Dana Aliaga como registros distintos)".
       - Agregar un cleanup idempotente del registro huérfano de Diana ANTES del loop de
         `equipo` dentro de `seedCollections`: eliminar cualquier doc de la colección
         'team-members' con `nombre` igual a "Diana Rodríguez" usando la Local API de Payload
         (`payload.delete({ collection: 'team-members', where: { nombre: { equals: 'Diana Rodríguez' } } })`,
         con `context: { disableRevalidate: true }` como el resto del seed). Es no-op si no
         existe; hace el re-seed self-correcting para bases ya sembradas con Diana.
  </action>
  <verify>
    <automated>cd aprendoclub && grep -rn "Diana Rodríguez\|diana.avif" scripts/seed/seed-data/quienes-somos.ts scripts/seed/pages.ts; test $(grep -rc "Diana Rodríguez\|diana.avif" scripts/seed/seed-data/quienes-somos.ts scripts/seed/pages.ts | grep -v ':0$' | wc -l | tr -d ' ') -eq 0 && grep -q "Dana Aliaga" scripts/seed/seed-data/quienes-somos.ts && grep -q "Dana Aliaga" scripts/seed/pages.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `equipo` en quienes-somos.ts contiene "Dana Aliaga" con `iniciales: "DA"` y SIN clave `foto`.
    - No aparece "Diana Rodríguez" ni "diana.avif" en quienes-somos.ts ni en pages.ts.
    - `QUIENES_SOMOS_TEAM_ORDER` contiene "Dana Aliaga" y no "Diana Rodríguez".
    - `SHARED_WITH_DIPLOMADO` incluye 'Dana Aliaga'; `DIPLOMADO_TEAM_ONLY` ya no contiene a Dana.
    - Existe el cleanup idempotente que elimina el team-member "Diana Rodríguez" de la DB.
    - Comentario y console.log obsoletos sobre "registros distintos" actualizados.
  </acceptance_criteria>
  <done>Dana Aliaga reemplaza a Diana en datos, orden de render y colección; un solo registro coherente mostrado en quienes-somos y diplomado; residuo de Diana limpiado.</done>
</task>

<task type="auto">
  <name>Task 2: TESTIM-01 — Quitar sección de logos de /testimonios</name>
  <files>aprendoclub/scripts/seed/pages.ts, aprendoclub/scripts/seed/seed-data/testimonios.ts</files>
  <read_first>
    - aprendoclub/scripts/seed/pages.ts (import de ./seed-data/testimonios ~líneas 24-35; buildTestimonios ~líneas 482-530, bloque logosRef ~líneas 504-512)
    - aprendoclub/scripts/seed/seed-data/testimonios.ts (export `logosBanda` ~líneas 89-90; `trustedCompanies` + interface `TrustedCompany` ~líneas 29-33 y 291-297)
    - discovered_facts #4 y #5 del contexto (por qué trustedCompanies se CONSERVA)
  </read_first>
  <action>
    Eliminar el consumo de la banda de logos en la página /testimonios, SIN tocar la
    colección Payload ClientesTrabajados ni su fuente de seed.

    A) En `pages.ts`:
       - Eliminar por completo el bloque `{ blockType: 'logosRef', ... }` del array `layout`
         de `buildTestimonios()` (~líneas 504-512), incluida la llamada a `idsFor(maps.clientes, ...)`.
         No dejar el bloque vacío.
       - En el import desde './seed-data/testimonios', quitar `logosBanda` y `trustedCompanies`
         (ambos quedan sin uso en pages.ts). CONSERVAR `retoImagenes` (sigue usándose ~línea 518)
         y el resto de los símbolos importados.

    B) En `testimonios.ts`:
       - Eliminar el export `logosBanda` (~líneas 89-90) — su único consumidor era el bloque
         logosRef que se acaba de quitar.
       - CONSERVAR intactos el array `trustedCompanies` (~líneas 291-297) y la interface
         `TrustedCompany` (~líneas 29-33): los siguen importando collections.ts y media.ts para
         sembrar la colección clientes-trabajados. NO borrarlos (contradice la decisión LOCKED
         de CONTEXT.md de no tocar ClientesTrabajados).

    NOTA: si el brief textual pedía "borrar trustedCompanies", ese pedido asumía que estaba
    sin uso, lo cual es falso (ver discovered_facts #4). Se honra la decisión LOCKED superior.
  </action>
  <verify>
    <automated>cd aprendoclub && test $(grep -c "blockType: 'logosRef'" scripts/seed/pages.ts) -eq 0 && test $(grep -c "^export const logosBanda" scripts/seed/seed-data/testimonios.ts) -eq 0 && grep -q "export const trustedCompanies" scripts/seed/seed-data/testimonios.ts && grep -q "interface TrustedCompany" scripts/seed/seed-data/testimonios.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `buildTestimonios()` en pages.ts ya no contiene ningún bloque `logosRef`.
    - El import de pages.ts desde ./seed-data/testimonios ya no incluye `logosBanda` ni `trustedCompanies`; sigue incluyendo `retoImagenes`.
    - El export `logosBanda` fue eliminado de testimonios.ts.
    - `trustedCompanies` y la interface `TrustedCompany` siguen presentes en testimonios.ts.
    - collections.ts y media.ts siguen importando `trustedCompanies` sin cambios.
  </acceptance_criteria>
  <done>La página /testimonios ya no construye el bloque de logos; código huérfano (logosBanda) eliminado; la colección ClientesTrabajados y su fuente de seed intactas.</done>
</task>

<task type="auto">
  <name>Task 3: Re-seed a Neon + gate de verificación</name>
  <files>(sin ediciones — ejecución + verificación)</files>
  <read_first>
    - aprendoclub/package.json (script "seed": "payload run scripts/seed.ts")
    - .planning/STATE.md (precedente Phase 26: `npm run seed` corrió contra Neon con env vars de .env.local ya configuradas)
  </read_first>
  <action>
    Propagar los cambios a Postgres/Neon y verificar el estado final.

    A) Compilación: correr `npx tsc --noEmit` desde `aprendoclub/` para confirmar que no
       quedaron imports rotos (p.ej. logosBanda/trustedCompanies removidos de pages.ts).
       Si falla por imports de este cambio, corregir; si falla por errores preexistentes
       ajenos a estos 4 archivos, reportarlo pero no bloquear el seed.

    B) Re-seed: correr `npm run seed` desde el subdirectorio `aprendoclub/`. Las env vars
       (DATABASE_URI / conexión Neon) ya están en `.env.local` por precedente de Phase 26 —
       NO inventar credenciales. Si el seed falla por credenciales o conexión, DETENERSE y
       reportar el error exacto a Juan; no improvisar.

    C) Gate de verificación (grep sobre fuente, ya cubierto por Tasks 1-2, se re-confirma):
       - "Diana Rodríguez" y "diana.avif" ausentes de scripts/seed/seed-data/quienes-somos.ts.
       - "logosRef" ausente del array de bloques de buildTestimonios en scripts/seed/pages.ts.
       - "Dana Aliaga" presente en quienes-somos.ts y en QUIENES_SOMOS_TEAM_ORDER de pages.ts.
       - trustedCompanies aún presente en testimonios.ts.
  </action>
  <verify>
    <automated>cd aprendoclub && test $(grep -c "Diana Rodríguez\|diana.avif" scripts/seed/seed-data/quienes-somos.ts) -eq 0 && test $(grep -c "blockType: 'logosRef'" scripts/seed/pages.ts) -eq 0 && grep -q "Dana Aliaga" scripts/seed/pages.ts && grep -q "export const trustedCompanies" scripts/seed/seed-data/testimonios.ts && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` no reporta errores nuevos derivados de los 4 archivos editados.
    - `npm run seed` termina exitosamente contra Neon (o se reporta el fallo exacto sin inventar credenciales).
    - El gate de grep pasa: sin Diana/diana.avif/logosRef; con Dana Aliaga y trustedCompanies.
  </acceptance_criteria>
  <done>Cambios propagados a Neon; proyecto compila; gate de verificación en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → Neon/Postgres | El seed escribe en la DB de producción usando credenciales locales (.env.local). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | `npm run seed` sobre Neon | accept | Solo ediciones de contenido idempotentes (upsert por clave natural + delete idempotente de un doc). Sin input externo no confiable. Precedente Phase 26 con las mismas credenciales. |
| T-27-02 | Denial | Borrado de datos en team-members | mitigate | El único delete apunta explícitamente a `nombre = "Diana Rodríguez"`; no hay deletes masivos ni por patrón amplio. |

Sin instalación de paquetes (npm/pip/cargo) en esta fase → no aplica gate de legitimidad de paquetes ni checkpoint T-{phase}-SC.
</threat_model>

<verification>
- Grep gate: sin "Diana Rodríguez"/"diana.avif" en quienes-somos.ts; sin "logosRef" en buildTestimonios (pages.ts).
- Presencia: "Dana Aliaga" en quienes-somos.ts, en QUIENES_SOMOS_TEAM_ORDER y en collections.ts.
- Integridad: `trustedCompanies` + interface `TrustedCompany` conservados; collections.ts y media.ts compilan.
- `npx tsc --noEmit` sin errores nuevos; `npm run seed` exitoso contra Neon.
</verification>

<success_criteria>
1. /quienes-somos muestra a "Dana Aliaga" (avatar de iniciales "DA") en el slot de Diana; Diana Rodríguez no aparece en el sitio renderizado.
2. /testimonios ya no renderiza la banda de logos de empresas confiadas.
3. La colección Payload ClientesTrabajados queda intacta y poblada.
4. El seed corre exitosamente contra Neon y el proyecto compila.
</success_criteria>

<output>
Create `.planning/phases/27-equipo-y-testimonios/27-01-SUMMARY.md` when done
</output>
