---
phase: 33-redirects-301-para-contenido-existente
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv
  - .planning/deliverables/v1.8/README-cloudflare-import.md
  - aprendoclub/scripts/query-payload-slugs.ts
autonomous: true
requirements: [REDIR-01, REDIR-02, REDIR-03, REDIR-04]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "El CSV en .planning/deliverables/v1.8/ contiene todas las filas 'Aplicar redireccion 301' del sheet cuyo destino existe hoy en Payload, con columnas source_url,target_url,status_code,preserve_query_string."
    - "Las filas de /reto y /diplomado apuntan a /reto y /diplomado (URLs activas hoy en aprendoclub), difiriendo la redirección a /programas/* como redirección interna para cuando Phase 34 reestructure las páginas."
    - "Los 2 redirects con nota de merge (certificaciones, academia-seo) están como redirect simple en el CSV, sin combinar contenido de páginas."
    - "Cada target_url del CSV final fue verificado contra un volcado en vivo de Payload (pages/authors/categories/blogposts) en el momento de ejecución de la fase, no contra el snapshot de una sesión anterior."
    - "La fila de /prensa y la fila del evento de ads no aparecen en el CSV final."
    - "Existe un README en español neutro explicando a Juan cómo importar el CSV en Cloudflare Bulk Redirects para la zona aprendoseo.com."
  artifacts:
    - ".planning/deliverables/v1.8/redirects-phase33-cloudflare.csv"
    - ".planning/deliverables/v1.8/README-cloudflare-import.md"
    - "aprendoclub/scripts/query-payload-slugs.ts"
  key_links:
    - "target_url del CSV ↔ slugs confirmados en vivo en Payload (pages/authors/categories/blogposts)"
    - "filas reto/diplomado ↔ dependencia explícita de Phase 34 (ROADMAP.md: Phase 34 depende de 33 precisamente por estas dos filas)"
    - "pull en vivo del sheet (WebFetch) ↔ snapshot de 33-SHEET-DATA.md (cualquier drift documentado en el SUMMARY)"
---

<objective>
Generar la lista completa de redirects 301 de aprendoseo.com → aprendoclub.com para todo el contenido que ya existe hoy en aprendoclub (o existirá en Phase 34), en un CSV listo para importar en Cloudflare Bulk Redirects — verificado fila por fila contra el sheet de mapeo de Juan en vivo y contra la base de datos de Payload en vivo, no contra el snapshot de la sesión anterior guardado en 33-SHEET-DATA.md.

Purpose: es la entrega de mayor prioridad del milestone v1.8 según decisión explícita de Juan — sin este CSV no hay forma de proteger el SEO de aprendoseo.com mientras avanza el resto de la migración.
Output: `.planning/deliverables/v1.8/redirects-phase33-cloudflare.csv` (el CSV importable), `.planning/deliverables/v1.8/README-cloudflare-import.md` (instrucciones de importación), `aprendoclub/scripts/query-payload-slugs.ts` (script de consulta de solo lectura a Payload, reutilizable en Phases 35-38 para las siguientes tandas de redirects).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-redirects-301-para-contenido-existente/33-CONTEXT.md
@.planning/phases/33-redirects-301-para-contenido-existente/33-SHEET-DATA.md
@aprendoclub/scripts/verify-media-alt.ts
@aprendoclub/next.config.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Extremo a extremo — sheet en vivo → Payload en vivo → primera fila validada del CSV</name>
  <files>aprendoclub/scripts/query-payload-slugs.ts, .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv</files>
  <precondition>Neon Postgres debe estar respondiendo (STATE.md registra inestabilidad ECONNRESET intermitente en v1.7) — si `npx payload run` falla por conexión, detener y reportarlo en vez de seguir generando el CSV sin verificación en vivo.</precondition>
  <action>
    Crear `aprendoclub/scripts/query-payload-slugs.ts` siguiendo el mismo patrón que `aprendoclub/scripts/verify-media-alt.ts` y `aprendoclub/scripts/list-users.ts` (script de solo lectura, sin ninguna escritura a Payload): importar `getPayload` de `payload` y `config` de `../payload.config`, y usar top-level `await` para invocar la función principal — sin eso `payload run` termina el proceso antes de que el script haga nada. El script debe llamar `payload.find` con `overrideAccess: true` sobre las colecciones `pages`, `authors`, `categories` (depth 0, limit 200 cada una) y `blogposts` (depth 1, limit 200, para que el campo de categoría venga poblado con su slug en vez de solo el ID); imprimir con `console.log(JSON.stringify(...))` un único objeto con las cuatro listas de slugs (para blogposts, par slug + slug de categoría); cerrar con `payload.destroy()` y `process.exit(0)`.
    Ejecutarlo parado en `aprendoclub/` con `npx payload run scripts/query-payload-slugs.ts` — nunca con `tsx` directo, porque `payload.config.ts` no carga `.env.local` por sí solo y necesita el wrapper `payload run` (ver 33-CONTEXT.md).
    Hacer WebFetch de la URL de exportación CSV del sheet en vivo, `https://docs.google.com/spreadsheets/d/1Iuy2kJE0Og_0ZUuk1gfSLTcmXkP1wBXfNvy0eVAHpzw/export?format=csv&gid=626359358`, y confirmar que la fila cuyo origen es `https://www.aprendoseo.com/curso-seo-con-ia/` sigue marcada `Aplicar redireccion 301` con destino `/programas/taller-seo-con-ia`.
    Crear `.planning/deliverables/v1.8/redirects-phase33-cloudflare.csv` con el header exacto `source_url,target_url,status_code,preserve_query_string` seguido de una única fila de datos que use el origen y destino confirmados en el paso anterior, con `status_code` 301 y `preserve_query_string` true.
    Este primer archivo es intencionalmente parcial (una sola fila): su único propósito es probar que las tres capas — fetch del sheet en vivo, consulta a Payload en vivo, escritura del CSV — funcionan juntas de punta a punta antes de generar el set completo en la Tarea 2.
  </action>
  <verify>
    <automated>bash -c '
CSV=".planning/deliverables/v1.8/redirects-phase33-cloudflare.csv"
test "$(sed -n "1p" "$CSV")" = "source_url,target_url,status_code,preserve_query_string" || { echo FAIL_HEADER; exit 1; }
ROWS=$(awk "END{print NR}" "$CSV")
[ "$ROWS" -eq 2 ] || { echo "FAIL_ROWS:$ROWS"; exit 1; }
grep -qx "https://www.aprendoseo.com/curso-seo-con-ia/,https://www.aprendoclub.com/programas/taller-seo-con-ia,301,true" "$CSV" || { echo FAIL_ROW_CONTENT; exit 1; }
(cd aprendoclub && npx payload run scripts/query-payload-slugs.ts) | grep -q "programas/taller-seo-con-ia" || { echo FAIL_PAYLOAD_SLUG; exit 1; }
echo TRACER_OK
'</automated>
  </verify>
  <done>El script de Payload corre sin errores vía `npx payload run` y confirma `programas/taller-seo-con-ia` en las páginas en vivo; el sheet en vivo confirma la fila de origen; el CSV existe con el header correcto y esa única fila validada extremo a extremo.</done>
</task>

<task type="auto">
  <name>Task 2: CSV completo — todas las filas verificadas contra el sheet y Payload en vivo</name>
  <files>.planning/deliverables/v1.8/redirects-phase33-cloudflare.csv</files>
  <action>
    Releer el sheet completo en vivo (mismo WebFetch de la Tarea 1) y usar 33-SHEET-DATA.md solo como referencia de comparación para detectar drift, no como fuente ciega. Aplicar estas reglas para construir el resto del CSV (la fila de la Tarea 1 queda incluida en el set final):
    1. Incluir solamente las filas cuya columna `accion` sea exactamente "Aplicar redireccion 301". Esto excluye automáticamente las filas marcadas "Crear Página nueva y redirigir" (se resuelven en Phases 35-38), la fila marcada "eliminar (sin valor)", la fila marcada "Página para ads" (se resuelve en Phase 38, MISC-02), las filas de encabezado de sección (columna accion vacía) y la fila cuyo origen es un simple punto "." (placeholder organizacional, no una URL real).
    2. `source_url`: copiar el valor de `url_origen` de la fila tal cual aparece en el sheet — mismo dominio, mismo path, misma barra final si la trae. Nunca modificar el origen: es la URL real que existe hoy en aprendoseo.com.
    3. `target_url`: si la celda `url_destino` ya es una URL absoluta (empieza con `http`), normalizar el dominio a `https://www.aprendoclub.com` (por si viene sin `www` o en `http` simple) dejando el resto del path igual. Si la celda es una ruta relativa, quitarle cualquier barra inicial, anteponer `https://www.aprendoclub.com/`, y quitarle una barra final sobrante (excepto cuando el path resultante queda vacío, en cuyo caso el target es la raíz del dominio).
    4. `status_code`: literal `301` en todas las filas.
    5. `preserve_query_string`: literal `true` en todas las filas.
    6. Casos especiales (aplican después de la regla 3, por decisión explícita de Juan): la fila cuyo origen es `https://www.aprendoseo.com/diplomado` debe apuntar a `https://www.aprendoclub.com/diplomado` — su URL activa en producción hoy en aprendoclub. Agregar además una fila con origen `https://www.aprendoseo.com/reto` apuntando a `https://www.aprendoclub.com/reto` — su URL activa hoy en aprendoclub. Las redirecciones hacia `/programas/reto` y `/programas/diplomado` se gestionarán como redirecciones internas una vez que la Phase 34 reubique esas páginas.
    7. Las dos filas con nota de "merge" (destino final: página de autor de Arianna Lupi, y `quienes-somos`) se incluyen como redirects simples igual que cualquier otra fila — no crear ninguna lógica de fusión de contenido, ni tocar el copy de esas páginas destino.
    8. No deduplicar por `target_url`: es válido y esperado que más de un `source_url` distinto apunte al mismo destino (por ejemplo, dos URLs viejas que convergen al mismo post o índice de categoría). Sí es obligatorio que cada fila del sheet que pase el filtro de la regla 1 produzca exactamente una fila en el CSV (deduplicar solo si el `source_url` viene literalmente repetido).
    9. Agrupar las filas del CSV final en el mismo orden de clusters que trae el sheet original (Programas, seo-basico, empieza-en-seo, seo-onpage, herramientas-seo, autor, seo-tecnico, urls sueltas), para que Juan pueda revisarlo comparando fácilmente contra el sheet.
    Verificación obligatoria antes de cerrar la tarea: para cada fila resultante, confirmar que el path del `target_url` (sin el dominio) resuelve contra el volcado en vivo de Payload de la Tarea 1 — un slug de `pages`, un slug de `authors` prefijado con `autor/`, un slug de `categories` tal cual, o un par categoría+slug confirmado en `blogposts` — o contra una de las rutas ya confirmadas en 33-SHEET-DATA.md que no vive en Payload (el listado del blog, la página de links). Cualquier fila cuyo target no resuelva debe corregirse (si el drift revela que el slug cambió respecto al snapshot) o excluirse del CSV con una nota — nunca incluirse sin verificar. Llevar una lista de cualquier corrección o exclusión encontrada (comparado contra 33-SHEET-DATA.md) para documentarla en el SUMMARY de la fase.
  </action>
  <verify>
    <automated>bash -c '
CSV=".planning/deliverables/v1.8/redirects-phase33-cloudflare.csv"
test "$(sed -n "1p" "$CSV")" = "source_url,target_url,status_code,preserve_query_string" || { echo FAIL_HEADER; exit 1; }
ROWS=$(awk "END{print NR}" "$CSV")
[ "$ROWS" -ge 74 ] && [ "$ROWS" -le 90 ] || { echo "FAIL_ROWCOUNT:$ROWS"; exit 1; }
DUPES=$(tail -n +2 "$CSV" | cut -d, -f1 | sort | uniq -d | wc -l | tr -d " ")
[ "$DUPES" -eq 0 ] || { echo FAIL_DUPES; exit 1; }
BADSRC=$(tail -n +2 "$CSV" | cut -d, -f1 | grep -vc "^https://www\.aprendoseo\.com/")
[ "$BADSRC" -eq 0 ] || { echo FAIL_SRC_DOMAIN; exit 1; }
BADTGT=$(tail -n +2 "$CSV" | cut -d, -f2 | grep -vc "^https://www\.aprendoclub\.com/")
[ "$BADTGT" -eq 0 ] || { echo FAIL_TGT_DOMAIN; exit 1; }
BADCODE=$(tail -n +2 "$CSV" | cut -d, -f3 | grep -vc "^301$")
[ "$BADCODE" -eq 0 ] || { echo FAIL_CODE; exit 1; }
BADQS=$(tail -n +2 "$CSV" | cut -d, -f4 | grep -vc "^true$")
[ "$BADQS" -eq 0 ] || { echo FAIL_QS; exit 1; }
grep -q ",https://www.aprendoclub.com/reto,301,true" "$CSV" || { echo FAIL_RETO; exit 1; }
grep -q ",https://www.aprendoclub.com/diplomado,301,true" "$CSV" || { echo FAIL_DIPLOMADO; exit 1; }
grep -q "aprendoseo.com/prensa" "$CSV" && { echo FAIL_PRENSA_PRESENT; exit 1; }
grep -q "seo-con-ia/evento" "$CSV" && { echo FAIL_EVENTO_PRESENT; exit 1; }
echo CSV_VALID
'</automated>
  </verify>
  <done>El CSV completo existe con todas las filas verificadas contra Payload en vivo (o documentadas como excepción de la regla 6), sin duplicados de origen, sin filas de /prensa ni del evento de ads, y pasa las 12 validaciones automatizadas.</done>
</task>

<task type="auto">
  <name>Task 3: README de importación a Cloudflare</name>
  <files>.planning/deliverables/v1.8/README-cloudflare-import.md</files>
  <action>
    Escribir `.planning/deliverables/v1.8/README-cloudflare-import.md` en español neutro (sin voceo, sin regionalismos), dirigido a Juan, con cuatro partes: (1) qué es el archivo y qué cubre — el contenido que ya existe hoy en aprendoclub (blog, autores, programas, URLs sueltas); (2) los pasos exactos para importarlo en Cloudflare: entrar al dashboard, seleccionar la zona aprendoseo.com, ir a Rules → Redirect Rules → Bulk Redirects, crear una lista nueva ("Create a bulk redirect list"), importar el CSV mapeando las columnas `source_url`/`target_url`/`status_code`/`preserve_query_string`, y crear la regla que referencia esa lista; (3) una advertencia destacada: las dos filas que apuntan a las URLs finales de reto y diplomado no deben activarse hasta que Phase 34 haya movido esas páginas en producción — si se activan antes, esas dos URLs devuelven un error 404 en vez del contenido — y la recomendación de activarlas junto con el deploy de Phase 34, o dejarlas pausadas en la lista de Cloudflare hasta entonces; (4) el resumen de cualquier corrección o fila descartada que haya surgido al reconciliar el sheet en vivo contra Payload en vivo en la Tarea 2 (si no hubo ninguna, decirlo explícitamente).
    Antes de dar la tarea por completa, revisar el texto con el criterio de la skill humanizer (`~/.claude/skills/humanizer/SKILL.md`): sin muletillas de IA, sin guiones em/en, tono natural y directo.
  </action>
  <verify>
    <automated>bash -c '
README=".planning/deliverables/v1.8/README-cloudflare-import.md"
test -f "$README" || { echo FAIL_MISSING; exit 1; }
grep -qi "Bulk Redirect" "$README" || { echo FAIL_NO_BULK; exit 1; }
grep -q "301" "$README" || { echo FAIL_NO_301; exit 1; }
grep -qi "aprendoseo.com" "$README" || { echo FAIL_NO_DOMAIN; exit 1; }
grep -qiE "Phase 34|programas/reto|programas/diplomado" "$README" || { echo FAIL_NO_DEPENDENCY_NOTE; exit 1; }
echo README_VALID
'</automated>
  </verify>
  <done>El README existe, en español neutro, con los pasos de import a Cloudflare, la advertencia de dependencia con Phase 34 para las filas de reto/diplomado, y el resumen de correcciones encontradas en la reconciliación.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Google Sheet de Juan (externo, editable fuera del repo) → CSV | El contenido del sheet (columnas origen/destino/acción) es input mutable que termina convertido en reglas de infraestructura de producción; nadie más revisa esas filas antes de que entren al CSV. |
| CSV generado → Cloudflare Bulk Redirects (import manual de Juan, fuera de esta fase) | Una vez importado, el CSV se convierte directamente en tráfico redirigido real; Cloudflare no tiene una capa de revisión automática que detecte un destino incorrecto. |
| Payload DB (lectura vía script) → CSV | Boundary interna de solo lectura; el script no escribe nada en Payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-33-01 | Tampering | contenido del Google Sheet (columnas accion/url_destino) | high | mitigate | Task 2 exige que cada `target_url` calce con el dominio `https://www.aprendoclub.com/` (allowlist de dominio en el validador automatizado) y resuelva contra un slug confirmado en vivo en Payload antes de incluirse; ninguna fila entra al CSV sin ese chequeo. |
| T-33-02 | Tampering | destino desactualizado o slug renombrado (REDIR-03) | medium | mitigate | Task 2 cruza cada target contra el volcado en vivo de `pages`/`authors`/`categories`/`blogposts` de la Tarea 1; toda fila sin match documentado se corrige o se excluye del CSV final, con la corrección anotada en el SUMMARY. |
| T-33-03 | Denial of Service | bucle de redirección (target apuntando de vuelta a aprendoseo.com o igual al origen) | medium | mitigate | El validador automatizado de Task 2 rechaza cualquier fila cuyo `target_url` no empiece con el dominio de aprendoclub.com; la regla de normalización nunca copia el dominio de origen al destino. |
| T-33-04 | Repudiation | flujo sin autenticación de usuario ni escritura a la base de datos | low | accept | No hay acción de usuario final ni mutación de datos involucrada; el único efecto es un archivo CSV de solo lectura para Juan. |
| T-33-05 | Information Disclosure | slugs de Payload expuestos en el CSV/README | low | accept | Los slugs de páginas, posts y autores ya son públicos (son las URLs del sitio en producción); no hay dato sensible en juego. |
| T-33-06 | Elevation of Privilege | script de consulta a Payload con `overrideAccess: true` | low | accept | El script es de solo lectura (`payload.find`, sin mutaciones), corre localmente durante la ejecución de la fase y no se expone como endpoint ni queda desplegado. |
</threat_model>

<verification>
- El CSV final pasa las 12 validaciones automatizadas de la Tarea 2 (header exacto, rango de filas, sin duplicados de origen, dominios de origen/destino correctos, código 301, preserve_query_string true, filas de reto/diplomado presentes con su URL final, ausencia de la fila de prensa y de la fila del evento de ads).
- El README pasa las validaciones de contenido de la Tarea 3.
- El script `query-payload-slugs.ts` corre sin errores vía `npx payload run` y refleja el estado real de Payload en el momento de ejecución de la fase, no un snapshot de una sesión anterior.
</verification>

<success_criteria>
Coincide con ROADMAP.md Phase 33 y con REDIR-01..04:
1. El CSV existe (formato Cloudflare Bulk Redirects) cubriendo los blog posts, autores, programas y URLs sueltas del sheet cuyo destino ya existe hoy en aprendoclub.
2. Las filas de /reto y /diplomado apuntan a /programas/reto y /programas/diplomado; los 2 redirects de merge (certificaciones, academia-seo) son redirects simples sin fusión de contenido.
3. Cada URL destino fue verificada contra la DB de Payload en vivo, con cualquier corrección documentada en el SUMMARY de la fase.
4. El CSV y el README quedan en un formato y ubicación que Juan puede usar directamente para importar en el dashboard de Cloudflare de aprendoseo.com.
</success_criteria>

<output>
Create `.planning/phases/33-redirects-301-para-contenido-existente/33-01-SUMMARY.md` when done
</output>
