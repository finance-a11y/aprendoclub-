---
phase: 31
plan: 1
title: Tipografía, motion y spacing (segunda pasada)
requirements: [TYPO-01, TYPO-02, LAY-02]
status: complete
---

# Plan 31-01: Tipografía, motion y spacing (segunda pasada)

## Tareas

1. [x] **TYPO-01**: `font-semibold` → `font-bold` en el patrón de H2 de sección (29 archivos, dos variantes de orden de clases) y en los H1 de `Hero.tsx`/`TallerHero.tsx`/`RetoHero.tsx` (Diplomado/Taller/Reto). El H1 del home (`HeroHome.tsx`) ya era bold desde v1.5, sin cambios.
2. [x] **LAY-02**: `section-spacing` en `app/globals.css` recortado de `4rem/6rem` a `2.75rem/4rem` (mobile/desktop) — gap entre secciones de 128px/192px a 88px/128px en las 45 secciones que usan el utility.
3. [x] **TYPO-02**: hover de `components/ui/card.tsx` (`lift`/`liftAccent`) y del hover propio de `FeatureGrid.tsx` reforzado: `-translate-y-1` → `-translate-y-1.5` + `scale-[1.01]` + `shadow-[var(--shadow-lg)]`. Ícono de las cards de imagen (Fase 30) suma `group-hover:scale-110`.
4. [x] `npx tsc --noEmit` sin errores.
5. [x] `npm run lint` sobre los archivos tocados: 0 errores, 1 warning preexistente sin relación (`fundadora` unused en `collections.ts`, no introducido por este plan).

## Archivos modificados

- `app/globals.css` — `section-spacing`.
- `components/ui/card.tsx` — hover reforzado.
- `components/blocks/render/FeatureGrid.tsx` — hover + ícono.
- `components/blocks/render/Hero.tsx`, `TallerHero.tsx`, `RetoHero.tsx` — H1 bold.
- 29 archivos de `components/blocks/render/` — H2 bold (ver `git show --stat` del commit para la lista completa).

## Nota de verificación

No se pudo verificar visualmente en vivo (ni `npm run dev` local ni el sitio en producción reflejan estos cambios sin `npm run seed`/deploy) porque tanto el dev server local como el seed dependen de conectar a Neon, que está devolviendo `ECONNRESET` (mismo bloqueo externo que Fase 30). El cambio es 100% de código (CSS/Tailwind), no depende de contenido de Payload — el riesgo de que no funcione visualmente es bajo, pero la confirmación en pantalla queda pendiente. Ver `31-VERIFICATION.md`.
