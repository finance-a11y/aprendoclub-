---
phase: 28-rebranding-diplomado
plan: 02
type: execute
wave: 2
depends_on: [28-01]
gap_closure: true
files_modified:
  - aprendoclub/scripts/seed/seed-data/diplomado.ts
  - aprendoclub/scripts/seed/media.ts
  - aprendoclub/scripts/verify-media-alt.ts
autonomous: true
requirements: [BRAND-01]

must_haves:
  truths:
    - "No queda ninguna ocurrencia case-insensitive de 'diplomado cero a seo' (con o sin 'de') en scripts/seed/ — incluida la variante sin 'de' del bullet de pricing"
    - "El bullet de pricing en diplomado.ts dice 'Diplomado de SEO + AIO completo'"
    - "seedMedia() actualiza alt (y cualquier campo seedeado que cambie) en documentos de media preexistentes, no solo en los nuevos"
    - "Tras re-seed, los 3 registros de media del diplomado en Neon (diplomado-hero.avif, diplomado-certificado.avif, diplomado-modulos2.avif) ya no contienen el nombre viejo en alt"
    - "Ningún registro de la tabla media en Neon contiene 'diplomado (de) cero a seo' en alt (verificado por query SQL directa, no por logs del seed)"
  artifacts:
    - path: "aprendoclub/scripts/seed/seed-data/diplomado.ts"
      provides: "bullet de pricing.features con el nuevo nombre"
      contains: "Diplomado de SEO + AIO completo"
    - path: "aprendoclub/scripts/seed/media.ts"
      provides: "seedMedia() con reconciliación de campos (payload.update) en la rama de documento existente"
      contains: "payload.update"
    - path: "aprendoclub/scripts/verify-media-alt.ts"
      provides: "script de verificación que consulta media.alt directamente en Neon vía payload.db.pool"
      contains: "payload.db.pool"
  key_links:
    - from: "scripts/seed/media.ts (seedMedia, rama existing.docs.length > 0)"
      to: "Postgres/Neon media.alt"
      via: "payload.update cuando el alt seedeado difiere del almacenado"
      pattern: "payload\\.update\\(\\{[^}]*collection: 'media'"
    - from: "scripts/verify-media-alt.ts"
      to: "Postgres/Neon tabla media"
      via: "payload.db.pool.query (SELECT ... WHERE alt ~* 'diplomado (de )?cero a seo')"
      pattern: "pool\\.query"
---

<objective>
Cerrar los 2 gaps de la verificación de Phase 28 (BRAND-01):

1. La variante `"Diplomado CERO A SEO completo"` (sin la palabra "de") sobrevivió en `diplomado.ts:515` porque el grep del plan original solo buscó el string literal exacto "Diplomado de Cero a SEO". Renombrarla.
2. `seedMedia()` nunca actualiza el campo `alt` de documentos de media que YA existen en Neon (solo los crea si no existen), así que los 3 assets del diplomado siguen mostrando el nombre viejo en su `alt`/`aria-label` en producción pese a que el archivo fuente ya está corregido. Es un bug estructural que afectaría a cualquier fase futura que edite alt de media ya subida. Arreglarlo de raíz (añadir `payload.update()` en la rama de documento existente) y re-propagar a Neon.

Purpose: El diplomado se presenta de forma consistente bajo su nuevo nombre en TODAS las páginas y datos donde aparece, incluyendo la card de precios y el alt/aria-label de las imágenes en la base de datos en vivo.
Output: 2 archivos de seed corregidos + 1 script de verificación de DB + base de datos de Neon re-seedeada y verificada por SQL directa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-rebranding-diplomado/28-CONTEXT.md
@.planning/phases/28-rebranding-diplomado/28-VERIFICATION.md

**Working directory para el seed y la verificación de DB:** el app vive en el subdirectorio `aprendoclub/` (`/Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub/`). Los comandos `npm run seed`, `npx payload run ...` y `npx tsc --noEmit` se ejecutan desde ese subdirectorio. Los grep de código fuente pueden correrse desde la raíz del repo usando el prefijo `aprendoclub/scripts/seed/`.

**Credenciales del seed:** ya existen en `aprendoclub/.env.local` (`DATABASE_URI` a Neon, `PAYLOAD_SECRET`). NO inventar ni sustituir credenciales. Si un comando de DB falla, detenerse y reportar el stderr completo.

**Por qué SQL directa y no logs:** por precedente de Phase 27/28, el stdout del seed no es prueba confiable de que un dato llegó a Neon (el bug de media.ts es exactamente eso: el log decía "ya existe" mientras el alt quedaba stale). La verificación de este plan consulta la tabla `media` directamente vía `payload.db.pool` (el adapter `@payloadcms/db-postgres` expone un pool de node-postgres).

<interfaces>
<!-- seedMedia(), estado actual — el bug está en la rama existing.docs.length > 0 -->
From aprendoclub/scripts/seed/media.ts:

```typescript
export async function seedMedia(payload: Payload): Promise<Map<string, number>>
// loop por asset { path, alt }:
//   const filename = path.basename(absPath)
//   const existing = await payload.find({ collection: 'media', where: { filename: { equals: filename } }, limit: 1 })
//   if (existing.docs.length > 0) {
//     const id = Number(existing.docs[0].id)
//     manifest.set(asset.path, id)
//     console.log(`[seed:media] ya existe: ${filename} -> ${id}`)
//     continue        // <-- BUG: nunca reconcilia alt
//   }
//   const created = await payload.create({ collection: 'media', data: { alt: asset.alt }, filePath: absPath })
```

Nota: los strings `alt` en el fuente de media.ts (L73, L81) YA fueron corregidos en 28-01 a "Diplomado de SEO + AIO". Este plan NO reedita esos strings; solo arregla la LÓGICA de propagación.

<!-- Acceso a SQL crudo desde un script payload run -->
El postgres adapter expone `payload.db.pool` (Pool de `pg`). Uso: `await payload.db.pool.query('SELECT ... WHERE ...', [params])`. Postgres soporta `~*` para regex case-insensitive.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renombrar la variante sin "de" en el bullet de pricing</name>
  <files>aprendoclub/scripts/seed/seed-data/diplomado.ts</files>
  <read_first>
    - aprendoclub/scripts/seed/seed-data/diplomado.ts (líneas 513-521: array `pricing.features` del plan "Especialista")
  </read_first>
  <action>
    En `diplomado.ts:515`, dentro del array `features`, reemplazar el string exacto `"Diplomado CERO A SEO completo"` por `"Diplomado de SEO + AIO completo"`. Es la variante de la marca vieja SIN la palabra "de", que el grep literal del plan 28-01 no capturó. No tocar los demás bullets del array ("Todo lo de Aprendiz", "3 sesiones semanales", etc.).
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Arianna/aprendoclub && grep -qF "Diplomado de SEO + AIO completo" aprendoclub/scripts/seed/seed-data/diplomado.ts && test "$(grep -riE 'diplomado (de )?cero a seo' aprendoclub/scripts/seed/ | wc -l | tr -d ' ')" -eq 0 && echo TASK1_OK</automated>
  </verify>
  <acceptance_criteria>
    - diplomado.ts contiene el bullet "Diplomado de SEO + AIO completo"
    - `grep -riE 'diplomado (de )?cero a seo' aprendoclub/scripts/seed/` devuelve 0 líneas (cubre la variante con y sin "de", case-insensitive, en todo scripts/seed/)
  </acceptance_criteria>
  <done>El bullet de pricing usa "Diplomado de SEO + AIO completo" y ninguna variante del nombre viejo (con o sin "de") queda en scripts/seed/.</done>
</task>

<task type="auto">
  <name>Task 2: Arreglar seedMedia() para reconciliar campos en documentos existentes</name>
  <files>aprendoclub/scripts/seed/media.ts</files>
  <read_first>
    - aprendoclub/scripts/seed/media.ts (líneas 100-136: función `seedMedia`, en particular la rama `existing.docs.length > 0`)
  </read_first>
  <action>
    Arreglar el bug estructural de la rama `existing.docs.length > 0` en `seedMedia()`: hoy hace `manifest.set(...)` + `continue` sin nunca actualizar el documento, por lo que un cambio de `alt` en el fuente jamás llega a Neon para assets ya subidos.

    Cambiar esa rama para que reconcilie los campos seedeados contra el documento existente: comparar el `alt` almacenado (`existing.docs[0].alt`) con el `alt` calculado (`asset.alt`) y, si difieren, llamar `payload.update({ collection: 'media', id, data: { alt: asset.alt } })` antes de setear el manifest. Loguear de forma distinta el caso "alt actualizado" del caso "sin cambios" para que el diff sea visible. Mantener el `continue` (o el flujo equivalente) que evita el `payload.create` cuando el doc ya existe.

    Hacerlo como fix general, no como parche de un caso: el campo `alt` es hoy el único campo que `payload.create` escribe en `data`, así que reconciliar `alt` cubre el bug por completo; estructurar el código con el patrón "diff-and-update de los campos seedeados" para que, si en el futuro se agregan más campos al `data` del create, la misma rama existente los reconcilie (dejar comentario indicando que cualquier campo nuevo del create debe añadirse también a esta comparación/update). No introducir dependencias nuevas ni cambiar la firma de `seedMedia`.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub && grep -q "payload.update" scripts/seed/media.ts && npx tsc --noEmit && echo TASK2_OK</automated>
  </verify>
  <acceptance_criteria>
    - `media.ts` contiene una llamada `payload.update({ collection: 'media', ... })` dentro de la rama de documento existente
    - La actualización se dispara cuando el `alt` almacenado difiere del calculado
    - Se mantiene la idempotencia: si el alt ya coincide, no se hace update (solo se reusa el id)
    - `npx tsc --noEmit` (desde aprendoclub/) sin errores
  </acceptance_criteria>
  <done>seedMedia() actualiza el alt de documentos de media preexistentes vía payload.update cuando difiere del fuente, cerrando el bug estructural; el fix es general (reconcilia los campos seedeados, no solo un one-off), tipa limpio y mantiene la idempotencia.</done>
</task>

<task type="auto">
  <name>Task 3: Re-seed a Neon + verificación por SQL directa + grep gate final</name>
  <files>aprendoclub/scripts/verify-media-alt.ts</files>
  <read_first>
    - aprendoclub/scripts/seed.ts (cómo `payload run` arranca payload vía getPayload + config; patrón top-level await)
    - aprendoclub/package.json (script `seed`: `payload run scripts/seed.ts`)
  </read_first>
  <action>
    Requiere Task 1 y Task 2 ya aplicadas (el re-seed debe propagar ambos fixes).

    Paso A — Crear el script de verificación de DB `aprendoclub/scripts/verify-media-alt.ts`, modelado sobre `scripts/seed.ts` (import de `getPayload` + `config` de `../payload.config`, `await run()` a nivel de módulo por el patrón top-level await de `payload run`). El script debe:
    - Obtener `payload = await getPayload({ config })`.
    - Ejecutar una query SQL directa contra Neon vía `payload.db.pool.query(...)`:
      - Query 1 (gate): `SELECT id, filename, alt FROM media WHERE alt ~* 'diplomado (de )?cero a seo'` — debe devolver 0 filas. Regex case-insensitive que cubre la variante con y sin "de".
      - Query 2 (confirmación positiva): `SELECT id, filename, alt FROM media WHERE filename IN ('diplomado-hero.avif','diplomado-certificado.avif','diplomado-modulos2.avif') ORDER BY filename` — imprimir las 3 filas con su alt para dejar evidencia del nombre nuevo.
    - Imprimir ambos resultados de forma legible.
    - Si Query 1 devuelve >= 1 fila, hacer `process.exit(1)` (gate fallado); si devuelve 0, imprimir un OK claro y `process.exit(0)`.
    - No inventar credenciales: usa el mismo `config`/`.env.local` que el seed.

    Paso B — Desde el subdirectorio del app (`/Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub/`), correr `npm run seed`. El upsert de media ahora reconcilia alt en los 3 docs preexistentes; el resto de upserts son idempotentes por clave natural. Si el seed falla (conexión a Neon, credencial, runtime), DETENERSE y reportar el stderr completo sin workarounds.

    Paso C — Tras el seed exitoso, correr `npx payload run scripts/verify-media-alt.ts` para probar contra Neon (no contra logs del seed) que ningún registro de media retiene el nombre viejo en alt y que los 3 assets del diplomado muestran "Diplomado de SEO + AIO".
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub && npm run seed && npx payload run scripts/verify-media-alt.ts && cd /Users/juan/Documents/Codigo/Arianna/aprendoclub && test "$(grep -riE 'diplomado (de )?cero a seo' aprendoclub/scripts/seed/ | wc -l | tr -d ' ')" -eq 0 && echo SEED_AND_DB_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run seed` termina con exit code 0 y el log de media muestra "alt actualizado" para los 3 assets del diplomado en la primera corrida post-fix
    - `npx payload run scripts/verify-media-alt.ts` termina con exit code 0: Query 1 devuelve 0 filas y Query 2 muestra los 3 assets con alt = "Diplomado de SEO + AIO ..."
    - `grep -riE 'diplomado (de )?cero a seo' aprendoclub/scripts/seed/` = 0 (código fuente limpio, variante con y sin "de")
    - Si el seed o la query fallan: el ejecutor se detiene y reporta el error en el SUMMARY (no marca el plan como completo)
  </acceptance_criteria>
  <done>El seed corrió contra Neon propagando ambos fixes, la query SQL directa confirma que la tabla media no retiene el nombre viejo en ningún alt y que los 3 assets del diplomado muestran el nombre nuevo, y el grep de código fuente da 0 ocurrencias de cualquier variante del nombre viejo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → Neon (Postgres) | El seed escribe/actualiza contenido en la DB de producción con credenciales de `.env.local` |
| verify-media-alt.ts → Neon (Postgres) | El script lee (SELECT) la tabla media vía `payload.db.pool` con las mismas credenciales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Tampering | `payload.update` sobre media en Neon | accept | El update solo escribe `alt` cuando difiere del fuente; no borra datos ni toca el binario del asset (no se re-sube el archivo). Riesgo: si Juan editó `alt` a mano vía Payload admin, el seed lo sobreescribe — comportamiento esperado del flujo seed-first del proyecto. |
| T-28-04 | Information Disclosure | `verify-media-alt.ts` (query cruda) | accept | Solo SELECT de columnas no sensibles (id, filename, alt) filtradas; sin parámetros de usuario (query estática), sin superficie de inyección. Usa el pool ya autenticado; no loguea credenciales. |

No hay instalación de paquetes (npm/pip/cargo) en esta fase — no aplica el gate de legitimidad de paquetes. `pg` ya viene transitivamente con `@payloadcms/db-postgres` (ya instalado); no se agrega dependencia.
</threat_model>

<verification>
- `grep -riE 'diplomado (de )?cero a seo' aprendoclub/scripts/seed/` = 0 (cubre variante con y sin "de", case-insensitive, todo scripts/seed/)
- diplomado.ts contiene "Diplomado de SEO + AIO completo"
- media.ts contiene `payload.update({ collection: 'media', ... })` en la rama de documento existente
- `npx tsc --noEmit` sin errores
- `npm run seed` exit code 0 (log muestra "alt actualizado" para los 3 assets del diplomado)
- `npx payload run scripts/verify-media-alt.ts` exit code 0: 0 filas con nombre viejo en media.alt; 3 assets del diplomado con el nombre nuevo
</verification>

<success_criteria>
Los 2 gaps de la verificación quedan cerrados: (1) ninguna variante del nombre viejo (con o sin "de") queda en scripts/seed/ y el bullet de pricing usa "Diplomado de SEO + AIO completo"; (2) seedMedia() reconcilia alt en documentos existentes vía payload.update, y una query SQL directa a Neon confirma que la tabla media ya no retiene "Diplomado de Cero a SEO" en ningún alt y que los 3 assets del diplomado muestran el nombre nuevo. BRAND-01 cumple "en todas las páginas y datos donde aparece", incluida la base de datos en vivo.
</success_criteria>

<output>
Create `.planning/phases/28-rebranding-diplomado/28-02-SUMMARY.md` when done
</output>
</content>
</invoke>
