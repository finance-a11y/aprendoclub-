---
phase: 28-rebranding-diplomado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aprendoclub/scripts/seed/seed-data/diplomado.ts
  - aprendoclub/scripts/seed/seed-data/testimonios.ts
  - aprendoclub/scripts/seed/seed-data/home.ts
  - aprendoclub/scripts/seed/collections.ts
  - aprendoclub/scripts/seed/globals.ts
  - aprendoclub/scripts/seed/media.ts
autonomous: true
requirements: [BRAND-01]

must_haves:
  truths:
    - "El diplomado aparece como 'Diplomado de SEO + AIO' en todas las ocurrencias del seed (badge, alt de certificado, testimonio, nav/programMenu, hub programas, títulos de FAQ, alt de media, CTA de WhatsApp)"
    - "No queda ninguna ocurrencia de 'Diplomado de Cero a SEO' (case-insensitive) en scripts/seed/"
    - "collections.ts ya no contiene 'Práctica real'; dice 'Práctica aplicada'"
    - "Las descripciones que mencionan 'especialista SEO' (programMenu[].desc, programas[].descripcion) quedan intactas"
    - "npm run seed corre exitosamente contra Neon y propaga los nuevos nombres a Postgres"
  artifacts:
    - path: "aprendoclub/scripts/seed/seed-data/diplomado.ts"
      provides: "badgeText, alt de certificado y títulos de audience/howItWorks con el nuevo nombre"
      contains: "Diplomado de SEO + AIO"
    - path: "aprendoclub/scripts/seed/seed-data/testimonios.ts"
      provides: "Mención del testimonio de Fransheska Sánchez con el nuevo nombre"
      contains: "Diplomado de SEO + AIO"
    - path: "aprendoclub/scripts/seed/collections.ts"
      provides: "nombre del programa y label de programMenu con el nuevo nombre; 'Práctica aplicada'"
      contains: "Diplomado de SEO + AIO"
    - path: "aprendoclub/scripts/seed/globals.ts"
      provides: "label de programMenu (nav) con el nuevo nombre"
      contains: "Diplomado de SEO + AIO"
    - path: "aprendoclub/scripts/seed/media.ts"
      provides: "alt text de hero y de features del diplomado con el nuevo nombre"
      contains: "Diplomado de SEO + AIO"
    - path: "aprendoclub/scripts/seed/seed-data/home.ts"
      provides: "CTA de WhatsApp URL-encoded con el nuevo nombre"
      contains: "Diplomado%20de%20SEO%20%2B%20AIO"
  key_links:
    - from: "scripts/seed/*.ts"
      to: "Postgres/Neon (colecciones Programas, Testimonios, Media, globals)"
      via: "npm run seed (upsert idempotente por clave natural)"
      pattern: "npm run seed"
---

<objective>
Renombrar el diplomado de "Diplomado de Cero a SEO" a "Diplomado de SEO + AIO" en TODAS sus ocurrencias del seed (BRAND-01), corregir el fix colateral "Práctica real" → "Práctica aplicada" en collections.ts, y re-correr el seed para propagar los cambios a la base de datos de Neon.

Purpose: El diplomado se presenta de forma consistente bajo su nombre nuevo confirmado por Juan en toda página y dato donde aparece.
Output: 6 archivos de seed editados + base de datos re-seedeada con los nombres nuevos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-rebranding-diplomado/28-CONTEXT.md

**Working directory para el seed:** el app vive en el subdirectorio `aprendoclub/` (`/Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub/`). Los comandos `npm run seed` y `npx tsc --noEmit` se ejecutan desde ese subdirectorio. Los grep de verificación pueden correrse desde la raíz del repo usando el prefijo `aprendoclub/scripts/seed/`.

**Credenciales del seed:** ya existen en `aprendoclub/.env.local` (conexión a Neon). NO inventar ni sustituir credenciales. Si `npm run seed` falla, detenerse y reportar el error tal cual.

<scope_note>
CONTEXT.md enumeró 5 ubicaciones + WhatsApp + "Práctica real". Un grep case-insensitive sobre `scripts/seed/` reveló 10 líneas con el nombre completo "Diplomado de Cero a SEO", incluyendo 5 que CONTEXT.md no listó explícitamente: `globals.ts:21`, `media.ts:73`, `media.ts:81`, `diplomado.ts:145`, `diplomado.ts:343`. Todas contienen el NOMBRE COMPLETO del programa (no son menciones genéricas), por lo que NO caen bajo el criterio de exclusión de CONTEXT.md y SÍ entran en el objetivo de fase ("en todas las páginas y datos donde aparece") y en el gate de verificación ("0 ocurrencias case-insensitive en scripts/seed/"). Se renombran todas.
</scope_note>

<exclusions>
NO tocar (describen la especialidad que se aprende, no el tagline de marca — criterio de exclusión de CONTEXT.md):
- `collections.ts:23` y `globals.ts:23` — `desc: '16 semanas para convertirte en especialista SEO.'`
- `collections.ts:20` — la frase `'convertirte en especialista SEO'` dentro de `descripcion` (solo se toca "Práctica real" en esa misma línea)
- `diplomado.ts:42` — `tituloPre: "Conviértete en especialista SEO, "`
Estas líneas NO contienen el nombre completo "Diplomado de Cero a SEO", así que el grep gate no las marca; no editarlas.
</exclusions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renombrar todas las ocurrencias del diplomado + fix "Práctica real"</name>
  <files>aprendoclub/scripts/seed/seed-data/diplomado.ts, aprendoclub/scripts/seed/seed-data/testimonios.ts, aprendoclub/scripts/seed/collections.ts, aprendoclub/scripts/seed/globals.ts, aprendoclub/scripts/seed/media.ts, aprendoclub/scripts/seed/seed-data/home.ts</files>
  <read_first>
    - aprendoclub/scripts/seed/collections.ts (líneas 14-75: arrays `programas` y `programMenu`; confirmar estado post-Phase 27 antes de editar)
    - aprendoclub/scripts/seed/seed-data/diplomado.ts (líneas 40-45, 78-82, 144-146, 341-344)
    - aprendoclub/scripts/seed/seed-data/testimonios.ts (líneas 138-144)
    - aprendoclub/scripts/seed/seed-data/home.ts (líneas 292-295)
    - aprendoclub/scripts/seed/globals.ts (líneas 19-25)
    - aprendoclub/scripts/seed/media.ts (líneas 72-82)
  </read_first>
  <action>
    Reemplazar el nombre del programa por el string exacto confirmado por Juan: "Diplomado de SEO + AIO". No inventar variantes ni acentos distintos. Editar exactamente estas 12 ocurrencias (BRAND-01) más el fix colateral:

    diplomado.ts:
    - L41 `badgeText: "Diplomado de Cero a SEO"` → `badgeText: "Diplomado de SEO + AIO"`
    - L80 `alt: "Certificado de finalización del Diplomado de Cero a SEO"` → `alt: "Certificado de finalización del Diplomado de SEO + AIO"`
    - L145 `titulo: "¿El diplomado de cero a SEO será para mí?"` → `titulo: "¿El Diplomado de SEO + AIO será para mí?"`
    - L343 `titulo: "¿Cómo funciona el diplomado de cero a SEO?"` → `titulo: "¿Cómo funciona el Diplomado de SEO + AIO?"`

    testimonios.ts:
    - L143 `"...sin el Diplomado de Cero a SEO de Ari, que me dio las herramientas."` → `"...sin el Diplomado de SEO + AIO de Ari, que me dio las herramientas."`

    collections.ts:
    - L18 `nombre: 'Diplomado de cero a SEO'` → `nombre: 'Diplomado de SEO + AIO'`
    - L20 en `descripcion`: `Práctica real, coaching en vivo y certificación.` → `Práctica aplicada, coaching en vivo y certificación.` (solo el adjetivo "real" → "aplicada"; NO tocar "convertirte en especialista SEO" que está en la misma cadena)
    - L58 `label: 'Diplomado de cero a SEO'` → `label: 'Diplomado de SEO + AIO'`

    globals.ts:
    - L21 `label: 'Diplomado de cero a SEO'` → `label: 'Diplomado de SEO + AIO'`

    media.ts:
    - L73 `'Estudiante del Diplomado de Cero a SEO trabajando en su laptop'` → `'Estudiante del Diplomado de SEO + AIO trabajando en su laptop'`
    - L81 template literal `` `${f.titulo} - Diplomado de Cero a SEO` `` → `` `${f.titulo} - Diplomado de SEO + AIO` ``

    home.ts (CTA de WhatsApp, texto URL-encoded en `botonHref` L294): el "+" debe codificarse como `%2B` y los espacios como `%20`. Reemplazar el fragmento `...ingresar%20al%20Diplomado%20de%20SEO%20para%20convertirme...` por `...ingresar%20al%20Diplomado%20de%20SEO%20%2B%20AIO%20para%20convertirme...`. Es decir, insertar `%20%2B%20AIO` entre `Diplomado%20de%20SEO` y `%20para`.

    NO editar: `collections.ts:23`, `globals.ts:23` (desc "especialista SEO"), `diplomado.ts:42` (tituloPre "especialista SEO"), ni la parte "convertirte en especialista SEO" de `descripcion`.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Arianna/aprendoclub && test $(grep -ric "Diplomado de Cero a SEO" aprendoclub/scripts/seed/) -eq 0 && test $(grep -rF "Diplomado de SEO + AIO" aprendoclub/scripts/seed/ | wc -l | tr -d ' ') -ge 9 && ! grep -rqF "Práctica real" aprendoclub/scripts/seed/collections.ts && grep -qF "Práctica aplicada" aprendoclub/scripts/seed/collections.ts && grep -qF "Diplomado%20de%20SEO%20%2B%20AIO" aprendoclub/scripts/seed/seed-data/home.ts && grep -qc "especialista SEO" aprendoclub/scripts/seed/collections.ts && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ric "Diplomado de Cero a SEO" aprendoclub/scripts/seed/` devuelve 0
    - "Diplomado de SEO + AIO" aparece en diplomado.ts, testimonios.ts, collections.ts, globals.ts y media.ts (>= 9 líneas en total)
    - collections.ts contiene "Práctica aplicada" y ya NO "Práctica real"
    - home.ts contiene el fragmento `Diplomado%20de%20SEO%20%2B%20AIO`
    - "especialista SEO" sigue presente en collections.ts y globals.ts (descripciones intactas)
    - `npx tsc --noEmit` (desde aprendoclub/) sin errores
  </acceptance_criteria>
  <done>Las 12 ocurrencias del nombre del diplomado usan "Diplomado de SEO + AIO", "Práctica real" quedó como "Práctica aplicada", las descripciones con "especialista SEO" están intactas, y el CTA de WhatsApp lleva el nombre nuevo URL-encoded.</done>
</task>

<task type="auto">
  <name>Task 2: Re-seed a Neon + gate de verificación</name>
  <files>(ninguno — ejecución de seed y verificación, sin edición de archivos)</files>
  <read_first>
    - aprendoclub/package.json (confirmar que existe el script `seed` y su comando)
  </read_first>
  <action>
    Desde el subdirectorio del app (`/Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub/`), correr `npm run seed`. El upsert es idempotente por clave natural (slug/nombre/question), así que re-correr es seguro para estos registros y no duplica. Las credenciales están en `.env.local`; no sustituirlas. Si el comando falla (error de conexión a Neon, credencial faltante, o error de runtime), DETENERSE y reportar el stderr completo sin intentar workarounds ni inventar credenciales.

    Tras un seed exitoso, correr el gate de verificación de fase (grep de confirmación) para probar que 0 ocurrencias del nombre viejo quedan en el seed y que el nombre nuevo está en las 5 ubicaciones de contenido objetivo.
  </action>
  <verify>
    <automated>cd /Users/juan/Documents/Codigo/Arianna/aprendoclub/aprendoclub && npm run seed && cd /Users/juan/Documents/Codigo/Arianna/aprendoclub && test $(grep -ric "Diplomado de Cero a SEO" aprendoclub/scripts/seed/) -eq 0 && grep -qF "Diplomado de SEO + AIO" aprendoclub/scripts/seed/seed-data/diplomado.ts && grep -qF "Diplomado de SEO + AIO" aprendoclub/scripts/seed/seed-data/testimonios.ts && grep -qF "Diplomado de SEO + AIO" aprendoclub/scripts/seed/collections.ts && grep -qF "Diplomado de SEO + AIO" aprendoclub/scripts/seed/globals.ts && grep -qF "Diplomado de SEO + AIO" aprendoclub/scripts/seed/media.ts && echo SEED_AND_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run seed` termina con exit code 0 (log de collections muestra programas/testimonios seedeados)
    - `grep -ric "Diplomado de Cero a SEO" aprendoclub/scripts/seed/` = 0
    - "Diplomado de SEO + AIO" presente en las 5 ubicaciones de contenido (diplomado.ts, testimonios.ts, collections.ts, globals.ts, media.ts)
    - Si el seed falla: el ejecutor se detiene y reporta el error en el SUMMARY (no marca el plan como completo)
  </acceptance_criteria>
  <done>El seed corrió exitosamente contra Neon propagando los nombres nuevos, y el grep gate confirma 0 ocurrencias del nombre viejo y el nombre nuevo en las 5 ubicaciones objetivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → Neon (Postgres) | El seed escribe en la DB de producción con credenciales de `.env.local` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | `npm run seed` sobre Neon | accept | El upsert es idempotente por clave natural (slug/nombre/question) y solo actualiza los campos definidos en el seed; no borra datos ajenos ni afecta contenido editado fuera de estos registros. Riesgo: si Juan editó estos registros vía Payload admin, el seed los sobreescribe — comportamiento esperado del flujo seed-first del proyecto. |
| T-28-02 | Information Disclosure | credenciales en `.env.local` | accept | Sin cambios de credenciales; el plan prohíbe inventarlas o loguearlas. Sin nueva superficie de ataque (solo edición de contenido). |

No hay instalación de paquetes (npm/pip/cargo) en esta fase — no aplica el gate de legitimidad de paquetes.
</threat_model>

<verification>
- `grep -ric "Diplomado de Cero a SEO" aprendoclub/scripts/seed/` = 0 (case-insensitive, todo scripts/seed/)
- "Diplomado de SEO + AIO" aparece en las 5 ubicaciones de contenido objetivo
- `grep -F "Práctica real" aprendoclub/scripts/seed/collections.ts` = 0; "Práctica aplicada" presente
- "especialista SEO" sigue presente en collections.ts y globals.ts (exclusiones respetadas)
- CTA de WhatsApp en home.ts contiene `Diplomado%20de%20SEO%20%2B%20AIO`
- `npm run seed` exit code 0
- `npx tsc --noEmit` sin errores
</verification>

<success_criteria>
El diplomado se presenta como "Diplomado de SEO + AIO" en todas sus ocurrencias del seed; no queda ninguna referencia al nombre viejo (case-insensitive) en scripts/seed/; el fix colateral "Práctica real" → "Práctica aplicada" está aplicado; las descripciones con "especialista SEO" quedan intactas; y el seed corrió exitosamente contra Neon propagando los cambios a la base de datos.
</success_criteria>

<output>
Create `.planning/phases/28-rebranding-diplomado/28-01-SUMMARY.md` when done
</output>
