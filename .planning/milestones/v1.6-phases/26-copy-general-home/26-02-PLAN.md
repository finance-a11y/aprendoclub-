---
phase: 26-copy-general-home
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - aprendoclub/scripts/seed/seed-data/diplomado.ts
autonomous: true
requirements: [COPY-01, COPY-02]

must_haves:
  truths:
    - "El copy visible de la pagina /diplomado ya no usa 'real'/'reales'"
    - "La claim de posicionamiento de marca en diplomado.ts de-enfatiza 'SEO' a favor de 'marketing con IA' (mismo criterio que quienes-somos.ts:79)"
    - "Las rutas de imagen /diplomado/real/*.avif y los comentarios de codigo quedan intactos"
  artifacts:
    - path: "aprendoclub/scripts/seed/seed-data/diplomado.ts"
      provides: "Copy del diplomado sin 'real'/'reales' en strings visibles y sin claim de posicionamiento 'academia especializada en SEO'"
  key_links:
    - from: "aprendoclub/scripts/seed/seed-data/diplomado.ts"
      to: "aprendoclub/scripts/seed/pages.ts"
      via: "objetos de contenido del diplomado"
      pattern: "descripcion|titulo|detalle"
---

<objective>
Reescribir las 12 ocurrencias de "real"/"reales" en el copy visible de `diplomado.ts` siguiendo los criterios de la fase, de-enfatizar la claim de posicionamiento de marca "primera academia especializada en SEO" (COPY-02), sin tocar rutas de imagen `/diplomado/real/*.avif` ni comentarios de codigo.

Purpose: El diplomado es la pagina de mayor densidad de copy con "real"; alinear su lenguaje con la voz de marca depurada, incluida la claim de posicionamiento que replica la de quienes-somos.
Output: `diplomado.ts` editado, strings tipados intactos, assets y comentarios sin tocar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-copy-general-home/26-CONTEXT.md

<criterios_de_reescritura>
NO find-replace ciego; aplicar caso por caso (de CONTEXT.md <decisions>):
- Contexto practico ("proyectos reales", "practicas reales", "casos reales", "clases ... proyectos reales") -> cortar el adjetivo o usar "proyectos", "practicas guiadas".
- Abstracto/aspiracional ("una necesidad real", "visibilidad real", "un proyecto real", "resultados reales") -> reformular: "una necesidad concreta del mercado", "visibilidad en buscadores", "un proyecto de cliente/portafolio", "resultados medibles".
- "oportunidades reales" -> "oportunidades concretas" u "oportunidades laborales".
- De-enfasis SEO SOLO donde "SEO" es frase central de posicionamiento de marca (ej. "primera academia especializada en SEO"). Termino tecnico del modulo ("proyecto SEO", "modulo SEO") SE MANTIENE.
- NO tocar: rutas `/diplomado/real/*.avif` (lineas ~79-91, 357-379), ni comentarios `//` o bloques `/* */` que mencionan "foto real"/"assets reales"/"personas reales" (lineas ~15, 58, 64, 319).
- Consultar `~/.claude/skills/humanizer/SKILL.md` antes de aplicar. No inventar datos ni resultados no confirmados.
</criterios_de_reescritura>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir "real"/"reales" en la primera mitad de diplomado.ts</name>
  <files>aprendoclub/scripts/seed/seed-data/diplomado.ts</files>
  <read_first>
    - aprendoclub/scripts/seed/seed-data/diplomado.ts (leer lineas ~46, 76, 113, 151, 164, 200 con su contexto)
    - ~/.claude/skills/humanizer/SKILL.md
  </read_first>
  <action>
    Reescribir el copy visible con "real"/"reales" en estas lineas segun los criterios:
    - ~46: "...clases grabadas, practicas reales y un equipo..." (practico).
    - ~76: "Clases en vivo, proyectos reales y una comunidad..." (practico).
    - ~113: "Una necesidad real. Una solucion creada desde la experiencia." (abstracto -> "Una necesidad concreta del mercado." o similar).
    - ~151: "...necesita visibilidad real en buscadores." (tangible -> "visibilidad en buscadores").
    - ~164: "...generando ingresos reales y posicionando negocios..." (tangible -> "ingresos adicionales").
    - ~200: "Aplicas desde la semana 1 en un proyecto real." (abstracto -> "en un proyecto de cliente" / "en un proyecto de portafolio").
    NO tocar rutas de imagen ni comentarios. Pasar cada reescritura por el humanizer.
  </action>
  <verify>
    <automated>cd aprendoclub && test $(sed -n '40,210p' scripts/seed/seed-data/diplomado.ts | grep -niE "reales?\b" | grep -vE ':\s*(//|\*)' | grep -v "/diplomado/real/" | wc -l) -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - En lineas ~40-210, 0 ocurrencias de "real"/"reales" en copy visible (excluyendo comentarios y rutas /diplomado/real/).
    - Las rutas `/diplomado/real/*.avif` de ese rango siguen intactas.
    - El archivo compila (strings validos).
  </acceptance_criteria>
  <done>La primera mitad de diplomado.ts no usa "real"/"reales" en copy visible.</done>
</task>

<task type="auto">
  <name>Task 2: Reescribir "real"/"reales" en la segunda mitad de diplomado.ts</name>
  <files>aprendoclub/scripts/seed/seed-data/diplomado.ts</files>
  <read_first>
    - aprendoclub/scripts/seed/seed-data/diplomado.ts (leer lineas ~242, 307, 355, 369, 517, 547 con su contexto)
    - ~/.claude/skills/humanizer/SKILL.md
  </read_first>
  <action>
    Reescribir el copy visible con "real"/"reales" en estas lineas segun los criterios:
    - ~242: "Configura tu primer proyecto SEO real paso a paso." (abstracto -> "tu primer proyecto SEO paso a paso" — cortar adjetivo, conservar "SEO" que aqui es termino tecnico del modulo).
    - ~307: "Prepara tu perfil profesional para oportunidades reales." (-> "oportunidades concretas" / "oportunidades laborales").
    - ~355: "...lecciones que puedes aplicar en proyectos reales desde el inicio." (practico -> cortar adjetivo).
    - ~369: "Proyecto real para tu portafolio" (abstracto -> "Proyecto para tu portafolio" / "Proyecto de cliente para tu portafolio").
    - ~517: "Proyectos aplicados reales" (practico -> "Proyectos aplicados").
    - ~547: "...con resultados reales y acompanamiento de la comunidad..." (tangible -> "resultados medibles").
    NO tocar rutas de imagen (~357, 365, 379) ni comentarios. Pasar cada reescritura por el humanizer.
  </action>
  <verify>
    <automated>cd aprendoclub && test $(grep -niE "reales?\b" scripts/seed/seed-data/diplomado.ts | grep -vE ':\s*(//|\*)' | grep -v "/diplomado/real/" | wc -l) -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - En todo diplomado.ts, 0 ocurrencias de "real"/"reales" en copy visible (excluyendo comentarios y rutas /diplomado/real/).
    - Las rutas `/diplomado/real/*.avif` siguen intactas (grep aun las encuentra).
    - El archivo compila.
  </acceptance_criteria>
  <done>diplomado.ts completo sin "real"/"reales" en copy visible; assets y comentarios intactos.</done>
</task>

<task type="auto">
  <name>Task 3: De-enfasis SEO en la claim de posicionamiento de diplomado.ts (COPY-02)</name>
  <files>aprendoclub/scripts/seed/seed-data/diplomado.ts</files>
  <read_first>
    - aprendoclub/scripts/seed/seed-data/diplomado.ts (leer linea ~128 con su contexto: el objeto con icon "users")
    - aprendoclub/scripts/seed/seed-data/quienes-somos.ts (linea ~79, para replicar el criterio de reescritura)
    - ~/.claude/skills/humanizer/SKILL.md
  </read_first>
  <action>
    Linea ~128 (bloque `icon: "users"`): "Asi nacio aprendoclub, la primera academia especializada en SEO para el mercado hispano. Hoy, mas de 750 personas se han formado con nosotros." Esta es la MISMA claim de posicionamiento de marca que quienes-somos.ts:79 (que ya se de-enfatiza en 26-03). Aplicar el mismo criterio marketing/IA: "primera academia especializada en SEO" -> "primera academia especializada en marketing con IA" (o "en marketing con IA para el mercado hispano"). NO tocar la cifra "750" ni el resto del texto. Pasar por el humanizer.
    Nota: NO existen otras claims de posicionamiento "academia ... SEO" en diplomado.ts; el resto de menciones "SEO" son terminos tecnicos del modulo y SE MANTIENEN.
  </action>
  <verify>
    <automated>cd aprendoclub && test $(grep -ciE "academia (especializada en|de) SEO" scripts/seed/seed-data/diplomado.ts) -eq 0 && test $(grep -c "750" scripts/seed/seed-data/diplomado.ts) -ge 1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - No queda ninguna claim "academia especializada en SEO" ni "academia de SEO" en diplomado.ts.
    - La cifra "750" permanece intacta.
    - El archivo compila.
  </acceptance_criteria>
  <done>La claim de posicionamiento de diplomado.ts de-enfatiza SEO a favor de marketing con IA, alineada con quienes-somos.ts:79.</done>
</task>

</tasks>

<verification>
- `grep -niE "reales?\b" scripts/seed/seed-data/diplomado.ts | grep -vE ':\s*(//|\*)' | grep -v "/diplomado/real/"` devuelve 0 lineas.
- `grep -ciE "academia (especializada en|de) SEO" scripts/seed/seed-data/diplomado.ts` = 0.
- `grep -c "/diplomado/real/" scripts/seed/seed-data/diplomado.ts` sigue siendo el mismo valor previo (rutas intactas).
</verification>

<success_criteria>
diplomado.ts entrega las 12 reescrituras de "real"/"reales" con voz humanizada y de-enfatiza la claim de posicionamiento "academia especializada en SEO" a favor de marketing con IA, sin alterar rutas de imagen ni comentarios de codigo, y el archivo permanece tipado y valido.
</success_criteria>

<output>
Crear `.planning/phases/26-copy-general-home/26-02-SUMMARY.md` al terminar.
</output>
</content>
</invoke>
