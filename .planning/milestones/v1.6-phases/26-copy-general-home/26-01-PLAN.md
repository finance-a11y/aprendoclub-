---
phase: 26-copy-general-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aprendoclub/scripts/seed/seed-data/home.ts
autonomous: true
requirements: [COPY-01, COPY-02, HOME-01, HOME-02]

must_haves:
  truths:
    - "El copy visible del home ya no contiene la palabra 'real'/'reales'"
    - "El badge y subtitulo del hero de-enfatizan 'SEO' en favor de 'marketing/IA'"
    - "La cifra de estudiantes en badge y ratingTexto refleja +10.000 con separador de punto"
  artifacts:
    - path: "aprendoclub/scripts/seed/seed-data/home.ts"
      provides: "Copy del home (hero + feature grids) sin 'real', SEO de-enfatizado, cifra actualizada"
      contains: "10.000"
  key_links:
    - from: "aprendoclub/scripts/seed/seed-data/home.ts"
      to: "aprendoclub/scripts/seed/pages.ts buildHome()"
      via: "hero.badgeText / hero.subtitulo / hero.ratingTexto"
      pattern: "badgeText|subtitulo|ratingTexto"
---

<objective>
Reescribir el copy del home (`home.ts`) para: (1) eliminar "real"/"reales" del copy visible, (2) de-enfatizar "SEO" en el badge y subtitulo del hero a favor de "marketing/IA", y (3) actualizar la cifra de estudiantes a +10.000 en badge y ratingTexto.

Purpose: El home es la primera impresion del sitio; su copy debe reflejar la voz de marca actual (marketing con IA, no "SEO") y la cifra de estudiantes vigente.
Output: `home.ts` editado con strings tipados intactos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-copy-general-home/26-CONTEXT.md

<criterios_de_reescritura>
Sacados de CONTEXT.md <decisions>. NO hacer find-replace ciego; aplicar caso por caso:
- "proyectos reales"/"casos reales" (contexto practico) -> cortar el adjetivo: "proyectos", "casos de estudiantes actuales".
- "una carrera real"/"necesidad real" (abstracto) -> reformular sin el adjetivo: "una carrera con salida laboral", "una necesidad concreta del mercado".
- "sitios reales"/"ingresos reales"/"visibilidad real" (resultado tangible) -> especifico y medible: "sitios de clientes", "ingresos adicionales", "visibilidad en buscadores".
- De-enfasis SEO: SEO queda implicito, no se censura en contenido educativo; solo se reemplaza donde es frase central de posicionamiento de marca.
- Separador de miles: usar punto ("10.000"), consistente con "2.000" en quienes-somos.
- Redaccion con voz humanizada: consultar `~/.claude/skills/humanizer/SKILL.md` antes de aplicar cada edit. No inventar datos ni resultados no confirmados por Juan.
</criterios_de_reescritura>

<interfaces>
Del hero de home.ts (interface HomeHero): badgeText: string, subtitulo: string, ratingTexto: string.
tituloAccent ya dice "marketing e IA" (v1.5) -> alinear el subtitulo con eso.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminar "reales" del copy visible del home</name>
  <files>aprendoclub/scripts/seed/seed-data/home.ts</files>
  <read_first>
    - aprendoclub/scripts/seed/seed-data/home.ts (leer lineas ~122 y ~286 completas antes de editar)
    - ~/.claude/skills/humanizer/SKILL.md (guia de voz)
  </read_first>
  <action>
    Leer las dos lineas de copy que contienen "reales" y reescribirlas siguiendo los criterios de reescritura del contexto:
    - Linea ~122: "Diplomado completo de SEO + cursos cortos actualizados cada mes con casos reales." El "SEO" aqui es parte del nombre del producto (Phase 28), NO tocar; solo resolver "casos reales" (contexto practico -> cortar adjetivo).
    - Linea ~286: "Proyectos reales para armar tu portafolio" (contexto practico -> cortar adjetivo).
    Pasar cada reescritura por la guia del humanizer. Mantener el string dentro de su objeto tipado sin alterar la estructura.
  </action>
  <verify>
    <automated>cd aprendoclub && test $(grep -niE "reales?\b" scripts/seed/seed-data/home.ts | grep -vE ':\s*(//|\*)' | wc -l) -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep de "real"/"reales" sobre lineas de copy (no comentarios) de home.ts devuelve 0 coincidencias.
    - El sentido de ambas frases se conserva (portafolio / casos de estudiantes).
    - No se introdujo "real"/"reales" en otra parte del archivo.
  </acceptance_criteria>
  <done>home.ts no contiene "real"/"reales" en copy visible; las dos frases quedan reescritas con voz humanizada.</done>
</task>

<task type="auto">
  <name>Task 2: De-enfatizar SEO y actualizar cifra en el hero (COPY-02 + HOME-01 + HOME-02)</name>
  <files>aprendoclub/scripts/seed/seed-data/home.ts</files>
  <read_first>
    - aprendoclub/scripts/seed/seed-data/home.ts (leer el objeto `hero`, lineas ~35-57: badgeText, subtitulo, ratingTexto)
    - ~/.claude/skills/humanizer/SKILL.md
  </read_first>
  <action>
    Editar el objeto `hero`:
    - `badgeText`: hoy "+500 estudiantes ya aprenden SEO con IA" -> reescribir a "+10.000 estudiantes ya se unieron" (o variante humanizada equivalente), de-enfatizando "SEO" a favor de "marketing con IA" y aplicando la cifra 10.000 con separador de punto. Debe contener "10.000".
    - `subtitulo`: hoy empieza "Especializate en SEO con la plataforma..." -> cambiar el arranque a "Especializate en marketing con IA con la plataforma..." (alinear con tituloAccent = "marketing e IA"); conservar el resto (cursos, comunidad, mentorias, vacantes). NO debe contener "academia de SEO" ni "acompanamiento real".
    - `ratingTexto`: hoy "4.9/5 de +500 estudiantes" -> "4.9/5 de +10.000 estudiantes". Debe contener "10.000".
    Pasar cada string por la guia del humanizer. No inventar cifras distintas de 10.000.
  </action>
  <verify>
    <automated>cd aprendoclub && test $(grep -c "+500 estudiantes" scripts/seed/seed-data/home.ts) -eq 0 && test $(grep -c "aprenden SEO" scripts/seed/seed-data/home.ts) -eq 0 && test $(grep -c "10.000" scripts/seed/seed-data/home.ts) -ge 2 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - badgeText y ratingTexto contienen "10.000"; no queda ningun "+500 estudiantes" en el archivo.
    - subtitulo no contiene "Especializate en SEO", "academia de SEO" ni "acompanamiento real".
    - Los tres campos siguen siendo strings validos del objeto hero (archivo compila).
  </acceptance_criteria>
  <done>Badge, subtitulo y ratingTexto de-enfatizan SEO y muestran +10.000 estudiantes con separador de punto.</done>
</task>

</tasks>

<verification>
- `grep -niE "reales?\b" scripts/seed/seed-data/home.ts` sobre copy (no comentarios) devuelve 0.
- `grep -c "10.000" scripts/seed/seed-data/home.ts` >= 2 (badge + ratingTexto).
- No quedan strings "+500 estudiantes", "aprenden SEO", "academia de SEO" ni "acompanamiento real".
</verification>

<success_criteria>
home.ts entrega el copy del home sin "real"/"reales", con SEO de-enfatizado en badge y subtitulo, y la cifra de estudiantes en +10.000 de forma consistente en badge y ratingTexto. El archivo sigue tipado y valido.
</success_criteria>

<output>
Crear `.planning/phases/26-copy-general-home/26-01-SUMMARY.md` al terminar.
</output>
