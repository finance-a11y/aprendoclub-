---
phase: 38-paginas-sueltas-faltantes
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - aprendoclub/scripts/seed-paginas-sueltas.ts
  - aprendoclub/next.config.ts
  - .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv
  - .planning/deliverables/v1.8/redirects-phase33-cloudflare-noheader.csv
  - .planning/deliverables/v1.8/redirects-phase33-cloudflare-both-domains.csv
autonomous: true
requirements: [MISC-01, MISC-02, MISC-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Las 6 páginas contacto, glosario, politica-privacidad, politica-reembolso, aviso-legal y terminos-condiciones existen en la colección pages de Payload CMS y resuelven vía [...slug]."
    - "next.config.ts y los CSVs de Cloudflare redirigen las URLs de aprendoseo.com hacia las nuevas páginas y redirigen /recursos/guia-seo-para-principiantes a /programas/curso-basico-de-seo."
    - "/seo-con-ia/evento redirige 301 a /programas/taller-seo-con-ia."
    - "/prensa no aparece en las listas de redirección (descartada sin valor)."
  artifacts:
    - "aprendoclub/scripts/seed-paginas-sueltas.ts"
    - "aprendoclub/next.config.ts"
    - ".planning/deliverables/v1.8/redirects-phase33-cloudflare.csv"
  key_links:
    - "Payload pages collection ↔ MISC-01"
    - "Redirect /seo-con-ia/evento ↔ MISC-02"
    - "Absence of /prensa in Cloudflare ↔ MISC-03"
---

<objective>
Publicar en Payload CMS las 6 páginas restantes (contacto, glosario y 4 legales), añadir sus redirecciones 301 en Cloudflare y next.config.ts, asignar destino a /seo-con-ia/evento y descartar /prensa.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-paginas-sueltas-faltantes/38-CONTEXT.md
@aprendoclub/collections/Pages.ts
@aprendoclub/next.config.ts
@.planning/deliverables/v1.8/redirects-phase33-cloudflare.csv
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Seed de las 6 páginas sueltas y legales en Payload CMS (Neon DB)</name>
  <files>aprendoclub/scripts/seed-paginas-sueltas.ts</files>
  <action>
    1. Crear `aprendoclub/scripts/seed-paginas-sueltas.ts`:
       - Importar `getPayload` de `payload` y `config` de `../payload.config`.
       - Crear o actualizar en la colección `pages`:
         - `slug: 'contacto'`
         - `slug: 'glosario'`
         - `slug: 'politica-privacidad'`
         - `slug: 'politica-reembolso'`
         - `slug: 'aviso-legal'`
         - `slug: 'terminos-condiciones'`
       - Cada página con bloques semánticos y estructurados (`hero` / `sectionHeader`, `featureGrid`, `ctaBanner`).
       - Usar `context: { disableRevalidate: true }`.
    2. Ejecutar `seed-paginas-sueltas.ts` contra Neon DB.
  </action>
  <verify>
    <automated>bash -c '
cd aprendoclub && node --env-file=.env.local -e '\''
const { Client } = require("pg");
const c = new Client({ connectionString: process.env.DATABASE_URI });
async function check() {
  await c.connect();
  const slugs = ["contacto", "glosario", "politica-privacidad", "politica-reembolso", "aviso-legal", "terminos-condiciones"];
  const res = await c.query("SELECT slug FROM pages WHERE slug = ANY($1);", [slugs]);
  console.log("Pages found in DB:", res.rows.map(r => r.slug));
  if (res.rows.length !== slugs.length) process.exit(1);
  await c.end();
}
check();
'\''
'</automated>
  </verify>
  <done>Las 6 páginas sueltas y legales están persistidas en Neon DB.</done>
</task>

<task type="auto">
  <name>Task 2: Configuración de redirecciones en Cloudflare CSVs y next.config.ts</name>
  <files>aprendoclub/next.config.ts, .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv, .planning/deliverables/v1.8/redirects-phase33-cloudflare-noheader.csv, .planning/deliverables/v1.8/redirects-phase33-cloudflare-both-domains.csv</files>
  <action>
    1. En `aprendoclub/next.config.ts`:
       - Añadir redirección para `{ source: "/recursos/guia-seo-para-principiantes", destination: "/programas/curso-basico-de-seo", permanent: true }`.
       - Añadir redirección para `{ source: "/seo-con-ia/evento", destination: "/programas/taller-seo-con-ia", permanent: true }`.
    2. En `.planning/deliverables/v1.8/redirects-phase33-cloudflare.csv` y sus variantes:
       - Añadir filas para:
         - `/contacto` -> `https://www.aprendoclub.com/contacto`
         - `/glosario` -> `https://www.aprendoclub.com/glosario`
         - `/politica-privacidad` -> `https://www.aprendoclub.com/politica-privacidad`
         - `/politica-reembolso` -> `https://www.aprendoclub.com/politica-reembolso`
         - `/aviso-legal` -> `https://www.aprendoclub.com/aviso-legal`
         - `/terminos-condiciones` -> `https://www.aprendoclub.com/terminos-condiciones`
         - `/recursos/guia-seo-para-principiantes/` -> `https://www.aprendoclub.com/programas/curso-basico-de-seo`
         - `/seo-con-ia/evento` -> `https://www.aprendoclub.com/programas/taller-seo-con-ia`
    3. Asegurarse de que `/prensa` NO esté presente en ninguno de los CSVs.
  </action>
  <verify>
    <automated>bash -c '
grep -q "contacto" .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv || { echo "FAIL_CONTACTO"; exit 1; }
grep -q "glosario" .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv || { echo "FAIL_GLOSARIO"; exit 1; }
grep -q "politica-privacidad" .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv || { echo "FAIL_PRIVACIDAD"; exit 1; }
grep -q "politica-reembolso" .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv || { echo "FAIL_REEMBOLSO"; exit 1; }
grep -q "aviso-legal" .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv || { echo "FAIL_AVISO"; exit 1; }
grep -q "terminos-condiciones" .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv || { echo "FAIL_TERMINOS"; exit 1; }
grep -q "guia-seo-para-principiantes" .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv || { echo "FAIL_GUIA"; exit 1; }
grep -q "seo-con-ia/evento" .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv || { echo "FAIL_EVENTO"; exit 1; }
grep -q "prensa" .planning/deliverables/v1.8/redirects-phase33-cloudflare.csv && { echo "FAIL_PRENSA_SHOULD_NOT_EXIST"; exit 1; }
echo MISC_REDIRECTS_OK
'</automated>
  </verify>
  <done>Redirecciones 301 agregadas a next.config.ts y Cloudflare CSVs, y /prensa confirmada descartada.</done>
</task>

<task type="auto">
  <name>Task 3: Verificación de compilación TypeScript y resolución de páginas</name>
  <files>aprendoclub/next.config.ts</files>
  <action>
    1. Ejecutar `npx tsc --noEmit` en `aprendoclub/`.
    2. Ejecutar prueba para validar que todas las 6 páginas son resolubles en la BD.
  </action>
  <verify>
    <automated>bash -c '
cd aprendoclub && npx tsc --noEmit || { echo "FAIL_TSC"; exit 1; }
echo "TSC_OK"
'</automated>
  </verify>
  <done>Compilación TypeScript limpia y páginas operativas.</done>
</task>

</tasks>
